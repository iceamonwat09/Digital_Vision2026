# =====================================================================
# requirements-server.txt — สำหรับ "Windows Server (IIS)" ที่ไม่มีกล้อง
# ใช้งาน: Login + ตรวจ Artwork (+ แดชบอร์ด/ประวัติ)
# =====================================================================
#
# ── เวอร์ชัน Python ที่ใช้กับไฟล์นี้ ─────────────────────────────────
#   ✅ 3.9.13 = ที่เลือกใช้บนเซิร์ฟเวอร์นี้ (ตรงกับสถานี) เพื่อให้ยังลง
#      onnxruntime==1.19.2 / openvino==2024.6.0 ได้ในวันที่ต้องต่อกล้อง
#      — ทั้งสองตัวมีล้อ cp39-win_amd64 เป็นรุ่นสุดท้าย
#      ⚠️ 3.9 หมดการสนับสนุนแล้ว (31 ต.ค. 2025) ต้องมีมาตรการชดเชย:
#      ดู "ภาคผนวก G" ใน docs/DEPLOY_IIS_WINDOWS_SERVER.md
#   ✅ 3.11 / 3.12 ใช้ได้ ถ้ายืนยันว่า "ไม่ต้องใช้กล้องบนเซิร์ฟเวอร์แน่นอน"
#      (ยังได้ security update แต่ลงตัวเร่ง onnx/openvino ข้างต้นไม่ได้)
#   ❌ 3.13 ห้าม — numpy 1.26 / Pillow 10.1 ไม่มีล้อ cp313
#
# ต่างจาก requirements.txt (ของเครื่องสถานีที่ต่อกล้อง) ตรงที่ตัดของที่
# เกี่ยวกับกล้อง/การเร่งความเร็ว inference ออก:
#   - ไม่ลง onnxruntime / onnx / onnxslim / openvino  (ไม่มีกล้อง = ไม่ต้องเร่ง)
#   - ไม่ลง pygrabber / comtypes                      (ใช้แจกแจงกล้อง USB เท่านั้น)
#   - ไม่ลง gevent                                    (IIS + waitress ทำหน้าที่แทน)
#   - เพิ่ม waitress                                  (WSGI server ที่ IIS เรียกใช้)
#   - เพิ่ม pytesseract                               (กรอบแดงชี้คำผิดใน Artwork)
#
# ⚠️ ยังต้องลง opencv + ultralytics (ซึ่งจะดึง torch มาด้วย ~2 GB) ถึงแม้
#    เซิร์ฟเวอร์จะไม่ตรวจกระป๋องเลย เพราะ app.py import โมดูลพวกนี้ไว้ที่หัวไฟล์
#    (บรรทัด 12/19/20) ถ้าไม่มีจะ import ไม่ผ่านทั้งแอป (ล็อกอินก็ไม่ขึ้น).
#    บน Windows ล้อ torch จาก PyPI เป็นรุ่น CPU อยู่แล้ว ไม่ต้องมี CUDA.
#
# ติดตั้ง (ใน venv ของโปรเจกต์ — ดู docs/DEPLOY_IIS_WINDOWS_SERVER.md):
#     .venv\Scripts\python.exe -m pip install -r deploy\requirements-server.txt

# ── Web ──────────────────────────────────────────────────────────────
Flask==3.0.0
Werkzeug==3.0.1
waitress>=3.0.0            # WSGI server ที่ HttpPlatformHandler เรียกขึ้นมา

# ── จำเป็นเพราะ app.py import ที่ระดับโมดูล ─────────────────────────
opencv-python==4.8.1.78
numpy>=1.26.0,<2.0         # opencv 4.8.1 ยังไม่รองรับ numpy 2.x
ultralytics==8.1.0         # ดึง torch (CPU) มาด้วย — ไม่ได้ใช้ตรวจบนเซิร์ฟเวอร์
Pillow==10.1.0

# ── ฐานข้อมูล (ผู้ใช้/สิทธิ์ + ประวัติ) ──────────────────────────────
pyodbc>=4.0.39             # ใช้ไดรเวอร์ {SQL Server} ที่ติดมากับ Windows อยู่แล้ว

# ── Login + RBAC ────────────────────────────────────────────────────
bcrypt>=4.1.0              # แฮชรหัสผ่าน — ขาดตัวนี้ = ล็อกอินไม่ได้เลย
PyJWT>=2.8.0               # access/refresh token ใน httpOnly cookie

# ── Artwork Proof Check ─────────────────────────────────────────────
PyMuPDF>=1.24.0            # เปิด/เรนเดอร์ PDF + ดึง text layer (กรอบแดงชั้นที่ 1)
requests>=2.31.0           # เรียก N8N webhook (OCR / แปลภาษา)
python-Levenshtein>=0.25   # เทียบข้อความ
scikit-image>=0.22.0       # ใช้ร่วมกับโหมดฉลากกระดาษ
pyspellchecker>=0.8.0      # ⚠️ ขาดตัวนี้ = ชั้นตรวจ dictionary หายเงียบ (จุดบอด QC)
pytesseract>=0.3.10        # กรอบแดงชั้นที่ 2 — ต้องลง tesseract.exe แยกด้วย

# ── ไม่จำเป็นบนเซิร์ฟเวอร์ แต่ปลดคอมเมนต์ได้ถ้าต้องใช้ ──────────────
# cryptography>=41.0.0     # เฉพาะตอนใช้ generate_cert.py (IIS ทำ TLS ให้แล้ว)

# ── 📷 เมื่อถึงวันที่ต้องต่อกล้องบนเซิร์ฟเวอร์นี้ ────────────────────
# อย่าเพิ่งปลดคอมเมนต์ตอนนี้ — ทำตาม "ภาคผนวก F" ใน
# docs/DEPLOY_IIS_WINDOWS_SERVER.md ซึ่งมีขั้นตอนบังคับที่ห้ามข้าม:
#   (1) คัดลอก weights\can_dent\bestX.pt มาเอง (ไม่ได้อยู่ในกิต)
#   (2) ติดตั้งของด้านล่าง  (3) ** ต้องรัน verify_onnx.py / verify_openvino.py
#       ให้ PASS ก่อนเปิดใช้ ** — เคยมีเคสจริงที่โหลดสำเร็จแต่ตรวจไม่เจอ
#       ทุกโหมดแบบเงียบ ๆ ไม่มี error  (4) ตั้ง VISIONIQ_IIS_INIT=full
# ⚠️ ทั้งสองตัวนี้คือเหตุผลเดียวที่เซิร์ฟเวอร์นี้ล็อกไว้ที่ Python 3.9
# pygrabber==0.2           # แจกแจงกล้อง USB (Windows)
# comtypes==1.4.16
# onnxruntime==1.19.2      # ล้อ cp39-win_amd64 ตัวสุดท้าย (1.20+ ตัด py39)
# onnxslim                 # ultralytics ตั้ง simplify=True เป็น default
# onnx>=1.15.0
# openvino==2024.6.0       # เฉพาะเมื่อเซิร์ฟเวอร์มี Intel iGPU จริง
#                          # (ไม่มี iGPU ให้ตั้ง config.OPENVINO_DEVICE = None)
