# CLAUDE.md — บริบทสำคัญสำหรับ AI ที่ทำงานต่อกับโปรเจกต์นี้

ระบบตรวจตำหนิบรรจุภัณฑ์ (Can Dent / ฉลาก / Artwork) — Flask + YOLO + SQL Server
รันบน **เครื่องสถานี Windows + Python 3.9** ที่ต่อกล้อง USB จริง อ่านไฟล์นี้ก่อนแก้โค้ด
เพื่อไม่พลาดกับดักที่เคยเจอมาแล้ว.

---

## 🔴 กฎเหล็ก (ยึดตลอด)

1. **ห้ามกระทบโหมดอื่น 100%** — ทุกฟีเจอร์ใหม่ต้อง opt-in (flag default = พฤติกรรมเดิม) และ
   scope เฉพาะโหมดที่ตั้งใจ. โหมดหลัก: **Live USB / RTSP / STREAM / Snapshot / Label / Artwork**.
2. **ความแม่นของการตรวจ (QC) สำคัญที่สุด** — ห้ามแลกความแม่นเพื่อความเร็ว. การเร่งความเร็ว
   ต้องคงผลตรวจให้เท่าเดิม (พิสูจน์ด้วย `verify_onnx.py` ก่อนเปิดใช้).
   - **ผลที่ผิดแบบมั่นใจ แย่กว่าไม่แสดงผล** — ใช้กับทุกชั้นที่ "ชี้จุด" ให้คนดู (เช่นกรอบแดงชี้คำผิด):
     ถ้าไม่มั่นใจให้**ไม่แสดง** ดีกว่าเดา เพราะคนจะเชื่อสิ่งที่ระบบชี้แล้วมองข้ามของจริง.
3. **วางแผน + ให้ผู้ใช้ยืนยันก่อนลงมือ** งานที่มีผลต่อโครงสร้าง/พฤติกรรม.
4. **ตรวจสอบความถูกต้องหลังทำ อย่างเป็นกลาง** (ไม่เข้าข้างตัวเอง) — ไล่ edge case + fallback.
5. Deploy: ผู้ใช้ `git pull` แล้ว **`py -3.9 app.py`** บนสถานี. ยืนยันโค้ดใหม่รันจริงด้วย
   **`CONFIG_VERSION` บน footer** (Flask อ่าน config ตอน start เท่านั้น — ต้องปิด-เปิดใหม่).

---

## 🚦 อ่านตรงนี้ก่อน — สถานะล่าสุด 25 ส.ค. 2026 (สำหรับแชทใหม่)

**Branch: `claude/igpu-candent-compatibility-2zip94`** · `CONFIG_VERSION` =
**`2026.08.25-expladder`** · pytest **911 ผ่าน / 58 fail (สับเซ็ตของ baseline)** (Label Paper `FieldResult`)

### ✅ 25 ส.ค. (รอบบ่าย) — **หาต้นเหตุ "ONNX ทำงานเสมอ" เจอ + ระบบซ่อม IR เองได้แล้ว**

อ่านหัวข้อ **"🎮 iGPU ทำงานกับโมเดลอะไรได้บ้าง"** ด้านล่างก่อน — ขั้นตอนแก้ด้วยมือ
ในกรอบถัดไปนี้ **ไม่ต้องทำแล้ว** ระบบทำให้เองตอนเปิด `app.py` (เก็บไว้เป็นประวัติ).
สิ่งที่ต้องทำบนสถานีคือ: `py -3.9 diagnose_igpu.py` → ถ้ามันซ่อมให้ ⇒ **ต้องรัน
`verify_openvino.py` ก่อนใช้งานจริงเสมอ**

### ▶️ ทำข้อนี้ก่อนอย่างอื่นทั้งหมด — **`bestX_openvino_model` เสีย ต้อง export ใหม่**

**หาต้นเหตุเจอแล้ว** (`diagnose_accel.py` บนสถานี 25 ส.ค.) — ไม่ใช่ไดรเวอร์
ไม่ใช่แพ็กเกจ ไม่ใช่ทั้งเครื่อง แต่เป็น **ไฟล์ IR ของโมเดลตัวเดียว**:

| โมเดล | task | backend ที่ได้ | inf @480 | inf @1280 |
|---|---|---|---|---|
| `best.pt` | detect | **OpenVINO@intel:gpu** ✅ | **19 ms** | 72 ms |
| `bestxx.pt` (= best.pt เนื้อเดียวกัน) | detect | **OpenVINO@intel:gpu** ✅ | **23 ms** | 90 ms |
| **`bestX.pt` (production)** | **segment** | **ONNX 🐢** | **274 ms** | **2,168 ms** |

```
2026-08-25 15:27:47 - WARNING - Accelerated backend smoke test failed
    (index 1 is out of bounds for dimension 0 with size 1).
2026-08-25 15:27:47 - ERROR - ⚠️ OpenVINO@intel:gpu ใช้ไม่ได้ — ตกไป backend ถัดไป
```

**อ่านข้อความนี้ให้ออก:** `preds[1]` บน tensor ที่มีมิติแรกขนาด 1 ⇒ IR คืน
**output เดียว** แต่โมเดล segmentation ต้องมี **2 output** (กล่อง `(1,38,N)` +
prototype mask `(1,32,H,W)`) ⇒ **ไฟล์ IR ถูก export ออกมาผิด**. OpenVINO
**โหลดผ่าน** (ไม่มี error ตอนโหลด) แล้วไปพังตอนตรวจภาพจริง

**เวลาบอกทุกอย่าง:** `bestX.onnx` = **1 ก.ค. 09:58** (ของเดิมที่ผ่าน verify) แต่
`bestX_openvino_model` = **24 ส.ค. 14:37** ⇒ IR ตัวที่ผ่าน `verify_openvino.py`
เมื่อ 2 ก.ค. **ถูกทับไปแล้ว** โดย re-export ที่เกิดจาก **บั๊ก mtime** (กู้ `bestX.pt`
กลับมา ⇒ mtime ใหม่กว่า IR ⇒ สั่ง export ใหม่ทุกครั้ง) — และ export รอบนั้นเสีย

> 💡 **ตาข่ายนิรภัยทำงานถูกต้อง 100%** — `_smoke_test()` จับได้และถอยไป ONNX
> แทนที่จะปล่อยให้ตรวจด้วย IR ที่เสีย. ราคาที่จ่ายคือความเร็ว **ไม่ใช่ความแม่น**
> (ตรงกับบทเรียน openvino 2025.3.0 ที่ "ตรวจไม่เจอแบบเงียบ ๆ" — คราวนี้ไม่เงียบ)

**▶️ ขั้นตอนแก้ (ทีละขั้น — ห้ามลบ IR ของโมเดลตัวอื่นที่ยังใช้ iGPU ได้อยู่):**

```
1) ปิด app.py
2) rmdir /s /q weights\can_dent\bestX_openvino_model
3) py -3.9 diagnose_accel.py --weights weights\can_dent\bestX.pt
   ✅ ต้องได้ backend = OpenVINO@intel:gpu · inf @480 ~45-50 ms · IR outputs = 2 ตัว
4) py -3.9 verify_openvino.py --weights weights\can_dent\bestX.pt --images <ภาพ NG จริง>
   ⚠️ ข้อ 4 **ห้ามข้าม** — ข้อ 3 พิสูจน์แค่ "รันได้" ไม่ได้พิสูจน์ "ตรวจถูก"
5) py -3.9 app.py → แถบ perf ต้องขึ้น `/ OpenVINO@intel:gpu` (ไม่มี 🐢)
```

**ถ้าข้อ 3 ยังเสียซ้ำ** (IR outputs = 1 อีก) แปลว่า **exporter ของ ultralytics
8.4.41 export โมเดล seg เป็น openvino ไม่ถูก** บนเครื่องนี้ ⇒ ทางเลือกถัดไปคือ
export จาก `bestX.onnx` (ซึ่งเป็นของ 1 ก.ค. และยังดีอยู่) ด้วย `ovc bestX.onnx`
แล้ววาง IR ลงในโฟลเดอร์เดิม — **ยังไม่ได้ลอง อย่าเพิ่งทำโดยไม่ verify**

> ⚠️ **ตัวเลข ONNX ที่วัดได้รอบนี้แย่กว่าที่เคยบันทึกไว้มาก** — 274 ms @480
> (เคยบันทึก ~280 ✅ ตรงกัน) แต่ **2,168 ms @1280** ขณะที่ตาราง "ถ่าย 1 เฟรม"
> เคยวัด inference imgsz 1280 ได้ ~420 ms **บน iGPU** ⇒ ปุ่ม "ถ่าย 1 เฟรม"
> ตอนนี้ช้ากว่าที่ควรเป็น **~5 เท่า** ด้วย เป็นผลพวงจากข้อเดียวกัน

### 🎮 iGPU ทำงานกับโมเดลอะไรได้บ้าง — และทำไม `bestX.pt` ถึงตกไป ONNX (25 ส.ค. 2026)

**คำตอบสั้น: iGPU ใช้ได้กับ *ทุก* โมเดลของ ultralytics — ไม่มีข้อจำกัดเรื่องชนิดโมเดล
เลย. `bestX.pt` ตกไป ONNX เพราะ *ไฟล์ IR ของมันเสีย* ไม่ใช่เพราะมันเป็น segmentation.**

**พิสูจน์บน container ด้วย ultralytics 8.4.41 + openvino 2024.6.0 (รุ่นเดียวกับสถานี)**
— export โมเดลทั้ง 5 task แล้วนับ output ของ IR:

| task | output ของ IR | รูปร่าง |
|---|---|---|
| detect / pose / obb / classify | **1** | `(1, 4+nc, N)` |
| **segment** | **2** | กล่อง `(1, 4+nc+nm, N)` ➕ prototype `(1, nm, H, W)` |

⇒ **โมเดล segment ที่ IR มี output เดียว = ไฟล์เสีย 100%** และเป็นเคสที่
`ultralytics` **โหลดผ่านโดยไม่มี error** แล้วไปพังตอนตรวจภาพจริง

**จำลองอาการของสถานีได้เป๊ะทุกตัวอักษร** (เอา IR ของโมเดล detect ไปวางในโฟลเดอร์
ของโมเดล seg แล้วโหลดด้วย `task="segment"`):

```
IndexError: index 1 is out of bounds for dimension 0 with size 1
```

#### 🔍 ทำไมถึงกลายเป็น "ONNX ทำงาน**เสมอ**" ไม่ใช่แค่ครั้งเดียว — บั๊ก 2 ชั้นซ้อน

| ชั้น | โค้ดเดิมทำอะไร | ผลที่ตามมา |
|---|---|---|
| ① | `_maybe_openvino()` จด `sha1` ของ `.pt` ลง `<xml>.src` **ทันทีหลัง export** ก่อนถึง smoke test | รอบถัดไป `_export_is_stale()` ตอบว่า "ตรงกัน ไม่ต้องทำใหม่" ⇒ **โหลด IR ที่เสียตัวเดิมซ้ำทุกครั้ง** |
| ② | smoke test จับได้แล้ว **ตกไป ONNX เงียบ ๆ** โดยไม่มีอะไรพยายามซ่อม | ตกไป ONNX **ถาวร** — รีสตาร์ตกี่รอบก็เหมือนเดิม |

> 💡 **`_smoke_test` ไม่ได้พัง — มันทำงานถูกต้อง แต่จับได้ *ทีหลัง* จุดที่ประทับตรา**
> ตาข่ายนิรภัยที่จับได้แต่ไม่มีทางออกกลับ = ระบบติดอยู่ในสภาพช้าตลอดไป

#### ✅ สิ่งที่แก้ (`CONFIG_VERSION 2026.08.25-irfix`)

| จุด | ของใหม่ |
|---|---|
| `_validate_ir()` | ตรวจ **จำนวน output เทียบกับ task ของ `.pt`** ก่อนส่งให้ ultralytics — รู้ผลทันทีโดยไม่ต้องรันโมเดล |
| จดลายนิ้วมือ | ย้ายไป**หลัง**ผ่านการตรวจ + `_drop_stamp()` ลบตราเมื่อพบว่าเสีย |
| ซ่อมตัวเอง | เสีย → **ลบโฟลเดอร์ IR แล้ว export ใหม่ 1 รอบ** → ยังเสีย → **สร้าง IR จากไฟล์ `.onnx` แทน** |
| `_quarantine_ir()` | smoke test ล้ม → ลบตรา + จดไว้ ⇒ **บูตหน้า export ใหม่** (เดิมโหลดตัวเดิมซ้ำ) |
| `<ov_dir>.badir` | ลองซ่อมได้ **1 รอบ** ไม่ใช่ export ใหม่ทุกบูตไม่รู้จบ · เนื้อ `.pt` เปลี่ยน = ได้โอกาสใหม่ |
| `_sync_metadata_names()` | ซ่อม IR แล้วต้องเขียน `metadata.yaml` จาก `.pt` ด้วย |
| `OPENVINO_VALIDATE_IR` / `OPENVINO_AUTO_REPAIR` | `False` ทั้งคู่ = พฤติกรรมเดิมก่อน 25 ส.ค. เป๊ะ |

**⚠️ ทำไมต้องเขียน `metadata.yaml` ใหม่ด้วย:** โฟลเดอร์ IR ที่เสียมักเป็นของ
**โมเดลตัวอื่น** (นั่นแหละเหตุที่ output ไม่ตรง) ⇒ `metadata.yaml` ในนั้นก็ของตัวอื่น.
ซ่อมแต่ `.xml`/`.bin` = ตรวจถูกแต่ **เรียกชื่อคลาสผิด** เงียบ ๆ. และวัดจริงแล้วว่า
**ไม่มี `metadata.yaml` เลย ⇒ ชื่อคลาสกลายเป็น `class0`/`class1`** โดยไม่มี error
⇒ อ่าน `.pt` ไม่ได้ = **ไม่ซ่อม** (กฎเหล็กข้อ 2)

**เส้นทาง ONNX → IR เชื่อถือได้แค่ไหน — วัดแล้ว ไม่ใช่เดา:** เทียบ tensor ดิบของ
PyTorch กับ IR ทั้งสองเส้นทางบนอินพุตเดียวกัน ได้ **Δ สูงสุด 5.3e-5** (กล่อง —
หน่วยพิกเซล) และ **9.8e-9** (prototype) **เท่ากันทั้งสองเส้นทางทุกหลัก**
> ⚠️ **ยังต้องรัน `verify_openvino.py` อยู่ดีก่อนใช้งานจริง** — ตัวเลขนี้วัดบน CPU
> ส่วน **GPU plugin รันภายในเป็น FP16** ซึ่งเป็นคนละเรื่อง (บทเรียนเดิมของ repo)

**🔑 ข้อเท็จจริงเชิงสถาปัตยกรรมที่ต้องรู้ (ไล่จากซอร์สของ ultralytics 8.4.41):**
`export(format="openvino")` **แปลงจากโมเดล PyTorch โดยตรง**
(`ov.convert_model(model, example_input=...)` ใน `ultralytics/utils/export/openvino.py`)
**ไม่ได้ผ่าน ONNX เลย** ⇒ `.onnx` ที่ดีอยู่ **ไม่ได้แปลว่า IR จะดีด้วย** และ
เป็นเหตุผลว่าทำไม "สร้าง IR จาก `.onnx`" ถึงเป็นเส้นทางสำรอง *ที่ต่างจริง ๆ*
> ⚠️ ultralytics รู้จักโฟลเดอร์ IR **เฉพาะที่ชื่อลงท้าย `_openvino_model`** เท่านั้น
> (วัดแล้ว: ชื่ออื่นได้ `TypeError: not a supported model format`) ⇒ การซ่อมต้อง
> เขียนทับในโฟลเดอร์เดิม ห้ามสร้างโฟลเดอร์ชื่อใหม่

#### 🔬 `diagnose_igpu.py` — ไล่ทีละชั้นของกองซ้อน (ใหม่)

```
py -3.9 diagnose_igpu.py                 # ตรวจทุก .pt ในโฟลเดอร์ weights
py -3.9 diagnose_igpu.py --fix           # ซ่อม IR ที่พบว่าเสีย
py -3.9 diagnose_igpu.py --quick         # อ่านอย่างเดียว
py -3.9 diagnose_igpu.py --images sample_cans   # เทียบความแม่นกับ PyTorch ด้วย
```

ต่างจาก `diagnose_accel.py` (ซึ่งตอบว่า *ตกไป backend ไหน*) ตรงที่ไฟล์นี้ตอบว่า
**ชั้นไหนคือตัวขวาง** โดยแยกสิ่งที่เดิมปนกันอยู่ในความล้มเหลวเดียว:

| ชั้น | ตอบคำถาม | แยกออกจาก |
|---|---|---|
| ① ค่าตั้ง | ระบบถูกสั่งให้ใช้ iGPU หรือเปล่า | — |
| ② แพ็กเกจ | openvino/onnxruntime/ultralytics/torch รุ่นไหน | — |
| ③ อุปกรณ์ | เห็น GPU ไหม · **compile โมเดลจิ๋วลงไปได้จริงไหม** | "เห็นชื่อ" ≠ "ใช้ได้" |
| ④ ไฟล์ IR | จำนวน output · **ช่องของกล่อง = 4+nc(+nm)** · `metadata.yaml` | ปัญหาของ **โมเดลตัวเดียว** vs ทั้งเครื่อง |
| ⑤ รัน IR ดิบ | เรียก OpenVINO ตรง ๆ ข้าม ultralytics | "IR/ไดรเวอร์พัง" vs "ตัวห่อถอดผลผิด" |
| ⑥ เส้นทางจริง | `load_model()` ตัวเดียวกับ app.py + เวลา @480/@1280 | — |
| ⑦ ความแม่น | เทียบ PyTorch ด้วยเกณฑ์ของ `verify_onnx` | "รันได้" ≠ "ตรวจถูก" |

**ด่าน ④ จับ IR ที่เป็นของโมเดลอื่นได้ด้วยหลักฐาน 2 ชิ้นที่เป็นอิสระกัน** — ทดสอบจริง:

```
IR outputs             1 ตัว: (?,84,?)
จำนวน output ที่ต้องมี  2 (task=segment) → ❌
ช่องของ output กล่อง    ได้ 84 · ต้องเป็น 116 → ❌   ⇒ IR **ไม่ใช่ของโมเดลตัวนี้**
```

> 💡 ช่องของกล่องบอกได้ถึงขั้น "IR นี้เป็นของโมเดลกี่คลาส" — `bestX` (2 คลาส, seg)
> ต้องได้ **38** (= 4+2+32). ถ้าเจอ 84 = IR ของโมเดล COCO 80 คลาส

**⚠️ บั๊กในตัวแก้ของตัวเองที่เทสต์จับได้ (แก้แล้ว):** `_export_is_stale()` มีสาขาที่
**เขียน sidecar ให้เอง** (กรณีอัปเกรดจากเวอร์ชันก่อนมีระบบ hash) ⇒ แค่ "ไม่จดตรา"
ไม่พอ IR ที่เสียจะถูกประทับโดยตัวตรวจ staleness เอง. ต้อง **ลบตราทิ้ง** และใช้
`.badir` เป็นแหล่งความจริงว่า "รอบนี้ต้องบังคับ export ใหม่"

**เทสต์: `tests/test_openvino_ir.py` 24 ตัว** — ล็อกตารางจำนวน output ที่วัดมา ·
"IR ที่เสียต้องไม่ได้ตรา" · เส้นทางยอมแพ้ 1 รอบ · ชื่อคลาสที่เป็นของโมเดลอื่น ·
"อ่านไม่ได้ = ไม่ตัดสิน". **พิสูจน์แล้วว่าจับของจริง** — ปิด flag ทั้งสองแล้วรัน
fixture เดียวกันได้ `smoke test failed (index 1 is out of bounds…)` → `ONNX` เป๊ะ
ตามที่สถานีเจอ · เปิด flag → `OpenVINO@intel:cpu` + IR กลายเป็น 2 output

### 🚨 มาตรฐาน NG ปลอมของหน้างาน — ตัวเลขที่กลับด้านทั้งโครงการ (25 ส.ค. 2026)

ผู้ใช้ส่งภาพหน้าจอ **ระบบ vision เดิมบนไลน์ 6** (ตรวจโค้ดพิมพ์ก้นกระป๋อง —
หน้าตาตรงกับ Keyence CV-X) มาให้เป็นข้อมูล **ไปดูเพิ่มไม่ได้ อ่านได้เท่าที่เห็น**:

```
TOT = 40062 · OK = 40062 · NG = 0 · NG rate = 0.00%      88ms      RUN
[11.CODE POSITION] Judge OK · Correlation 84.94 · Angle -0.61°
```

**อ่านได้ 3 อย่างที่ใช้ตัดสินใจได้ทันทีโดยไม่ต้องไปหน้างาน:**

| สิ่งที่อ่านได้ | แปลว่าอะไร |
|---|---|
| **NG 0 จาก 40,062** | เพดานบนของอัตรา NG ปลอม = **0.0075%** (Clopper-Pearson 95%) — นี่คือมาตรฐานที่พนักงานคุ้นเคย |
| **88 ms/ใบ** | รอบทั้งหมด (จับภาพ→filter 4 ชั้น→หาโค้ด→ตัดสิน) = 66% ของคาบ 133 ms ⇒ **imgsz 480 (62 ms) ทันแน่นอน · imgsz 1280 (420 ms) ตัดทิ้งได้เลย** |
| **ภาพเป็น binary ขาว-ดำ เห็นวงแหวนก้นกระป๋องคมชัด** | ไฟที่สถานีนั้นให้คอนทราสต์สูงพอบน **ผิวเดียวกับที่เราจะตรวจรอยบุบ** ⚠️ แต่เป็น bright-field (ตัวอักษรเด่น) ส่วน dent ต้องการ **dark-field** — คนละมุมไฟ |

**⇒ ตัวจำกัดของโครงการนี้คือ precision ไม่ใช่ recall** ซึ่ง **กลับด้านกับที่วัดมาทั้งหมด**:

| | false NG /นาที ที่ 450 CPM | /ชั่วโมง |
|---|---|---|
| เป้าในแผน (`PLAN` §1: ≤2%) | **9 ใบ** | 540 ใบ |
| ระบบเดิมบนไลน์ (≤0.0075%) | 0.03 ใบ | ~2 ใบ |

ห่างกัน **~270 เท่า** — และเชื่อมกับกรอบ `Can Dent 0.25` / `0.43` ที่ไปเกาะบน
**ตัวอักษรที่พิมพ์บนฝา** ในภาพหน้าจอระบบเราเอง ⇒ นั่นคือ false NG ที่กำลังจะเกิดจริง

> ⚠️ **เป้า "false alarm ≤ 2%" ใน `docs/PLAN_LINE_DENT_INSPECTION.md` §1 ยังไม่ได้แก้**
> — บันทึกความขัดแย้งไว้ในเอกสารแล้ว แต่การเปลี่ยนเกณฑ์รับงานเป็นการตัดสินใจของผู้ใช้

#### ✅ ปิดช่องที่ค้างมานาน: `blur_tolerance.py --ok-images`

เครื่องมือนี้วัดแต่ "ยังเจอไหม" มาตลอด (CLAUDE.md เตือนไว้เอง 2 ที่ว่า
*"ยังไม่ได้วัด NG ปลอม"*) ตอนนี้ไล่ความเบลอชุดเดียวกันบน **ภาพกระป๋องดี** ได้แล้ว:

| ของใหม่ | ทำอะไร |
|---|---|
| `sweep_false()` | นับ **ใบที่โดนทัก** (หน่วยเดียวกับ "NG rate %" ของไลน์) ไม่ใช่จำนวนกรอบ |
| `false_knee()` | เบลอสูงสุดที่ยัง **ไม่เพิ่ม** NG ปลอมจากฐานที่ L=0 (กติกาต่อเนื่องเหมือน `knee()`) |
| แถว `blur = 0` = **ฐาน** | NG ปลอมที่มีอยู่แล้วโดยไม่เกี่ยวกับความเบลอ **ต้องแยกออก** ไม่งั้นจะส่งผู้ใช้ไปแก้ exposure/ไฟ ทั้งที่ต้องแก้โมเดล/เกณฑ์ conf |
| รวมสองด้าน | รายงาน `min(recall_knee, false_knee)` + บอกว่า **ตัวจำกัดคือด้านไหน** |

**🔑 ชั้นที่สำคัญที่สุดคือความซื่อสัตย์ทางสถิติ (`upper_bound_95`)** — "ทดสอบ 30 ใบ
ไม่เจอ NG ปลอมเลย" **ไม่ได้แปลว่าอัตราเป็น 0**:

| จำนวนใบที่ทดสอบ (ไม่เจอเลย) | พิสูจน์ได้แค่ |
|---|---|
| 6 | ≤ 39.3% |
| 30 | ≤ 9.5% |
| 200 (ชุด blind ตามแผน) | ≤ **1.49%** |
| **39,942** | ≤ 0.0075% ← เท่าที่ไลน์ทำจริง |

⇒ **การทดสอบบนโต๊ะยืนยันว่า "ผ่านมาตรฐานหน้างาน" ไม่ได้เชิงสถิติ** ทำได้แค่
*เรียงลำดับว่าตั้งค่าแบบไหนแย่ลง* — เครื่องมือพิมพ์ข้อจำกัดนี้ออกมาเองทุกครั้ง
และเมื่อไม่ใส่ `--ok-images` จะขึ้นหัวข้อ **"⑤ NG ปลอม — ยังไม่ได้วัด"** พร้อม exit code 1
> 💡 เลข 39,942 ที่คำนวณได้ **เกือบเท่ากับ 40,062 ที่ไลน์รันจริงพอดี** — บังเอิญที่
> อธิบายได้: ระบบพาณิชย์ถึงกล้าประกาศ 0.00% เพราะมันวิ่งมาถึงจำนวนนั้นแล้ว

**เทสต์เพิ่ม 12 ตัวใน `tests/test_blur_tolerance.py` (รวม 38)** — ล็อกทั้ง
"นับใบไม่ใช่นับกรอบ" · "ฐานที่ L=0 ต้องไม่ถูกโทษว่าเป็นความผิดของความเบลอ" ·
"ผ่านแบบฟลุ๊คหลังพังแล้วต้องไม่ถูกนับ" · และตารางสถิติข้างบนทุกแถว

### 🏭 "กล้องถ่ายทัน 450 ใบ/นาที ไหม · เบลอไหม" — แยกเป็น 2 คำถามที่แก้คนละทาง (25 ส.ค. 2026)

| คำถาม | ตัดสินด้วยอะไร | เครื่องมือที่มี |
|---|---|---|
| **① ถ่ายทันทุกใบไหม** | `FOV ÷ ความเร็ว × fps` + เฟรมหาย/ทิ้ง | ✅ **โหมดถ่ายรัวตอบครบแล้ว** |
| **② เบลอกี่พิกเซล** | `เบลอ = ความเร็ว × exposure` **เท่านั้น** | ⚠️ มีสูตรแล้ว แต่ `mm/px` ยังเป็นค่าที่เดาไว้ |
| **③ ไม่มีไฟ → กดลงได้ต่ำสุดแค่ไหน** | ไล่ exposure + ชดเชย gain + **รันโมเดล** | 🆕 **`hik_exposure.py` (ทำแล้วรอบนี้)** |

**ข้อ ② ไม่ต้องมีวัตถุเคลื่อนที่ก็ตอบได้** — เบลอขึ้นกับ exposure อย่างเดียว ⇒
`blur_at_line_px` คำนวณได้แม้กระป๋องวางนิ่งสนิท (โค้ดทำแบบนี้อยู่แล้วโดยตั้งใจ)

#### 🔦 ข้อค้นพบที่เปลี่ยนสเปกไฟทั้งโครงการ: **gain ยังเหลือหัวอีก 16 dB**

บันทึกเดิมว่า "ต้องเพิ่มไฟ ~4.2 เท่า" **ลืมนับ gain** — กล้องมี gain ถึง **23.98 dB**
แต่ใช้อยู่แค่ **7.7 dB** ⇒ เหลือ **6.5 เท่า** ฟรี ๆ. คำนวณจากภาพหน้าจอสถานี
(สว่าง **101.9/255** ที่ exposure 2165 µs · gain 7.7 dB · เปิดรูรับแสงสุดแล้ว):

| exposure | สว่างที่ได้ | gain ที่ต้องใช้ให้กลับไป 80/255 | **เบลอที่ไลน์** |
|---|---|---|---|
| 2165 µs (ตอนนี้) | 101.9 | 5.6 dB | **16.8 px** ❌ |
| 1025 µs | 48.3 | 12.1 dB ✅ | 8.0 px ⚠️ ขอบเกณฑ์ที่โมเดลทนได้ |
| **512 µs** | 24.1 | **18.1 dB ✅ ยังไม่ชนเพดาน** | **4.0 px** ✅ เป้าออกแบบ |
| 350 µs | 16.5 | 21.4 dB ✅ | 2.7 px ✅ |
| 256 µs | 12.0 | 24.2 dB ❌ **เกินเพดาน** | 2.0 px |

⇒ **exposure 512 µs (เบลอ 4 px) ทำได้ตั้งแต่วันนี้โดยไม่ต้องมีไฟเพิ่มเลย**
⇒ **ราคาที่จ่ายคือสัญญาณรบกวน** ซึ่งเป็นตัวแปรเดียวที่ยังไม่มีใครวัด

#### 🛝 สร้างความเร็วระดับไลน์โดยไม่ต้องมีไลน์ (ไม่ต้องซื้ออะไรเลย)

โบกมือได้แค่ **74-1,635 px/s** ⇒ ระบบปฏิเสธการสรุปเรื่องเบลอถูกต้องแล้ว
(ต้อง ≥ 1,950 px/s = 25% ของไลน์). ทางออกที่ใช้ได้ทันที:

> กระป๋องที่ **ตกอิสระจากความสูงแค่ 21 mm** ก็ถึง 638 mm/s แล้ว (`v = √(2gh)`)
> ทางลาดกระดาษแข็ง/ไม้อัดเอียง ~25° ระยะไถลเพียง **15 cm** ⇒ ได้ ~640 mm/s พอดี
> **วางทางลาดให้วิ่งตามแกนกว้างของเฟรม** (ROI กว้าง-เตี้ย 2448×1024) ⇒ อยู่ในเฟรม
> ~155 ms = ~4 เฟรม ⇒ **วัดความเร็วได้** (ต้องมี ≥2 เฟรมถึงจับคู่ตำแหน่งได้)

⚠️ ถ้าปล่อยตกในแนวดิ่ง จะข้ามด้านสูง 84 mm ใน ~42 ms = ได้ **1 เฟรม** ⇒ วัด
*ความเบลอ* ได้ แต่ **วัดความเร็วไม่ได้** — ต้องให้วิ่งตามแกนกว้าง

### 🔦 โหมดใหม่ "ไล่ exposure" (`hik_exposure.py` · `CONFIG_VERSION 2026.08.25-expladder`)

**วางกระป๋องนิ่ง ๆ แล้วกดปุ่มเดียว** — ระบบไล่ exposure ให้ · ปรับ gain ชดเชย
อัตโนมัติ · รันโมเดลทุกเฟรม · สรุปว่า **exposure ต่ำสุดที่ยังเชื่อถือได้คือเท่าไร**
แล้วแปลเป็น **เบลอที่ความเร็วไลน์**

| ส่วน | ของใหม่ |
|---|---|
| `hik_exposure.py` | โมดูลใหม่ — ไม่ import Flask/ultralytics (รับ `detect_fn` เข้ามา) จึงเทสต์ด้วยของปลอมได้ |
| `app.py` | 6 route ใต้ `/api/camera/hik/exposure*` — **ไม่แตะการนับ/DB/verdict** |
| `static/js/hik_exposure.js` | โมดูลปิด เปิดออกมาแค่ `window.HikExposure` |
| `templates/index.html` | แผงในแถบข้าง + หน้าต่างผล (ตาราง + ภาพต่อขั้น) |
| เทสต์ | `tests/test_hik_exposure.py` **44 ตัว** + route อีก **10 ตัว** |

**⚠️ ต้องวัด 2 ทิศทาง ไม่ใช่ทางเดียว** — สัญญาณรบกวนทำร้ายงาน QC ได้ 2 แบบ:
`role="ng"` วางกระป๋องบุบจริง → **ยังเจอไหม** · `role="ok"` วางกระป๋องดี →
**เจอของที่ไม่มีไหม**. `combine()` **คืน `None` เมื่อมีด้านเดียว** ไม่สรุปจากด้านเดียว
(ช่องเดียวกับที่ `blur_tolerance.py` ยังปิดไม่ได้)

**ความปลอดภัย 3 ชั้น (โหมดนี้ *เปลี่ยนค่าในกล้องจริง* ต่างจากถ่ายรัวที่แค่บันทึกภาพ):**
1. **หยุดตรวจสดเสมอตลอดช่วง — ไม่ใช่ opt-in** ภาพช่วงนั้นถูกตั้งให้มืด/สว่างผิดปกติ
   โดยตั้งใจ ถ้าปล่อยให้ `inference_loop` ตรวจต่อ มันจะนับกระป๋องและ **เขียน NG ปลอม
   ลง DB** จากภาพทดสอบ (มีเทสต์นับ `detector.detect` เฉพาะที่ลูปสดเรียก)
2. **คืนค่ากล้องเดิมทุกตัวใน `finally`** รวมถึงตอนกดยกเลิก/โมเดลระเบิดกลางคัน —
   ไม่งั้นกล้องค้างที่ exposure ของขั้นสุดท้าย แล้วผลตรวจสดหลังจากนั้นผิดโดยไม่มีใครรู้
3. **deadline คลายการหยุดตรวจเอง** (จำนวนขั้น × 30 วิ + 30) ⇒ ต่อให้งานเบื้องหลัง
   พังทั้งหมด ระบบก็กลับมาตรวจเอง — เป็นไปไม่ได้ที่จะค้างในสภาพ "ไม่ตรวจ" ตลอดไป

**กติกา "ไม่รู้ ≠ ไม่ดี" ที่ฝังไว้ (แต่ละข้อคือกับดักที่ทำให้เครื่องมือโกหก):**
| จุด | ทำอะไร |
|---|---|
| วัด noise **ตามเวลา** (σ ข้ามเฟรม) | วัดในเฟรมเดียวจะนับลายกระป๋องเป็น noise = ตัวเลขที่ผิด |
| ฉากขยับ → `noise = None` | ถ้าขยับ σ ไม่ใช่ noise อีกต่อไป ⇒ ไม่รายงานตัวเลขที่ผิด |
| **ทุกขั้นมืดสนิท → ไม่โทษโมเดล** | ขึ้นว่า "เปิดฝาเลนส์/รูรับแสง" แทน — ไม่ส่งผู้ใช้ไปแก้ของที่ไม่ได้พัง |
| gain ชนเพดาน → ขั้นนั้นเป็นคำตอบไม่ได้ | ภาพมืดกว่าเป้า = ไม่ใช่การเทียบที่ยุติธรรม |
| **รันโมเดลทุกเฟรม ไม่ใช่เฟรมเดียว** | ที่ noise สูงผลตรวจจะ **กะพริบ** — "เจอ 5/5" กับ "เจอ 1/5" คือคนละคำตอบ |
| เดินจาก exposure **ยาวสุดลงไป** หยุดที่ขั้นแรกที่ตก | ขั้นที่ผ่านแบบฟลุ๊คหลังพังไปแล้วจะให้คำตอบดีเกินจริง (บทเรียนเดียวกับ `blur_tolerance.py`) |
| บันทึกภาพ **ความละเอียดเต็ม ไม่ย่อ** | การย่อ = เฉลี่ยพิกเซล = **ลบสัญญาณรบกวนทิ้ง** ซึ่งคือสิ่งที่กำลังวัดอยู่พอดี |

**🔬 ด่าน "ฉากนิ่งจริงไหม" — ต้องใช้สัดส่วน ไม่ใช่ค่าสัมบูรณ์ (บั๊กที่เทสต์จับได้):**
เกณฑ์แรกที่เขียนคือ "ความต่างระหว่างเฟรมเกิน N ระดับสี = ขยับ" ⇒ **ที่ gain 18-21 dB
สัญญาณรบกวนเองมี σ ≈ 8-12 ระดับสี ⇒ เกินเกณฑ์ทันที** ⇒ ระบบจะบอกว่า "ฉากขยับ"
แล้วไม่ยอมรายงาน noise **ในขั้นที่สำคัญที่สุดพอดี**. แก้เป็น **สัดส่วน σ ของภาพที่
เบลอแล้ว ÷ σ ของภาพดิบ**:

| | สัดส่วน | เหตุผล |
|---|---|---|
| ฉากนิ่ง (gain 0 → 21 dB) | **0.114-0.116 คงที่ทุกระดับ** | noise = ความถี่สูง ⇒ เบลอ 9×9 ลดลง 9 เท่า (ทฤษฎี 1/9 = 0.111) |
| ฉากขยับ 5-60 px/เฟรม | **0.51-0.93** | โครงสร้างรอดจากการเบลอ |

เกณฑ์ **0.30** ห่างจากทั้งสองฝั่ง ~2.6 เท่า · ขีดจำกัดที่วัดได้: ที่ gain 21 dB
จับการขยับได้ตั้งแต่ **~8 px/เฟรม** · ที่ gain 0 จับได้ตั้งแต่ **~3 px/เฟรม**

**🐛 บั๊กที่เจอเพราะขับเบราว์เซอร์จริง (เทสต์ยูนิตมองไม่เห็นทั้งคู่):**
1. **หน้าต่างผลโปร่งใส** เห็นแถบข้างทะลุขึ้นมาทับตาราง — เพราะเขียน
   `class="snap-panel"` / `snap-head` ซึ่ง **ไม่มีอยู่จริงในไฟล์นี้** (ของจริงคือ
   `.snap-modal` / `.snap-modal-head`) ⇒ ไม่มีพื้นหลัง/เงา/มุมโค้งเลย
   — ตระกูลเดียวกับกับดัก `$("id")` ที่ไม่มีจริงแล้วเงียบ
2. **CSS ของหน้าต่างผลไปอยู่ในบล็อกของแถบข้าง** ⇒ เทสต์เดิม
   `test_no_fixed_width_columns_wider_than_the_sidebar` แดงทันที (มันสแกนเฉพาะช่วง
   `.hik-presets` → `.snap-overlay`) — **เทสต์ทำงานถูกต้อง** กริดของหน้าต่างต้อง
   อยู่นอกช่วงนั้น
> ⚠️ และ **Flask แคช template** ตอน `debug=False` ⇒ แก้ `index.html` แล้วต้อง
> **รีสตาร์ต** ไม่งั้นเบราว์เซอร์จะเห็นของเก่าแล้วไล่บั๊กผิดทาง (เสียเวลาไป 1 รอบ)

**🐛 บั๊กเดิมที่แก้ไปพร้อมกัน:** fixture `shotclient` ใน `test_hik_routes.py` พึ่ง env
`AUTH_ENABLED` อย่างเดียว ⇒ ไฟล์เทสต์ auth เปิดค่านี้ค้างไว้ ⇒ **รันทั้งชุดพร้อมกัน
แล้วได้ 401 ⇒ error ที่ setup 5 ตัว** (เป็นมานานแล้ว) ตอนนี้บังคับปิดใน fixture

**ผลรัน:** pytest **911 ผ่าน / 58 fail** — ชุดที่ fail เป็น **สับเซ็ตของ baseline**
(แก้ error เดิมได้ 5 ตัว ไม่มีของใหม่พัง) · ขับด้วย Chromium จริงบน SDK ปลอมแล้ว:
แผงขึ้นในแถบข้าง 263px · หน้าเว็บไม่เลื่อนแนวนอน · ตัวประมาณคำนวณตรงกับฝั่ง Python
(2000 µs → 15.6 px · 512 µs → 4.0 px) · ตาราง 5 ขั้น · การ์ดภาพ 5 ใบโหลดครบ ·
กริดไม่ซ้อนทับ · **ไม่มี JS error**

> ▶️ **ลำดับที่ควรทำบนสถานี**
> 1. **วัด mm/px** (2 นาที) — ถ่ายไม้บรรทัดที่ระนาบฝากระป๋องด้วยปุ่ม "ถ่าย 1 เฟรม"
>    → ใส่ `HIK_BURST_MM_PER_PX` และแก้ `HIK_BURST_LINE_SPEED_PX_S` ให้ตรง
>    **ถ้าไม่ทำข้อนี้ ทุกตัวเลข exposure/เบลอยังเป็นการเดา**
> 2. **ไล่ exposure กับกระป๋อง NG** → ได้ exposure ต่ำสุดที่ยังเจอรอยบุบ
> 3. **ไล่ exposure กับกระป๋องดี** → ได้ exposure ต่ำสุดที่ยังไม่มี NG ปลอม
>    ⇒ ระบบจะสรุปคำตอบรวมให้เอง (เอาด้านที่เข้มกว่า)
> 4. **ทางลาดเอียง** + ถ่ายรัวที่ exposure ที่ได้จากข้อ 3 → ยืนยันด้วยของจริง
> 5. ถ้าข้อ 3 ได้ exposure ที่เบลอ ≤ 4 px ⇒ **ตอบได้ว่าไม่ต้องซื้อไฟ**

### 🎯 ค่าที่ควรใช้กับไลน์ (จากผลวัด 3 ROI)

| ค่า | ตั้งเป็น | เหตุผล |
|---|---|---|
| **ROI** | **2448×1024 ("กว้าง-เตี้ย")** | เฟรมเต็มใบ/กระป๋อง **4.97** vs ครึ่งกลาง 1.11 |
| exposure | **~513 µs** (ปลายทาง) | ให้เบลอ 4 px ที่ความเร็วไลน์ · ตอนนี้ 2165 µs = เบลอ 16.9 px |
| ไฟ | **ต้องเพิ่ม ~4.2 เท่า** | จาก 2165 → 513 µs |
| ถ่ายรัว | หน้าต่าง **133 ms** + หยุดโมเดล | 1 ภาพต่อกระป๋อง · ดิสก์ไม่ทิ้งเฟรม |

> ⚠️ **`HIK_DEFAULTS` ตั้ง ROI ไว้ที่ 1224×1024** (ค่าที่ดีที่สุดตอนที่ยังไม่ได้ทดสอบ
> "กว้าง-เตี้ย") — **ยังไม่เปลี่ยนเป็น 2448×1024** เพราะ fps ที่วัดได้ยังถูก inference
> ที่ช้าบีบอยู่ ⇒ **ต้องแก้ inference ก่อน แล้ววัด ROI ใหม่ทั้งสองแบบ** ค่อยตัดสิน
> ⚠️ ค่าที่ผู้ใช้บันทึกเองจากหน้าเว็บ (`HIK_SETTINGS_FILE`) **ทับ `HIK_DEFAULTS` เสมอ**

### ❓ 3 ตัวเลขที่ยังไม่เคยวัด และค้ำทุกอย่างอยู่

1. **mm/px** — ทุกตัวเลข exposure/ไฟ/เฟรมต่อใบ อิง 0.082 ที่**เดาไว้** (ถ่ายไม้บรรทัด 2 นาที)
2. **NG ปลอมตอนเบลอ** — `blur_tolerance.py` วัดแค่ "ยังเจอไหม" ไม่ได้วัด "เจอของที่ไม่มี"
3. **ความเร็วจริงที่ทดสอบ** — ยังโบกได้แค่ 74 px/วิ (ไลน์ 7,800) ⇒ ยังสรุปเรื่องเบลอไม่ได้

---

## 🎥 งานปัจจุบัน — ต่อกล้อง Hikrobot MV-CS050-10GC เข้าโหมด Can Dent (19 ส.ค. 2026)

**Branch: `claude/can-dent-detection-hikrobot-test-3zhqg9`** (ห้าม push main ·
ก่อนหน้า: `claude/hikrobot-camera-modes-s2hxiy` merge เข้ามาแล้วผ่าน PR #40)

ผู้ใช้ได้กล้องอุตสาหกรรมมาแล้ว: **Hikrobot MV-CS050-10GC** (IMX264 2/3" **2448×2048 global
shutter**, GigE, ~24 fps เต็มเฟรม, exposure 15 µs–10 s, gain 0–24 dB, มี Trigger Line0) ·
SN `DA4994130` · firmware `V4.0.42` · กล้อง `172.32.1.253` บน NIC `172.32.1.9` · ติดตั้ง MVS แล้ว.

⚠️ **ไม่ใช่รุ่นที่ `docs/PLAN_LINE_DENT_INSPECTION.md` §5 เลือกไว้ (CS016-10GC)** — ดู §0
ของเอกสารนั้นสำหรับตารางเทียบและตัวเลขที่เปลี่ยน. สรุปสั้น: ทิศทางในแผนยังถูก แต่
(ก) **fps 24 ไม่ใช่ 65** ⇒ free-running ที่ 12 ใบ/วิ เหลือ ~2 เฟรม/ใบ (ตรรกะนับ "เฟรมว่าง"
ของ live mode ใช้บนไลน์จริงไม่ได้) · (ข) **5MP@24fps ≈ 960 Mbps ชนเพดาน GigE** ⇒ ต้อง
Jumbo Frame + packet size ที่เหมาะ ไม่งั้น **ภาพแหว่งเงียบ ๆ** · (ค) **5MP ที่ imgsz 480
ทิ้งรายละเอียด ~5 เท่า** ⇒ ต้องใช้ ROI ให้ฝาเต็มเฟรม · (ง) ภาพใน MVS ที่ exposure 2635 µs
ยังมืด ⇒ จะไป 150-200 µs ต้องเพิ่มไฟ **~13-17 เท่า**.

**แผนที่ผู้ใช้อนุมัติแล้ว:** เพิ่ม **แหล่งภาพที่ 4** ("กล้องอุตสาหกรรม (Hikrobot)")
แบบ **free-running + ถ่ายทีละเฟรม** เพื่อทดสอบ `bestX.pt` — **ยังไม่ทำ triggered mode**
(รอ photo-eye + ไฟ). ลำดับงานและรายการค่าที่ปรับได้อยู่ใน §0 ของเอกสารแผน.

| ขั้น | ของที่ส่งมอบ | สถานะ |
|---|---|---|
| 1 | **`diagnose_hikrobot.py`** + `tests/fake_mvs/` + `tests/test_hikrobot_diagnose.py` (16 ตัว) | ✅ **เสร็จ + รันบนสถานีแล้ว** |
| 2 | **`hik_camera.py`** — คลาส `HikCamera` สัญญาเดียวกับ `camera.Camera` | ✅ เสร็จ |
| 3 | **`config.py`** — ค่าใหม่ 15 ตัว (opt-in ทั้งหมด) + `CONFIG_VERSION` ใหม่ | ✅ เสร็จ |
| 4 | **`app.py`** — สาขา `hik:` ใน `start_detection` + 5 route ใหม่ | ✅ เสร็จ |
| 5 | **แท็บที่ 4 + `static/js/hik_camera.js`** | ✅ เสร็จ |
| 6 | เทสต์ 51 ตัว + เอกสาร | ✅ เสร็จ (**ยังไม่ได้ทดสอบกับกล้องจริง**) |

### 🧩 โหมดใหม่ทำงานอย่างไร (ขั้นที่ 2-6 — `CONFIG_VERSION 2026.08.19-hikrobot`)

| ส่วน | ของใหม่ | หลักการที่ยึด |
|---|---|---|
| `hik_camera.py` | `HikCamera` (`initialize`/`read_frame`/`release`) · `scan_devices()` · `probe_params()` · `load/save_settings()` | **`capture_loop`/`inference_loop`/`generate_frames` ไม่ถูกแก้แม้บรรทัดเดียว** |
| `app.py` | สาขา `camera_index` ขึ้นต้น `hik:` (แบบเดียวกับ sentinel `stream` เดิม) + `/api/camera/hik/{scan,params,status,dataset,shot}` | ไม่แตะ route/สถานะของโหมดเดิมเลย |
| `templates/index.html` | แท็บ **"กล้องอุตสาหกรรม"** + แผงตั้งค่า + แถบสถิติ `#hikStats` | เพิ่มอย่างเดียว ไม่แก้ตรรกะของ 3 แท็บเดิม |
| `static/js/hik_camera.js` | โมดูลปิด เปิดออกมาแค่ `window.HikUI` | สคริปต์ในหน้าเดิมยาว ~950 บรรทัดและคุม 3 โหมด — ไม่เอาไปปนกัน |

**สิ่งที่ฝังไว้เป็นพฤติกรรมของคลาส (มาจากผลวัดจริง ไม่ใช่ทฤษฎี):**
1. **ตั้ง packet size (auto) + delay 0 ทุกครั้งตอนเปิดกล้อง** — กล้องไม่จำค่านี้
   (มีเทสต์ `test_open_sets_packet_size_every_time` ล็อกไว้)
2. **เฟรมที่คืนเป็นสำเนาเสมอ** ไม่ใช่ view ของบัฟเฟอร์ที่ใช้ซ้ำ — `capture_loop` เก็บเฟรมไว้
   ให้ `inference_loop` อ่านทีหลัง ถ้าเป็น view จะถูกเขียนทับกลางคัน = **ตรวจภาพที่ไม่มีอยู่จริง**
3. **ย่อภาพก่อนส่งเข้า pipeline** (`HIK_LIVE_MAX_WIDTH=1280`) — 5MP = 15 MB/เฟรม
   ถ้าส่งดิบ ๆ `generate_frames` ต้อง encode JPEG ของภาพ 5MP ทุกเฟรม
4. **แปลงสีด้วยตัวแปลงของ SDK** ไม่ใช่เดา Bayer pattern เองใน OpenCV (สลับผิด = สีเพี้ยนเงียบ ๆ)
5. **ค่าที่ GenICam ล็อกระหว่างสตรีม** (Width/Height/PixelFormat/ReverseX/Y) จะถูกตั้งใน
   ช่วง Stop→Start ให้อัตโนมัติ — ไม่งั้น set ไม่ติดแบบเงียบ แล้วค่าบนจอไม่ตรงกับกล้องจริง
6. **`get_params`/`set_params` ถือ lock ทั้งชุด** — ถ้าปล่อยให้อ่านทีละ node เธรดจับภาพ
   จะแทรกคิวทุกครั้ง อ่าน 22 ค่าจะกลายเป็น 22 × เวลารอเฟรม (หน้าเว็บค้างหลายวินาที
   ตอนอยู่ในโหมดทริกเกอร์ที่ยังไม่มีสัญญาณ)
7. **ไม่มีเมธอด `set_control`** โดยตั้งใจ — `/api/camera/control` (สไลเดอร์ brightness ของ
   กล้อง USB) กรองด้วย `hasattr(cam, "set_control")` ถ้ามีชื่อชนกัน สไลเดอร์โหมด USB
   จะไปสั่งกล้องอุตสาหกรรม (มีเทสต์ทั้งระดับคลาสและระดับ HTTP ล็อกไว้)
8. **`probe_params()` ไม่เขียนอะไรลงกล้องเลย** — แค่เปิดแท็บดูค่าต้องไม่ทับค่าที่ช่างตั้งใน MVS

**ปุ่ม/ค่าที่มีในแผง:** เลือกกล้อง (ตามซีเรียล) · ROI สำเร็จรูป (เต็ม/ครึ่งกลาง/¼ กลาง) ·
ตั้งค่า 19 ตัวที่สร้างจาก **ช่วงที่กล้องบอกเอง** (exposure/gain/auto/WB/framerate/ROI/
pixel format/reverse/trigger/packet) · เก็บภาพชุดข้อมูลความละเอียดเต็ม ·
**ถ่าย 1 เฟรมเต็มความละเอียดแล้วตรวจที่ imgsz 1280** · แถบสถิติสด (fps จริง · เฟรมหาย ·
แพ็กเก็ตหาย · ความสว่าง/พิกเซลล้น) — ตัวเลขทั้งหมดเป็นของที่วัดได้ ไม่ใช่ค่าที่ตั้งไว้

**⚠️ ยังไม่ได้ทำ (ตั้งใจ):** triggered mode จาก photo-eye (§4 ของแผน) · ใช้กล้องนี้กับ
ปุ่ม "ถ่ายรูปตรวจ" เดิม (viewfinder ผูกกับ `Camera` + resolution ladder) · การนับบนไลน์จริง

### 🎬 โหมด "ถ่ายรัว" (burst) — ตอบว่า **กล้องหยุดการเคลื่อนที่ได้ไหม** (20 ส.ค. 2026)

**โจทย์ของผู้ใช้:** *"เลื่อนวัตถุผ่านหน้ากล้อง กดปุ่มเดียวให้ถ่ายรัว 10 วินาที
แล้วดูภาพทั้งหมด + ลบได้ ไม่งั้นภาพจะเต็ม"* · **ไลน์จริง = 450 ใบ/นาที = 7.5 ใบ/วิ**

| ส่วน | ของใหม่ |
|---|---|
| `hik_burst.py` | โมดูลใหม่ (ไม่ import Flask/ultralytics) — วัดผล · แกลเลอรี · ลบ · งานเบื้องหลัง |
| `app.py` | 10 route ใต้ `/api/camera/hik/burst*` — **ไม่แตะการนับ/DB/verdict เลย** |
| `static/js/hik_burst.js` | โมดูลปิด เปิดออกมาแค่ `window.HikBurst` (ไม่ปนกับ `hik_camera.js`) |
| `templates/index.html` | แผง "🎬 ถ่ายรัว" + แกลเลอรีเต็มจอ + ภาพขยายรายเฟรม |
| เทสต์ | `tests/test_hik_burst.py` **63 ตัว** + route อีก **22 ตัว** ใน `test_hik_routes.py` |

**ลำดับการทำงาน (สำคัญที่สุด — อย่ากลับด้าน):**
`ถ่ายที่อัตรากล้อง → วัดผลทั้งชุด → ตรวจ 12 ใบที่คมที่สุด`
> ⚠️ **ห้ามทำเป็น "ถ่าย → ตรวจ → ถ่าย → ตรวจ"** ซึ่งเป็นสิ่งที่คำว่า "ถ่ายส่งตรวจรัว ๆ"
> ชวนให้ทำ: detect ที่ imgsz 1280 = 370-468 ms ⇒ ได้ **~2 ภาพ/วินาที** และวัตถุขยับ
> **~270 mm ระหว่างสองภาพ** ⇒ สิ่งที่วัดได้คือ *ความเร็วของ bestX* ไม่ใช่
> *ความสามารถของกล้อง*. ถ่ายอย่างเดียวที่ ROI ครึ่ง 69 fps ได้ **~690 ภาพ/10 วิ**
> (วัตถุขยับ ~9 mm ต่อภาพ). ใช้ `_DatasetWriter` ตัวเดิม ⇒ ไม่มีเธรดใหม่มาแย่งเฟรม

**ตัวเลข 4 ตัวที่แกลเลอรีแสดง (คำนวณตอนเปิดดู ไม่ใช่ตอนถ่าย):**
| ตัวเลข | ตอบอะไร |
|---|---|
| คะแนนคม (Tenengrad) | คมแค่ไหน |
| **อัตราส่วนขอบแกน x/y** | **แยก "เบลอเพราะเคลื่อนที่" (แกนเดียว) ออกจาก "โฟกัสหลุด" (สองแกน)** |
| ระยะเลื่อนจากเฟรมก่อน | หา **ความเร็วจริง** ของการโบกมือได้โดยไม่ต้องมีจานหมุน |
| **เบลอกี่พิกเซล** | ความเร็วที่วัดได้ × exposure จาก `meta.json` |

**ตรวจสอบความถูกต้องด้วยการจำลองที่ 450 CPM** (638 mm/s, 0.082 mm/px) — วิ่งผ่าน
`compute_metrics()` ตัวจริง: ความเร็วที่วัดได้ 7848-7870 px/s (จริง 7780 = **คลาด <1%**) ·
เบลอที่วัดได้ 20.68/6.30/2.36/1.00 px (จริง 20.50/6.22/2.33/1.00) · `max_exposure_us_1px`
= **127 µs** ตรงกับที่คำนวณด้วยมือ (128 µs).

**สิ่งที่ฝังเป็นพฤติกรรม (มาจากการวัด ไม่ใช่ทฤษฎี):**
1. **ต้องวัดที่ "วัตถุ" ไม่ใช่ทั้งเฟรม** — วัดจริง: วัตถุที่เบลอ 15 px ทำให้ความคม
   *ทั้งเฟรม* ลดลงเหลือ **0.97 เท่า** (แทบแยกไม่ออก) แต่ความคม *เฉพาะวัตถุ* เหลือ
   **0.10 เท่า**. ถ้าวัดทั้งเฟรมหน้าเว็บจะบอกว่า "คม" ทั้งที่วัตถุอ่านไม่ออก =
   **คำตอบที่ผิดแบบมั่นใจ** (กฎเหล็กข้อ 2). หาวัตถุด้วยการเทียบกับ **ค่ามัธยฐาน
   ของทั้งชุด** (= ฉากหลัง) ไม่ใช่เทียบเฟรมติดกัน (ซึ่งได้เงาสองอัน)
2. **คะแนน phase correlation อย่างเดียวเชื่อไม่ได้** — วัดจริง: ภาพสุ่มสองใบที่ไม่
   เกี่ยวกันเลยได้ **0.265** ขณะที่การเลื่อนจริง 25 px ได้ **0.289** ⇒ ตั้งเกณฑ์แยก
   ไม่ได้. จึง **ตรวจซ้ำด้วยการวัดที่เป็นอิสระ**: ระยะที่จุดกึ่งกลางกรอบวัตถุขยับ
   (หยาบแต่คนละวิธี) — ไม่ตรงกัน = คืน `None` ไม่ใช่เดา
3. **หน้าต่างจับคู่ต้องรัดวัตถุ** — ฉากหลังที่นิ่งและกินพื้นที่ส่วนใหญ่จะ "ชนะ"
   แล้วได้ระยะเลื่อน ≈ 0 ทั้งที่วัตถุขยับจริง (มีเทสต์ล็อกทั้งเคสที่ผ่านและไม่ผ่าน)
4. **`meta.json` ต่อชุด** (exposure/gain/ROI + **เวลามาถึงของทุกเฟรม**) — ตัวเก็บ
   ชุดข้อมูลเดิมเขียนแค่ `00001.jpg` ล้วน ⇒ เก็บ 3 exposure แล้ว **แยกไม่ออกทีหลัง**
   = ข้อมูลที่อุตส่าห์ไปเก็บที่ไลน์ใช้วัดอะไรไม่ได้เลย. เวลาบันทึกใน **เธรดจับภาพ**
   ตอนเข้าคิว (ไม่ใช่ตอนเขียนไฟล์ซึ่งช้าและไม่คงที่) และตัดให้เท่าจำนวนที่เขียนจริง
5. **"ต้องเพิ่มไฟกี่เท่า" รายงาน 2 ตัวแยกกัน** — `light_factor_needed` (ให้สว่าง
   *เท่าเดิม* ที่ exposure สั้นลง) กับ `light_factor_usable` (ให้สว่าง *พอใช้งาน*
   ~80/255). ตัวแรกอย่างเดียวทำให้เข้าใจผิดว่า "เพิ่ม 21 เท่าแล้วจบ" ทั้งที่ภาพ
   ตอนนี้ก็มืดอยู่แล้ว. `TARGET_MEAN = 80.0` **ต้องตรงกับ `diagnose_hikrobot.py`**
6. **ตัวเขียนไฟล์มีตัวเดียวต่อกล้อง** ⇒ ถ่ายรัวกับ "เก็บชุดข้อมูล" เปิดพร้อมกันไม่ได้
   (ทั้งสองทางตอบ 409) ไม่งั้นชุดที่กำลังถ่ายถูกตัดจบกลางคันแบบเงียบ

**⛔ 2 ไอเดียที่วัดแล้วปฏิเสธ (อย่าเสียเวลาทำซ้ำ):**
- **ลบฉากหลังก่อนจับคู่ตำแหน่ง** (absdiff กับฉากหลัง) — ฟังดูควรช่วย แต่แย่ลงมาก
  (เลื่อนจริง 24 px อ่านได้ **1.7 px**) เพราะภาพผลต่างของวัตถุ ณ สองตำแหน่งมี
  "ลายข้างใน" ไม่เหมือนกัน (ฉากหลังใต้วัตถุคนละที่) = ไม่ใช่การเลื่อนอีกต่อไป
- **เช็คความสมมาตร a→b กับ b→a เพื่อกรองผลมั่ว** — `cv2.phaseCorrelate` ให้ผล
  ตรงข้ามกันเป๊ะเสมอ (คลาดเคลื่อน **0.000** ทุกเคสรวมทั้งภาพสุ่ม) ⇒ ใช้แยกไม่ได้เลย

**🐛 2 บั๊กที่เจอจากการขับด้วย Chromium จริง (เทสต์ยูนิตจับไม่ได้ — แก้แล้ว):**
- **ลบทั้งชุดขณะงานวัดผลยังวิ่ง** → งานเขียน `metrics.json`/`_thumbs/` กลับลง
  โฟลเดอร์ที่เพิ่งลบ ⇒ เหลือ **"ซากชุดที่ไม่มีภาพสักใบ"** ค้างในรายการตลอดไป.
  แก้: route ลบสั่ง `cancel_job(wait=3.0)` ก่อนเสมอ
- **ไฟล์หายระหว่างเสิร์ฟ** (ผู้ใช้กดลบขณะภาพย่อยังทยอยโหลด) → `send_file` โยน
  `FileNotFoundError` = **HTTP 500 + stack trace** ทั้งที่เป็นเรื่องปกติ. แก้เป็น 404
- (+ อีก 3 จุดของ UI: หน้าจอค้างที่ "ยังไม่ได้วัด" ตลอดช่วงที่กำลังตรวจ ·
  สั่งงานไม่สำเร็จแล้วไม่รีเฟรช · บอกว่า "วัดเฉพาะในกรอบสีฟ้า" ทั้งที่ยังไม่ได้วัด)

**🎨 หน้าตา (ปรับหลังผู้ใช้เห็นของจริง 20 ส.ค.):** ใช้โทเคนของ `style.css` ทั้งหมด
(`--navy/--blue/--gray-*/--radius/--shadow-*` + `.btn`) ⇒ เป็นส่วนหนึ่งของหน้าเดียวกัน.
สรุปผลอยู่ใน **การ์ด KPI 8 ใบ** (ตัวเลขใหญ่ + สีตามเกณฑ์) แทนย่อหน้ายาว ·
**แถบเตือน "ทิ้งกี่เฟรม" แยกเป็นกล่องแดงของตัวเอง** เพราะเฟรมที่ทิ้ง = กระป๋องที่หายไป ·
การ์ดแต่ละใบ = ภาพย่อ + ป้ายบนภาพ + ตาราง key/value 4 บรรทัด · ปุ่มลบใช้
`.btn-danger-soft` (ขาว-ขอบแดง) ไม่ใช่แดงทึบ — แดงทึบสงวนให้ Stop Detection

**🐛 บั๊ก CSS ที่ต้องรู้ (เจอบนสถานีจริง — เทสต์ยูนิตจับไม่ได้เลย):**
การ์ดในแกลเลอรี**ซ้อนทับกัน** เห็นเป็นแถบเทายาวมีแต่ปุ่มกากบาท ไม่มีตัวเลขสักตัว.
วัดแล้วพบว่า `getBoundingClientRect` บอกว่าการ์ดสูง 277px **แต่แถวของกริดสูงแค่ 48px**
⇒ การ์ดล้นออกไปทับใบถัดไป. สองสาเหตุที่ต้องแก้พร้อมกัน:
1. **`grid-auto-rows: max-content`** — กริดที่ความสูง "ถูกกำหนดมาแล้ว" จากชั้น flex
   จะบีบแถว auto ให้เล็กลง
2. **ความสูงภาพย่อต้องเป็น px คงที่** (`height: 152px`) — ห้ามใช้ `aspect-ratio` +
   `width:100%` เพราะความสูงขึ้นกับความกว้างคอลัมน์ซึ่ง**ยังไม่รู้ค่าตอนกริดคำนวณแถว**
   ⇒ ภาพนับเป็น 0 แล้วแถวถูกบีบ
> ⚠️ และต้องมี **`min-height: 0` ทุกชั้นของ flex ที่ห่อกริด** ไม่งั้นกริดไม่ scroll
> ⚠️ **วิธีจับบั๊กแบบนี้: เทียบ `scrollHeight` ของกริดกับ (จำนวนแถว × ความสูงการ์ด)**
> ถ้าไม่ตรง = ซ้อนทับ. อ่านค่า `getBoundingClientRect` ของการ์ดอย่างเดียว **ไม่พอ**
> เพราะมันรายงานความสูงที่ถูกต้องทั้งที่ภาพบนจอผิด (เสียเวลาไล่นานเพราะข้อนี้)

**📉 "อยากได้ภาพน้อยลง" — ตัวช่วยในแผง:** ช่อง **"เก็บ 1 ใน N เฟรม"** + ปุ่มสำเร็จรูป
(ทุกเฟรม / 1 ใน 3 / 1 ใน 7 / 1 ใน 14) + **ตัวประมาณสด** ที่คำนวณจาก **fps ที่วัดได้จริง
ของกล้อง** (poll `/api/camera/hik/status` ทุก 4 วิ เพราะ fps ขยับตาม ROI/exposure)
บอกว่า *จะได้กี่ภาพ · กี่ MB · ห่างกันกี่ ms* **ก่อนกดถ่าย** และเตือนเมื่อเกิน 400 MB.
> 💡 **`every_n` ไม่ทำให้ภาพเบลอขึ้นหรือคมขึ้นเลย** — ความเบลอของแต่ละภาพขึ้นกับ
> exposure อย่างเดียว. มันแค่ลดจำนวนภาพที่ต้องเขียนลงดิสก์ ⇒ เป็นทางแก้ตรงจุดของ
> อาการ "ทิ้งเฟรมเพราะดิสก์ตามไม่ทัน"

**วิธีใช้ให้ได้คำตอบ (ต้องบอกผู้ใช้):** ที่ exposure 2635 µs ปัจจุบัน **จะไม่มีภาพคม
สักใบ** ไม่ว่าจะถ่ายกี่ร้อยเฟรม (เบลอ ~20 px) ⇒ **ถ่าย 3-4 รอบที่ exposure ต่างกัน**
(2635 / 800 / 300 / 150 µs) แล้วเทียบกัน จะเห็นสองเส้นสวนกัน: คมขึ้นเรื่อย ๆ พร้อมกับ
มืดลงเรื่อย ๆ — **จุดที่ทั้งคมพอและสว่างพอคือคำตอบว่าต้องเพิ่มไฟเท่าไร**

**📊 ผลรันจริงครั้งแรกบนสถานี (20 ส.ค. 2026) — 1224x1024, exposure 2003 µs, gain 7.7 dB:**
เก็บได้ **38 ภาพ แต่ทิ้ง 127 เฟรม (77%)** เพราะดิสก์เขียนไม่ทัน · พบวัตถุเคลื่อนไหว 6/38
⇒ ยังวัดความเร็วไม่ได้. **สองข้อที่ต้องแก้ก่อนรอบหน้า: ตั้ง "เก็บ 1 ใน 7 เฟรม"
(ลดภาระดิสก์ ~7 เท่า) และโบกวัตถุให้ผ่านกลางเฟรมช้าลง/นานขึ้น**

**⏳ ยังไม่ได้ทำ (ตั้งใจ):** `HIK_BURST_MM_PER_PX` ยังเป็น `None` (ต้องถ่ายไม้บรรทัด
ที่ระนาบชิ้นงานแล้ววัดเอง) ⇒ ตอนนี้รายงานความเร็ว/ระยะเบลอเป็น **พิกเซล** อย่างเดียว
ไม่เดาเป็น mm · ยังไม่มี `blur_tolerance.py` (วัดว่า **bestX ทนเบลอได้กี่พิกเซล** —
ตอนนี้ `hik_offline_eval.py --blur-limit-mm 0.2` ยังเป็นค่าที่ **เดาไว้ ไม่เคยวัด**)

### 📐 `blur_tolerance.py` — วัดว่า **โมเดลทนเบลอได้กี่พิกเซล** (21 ส.ค. 2026)

**ปัญหาที่แก้:** สเปกของทั้งระบบไล่มาจากตัวเลขเดียว —
`เบลอกี่ px ที่โมเดลยังเห็น → exposure สูงสุด → ต้องเพิ่มไฟกี่เท่า → ซื้อไฟตัวไหน`
แต่ตัวเลขต้นทางนั้นเป็น **ค่าที่เดาไว้** (`--blur-limit-mm 0.2`) ไม่เคยวัดเลย.
**เดาผิด 3 เท่า = งบไฟผิด 3 เท่า**

```
py -3.9 blur_tolerance.py --selftest                    # ตรวจตัวเครื่องมือเอง (18 ข้อ)
py -3.9 blur_tolerance.py --images out_ng --model bestX.pt ^
        --speed-mm-s 638 --mm-per-px 0.082 --exposure-us 2003 --mean-brightness 20
```

**วิธีวัด:** ใส่ **เบลอทิศทางเดียว** (เคอร์เนล 1×L ตามแนวที่ชิ้นงานวิ่ง — ไม่ใช่ Gaussian
ซึ่งเป็นอาการ "โฟกัสหลุด" คนละเรื่อง) ไล่ L แล้วดูว่าผลตรวจเสื่อมตรงไหน →
`exposure สูงสุด = L × mm_per_px ÷ ความเร็ว` → `ต้องเพิ่มไฟกี่เท่า`

**สิ่งที่ฝังเป็นพฤติกรรม (แต่ละข้อคือกับดักที่ทำให้เครื่องมือโกหก):**
1. **ภาพที่โมเดลหาไม่เจอตั้งแต่ L=0 ต้องถูกตัดออก** — ถ้านับรวมจะได้ "ยังเจอ 100%"
   ทุกระดับความเบลอ = เครื่องมือบอกว่าโมเดลทนเบลอได้ไม่จำกัด ทั้งที่ไม่เคยเห็นอะไรเลย
   (`images_used` vs `images_total` รายงานแยกให้เห็น)
2. **วัดเทียบกับผลของตัวโมเดลเองที่ L=0 ไม่ใช่เทียบเฉลยที่คนติดป้าย** — สิ่งที่วัดคือ
   *ความทนทานต่อความเบลอ* ไม่ใช่ *ความแม่น* (เขียนไว้ในหัวรายงานเพื่อไม่ให้อ่านผิด)
3. **หาเพดานแบบ "ต่อเนื่อง"** — ถ้าดูเฉพาะจุดสุดท้ายที่ผ่าน จุดที่ผ่านแบบฟลุ๊คหลังจาก
   พังไปแล้วจะทำให้ได้คำตอบที่ดีเกินจริง (มีเทสต์ล็อกเคส `[1,1,.98,.50,1.00]` → ต้องได้ 2)
4. **เกณฑ์จับคู่กรอบต้องหลวมกว่าปกติ** (`MATCH_IOU=0.30`) เพราะกรอบของภาพที่เบลอจะ
   ขยับ/ขยายเป็นธรรมชาติ — ใช้ 0.5 แบบมาตรฐานจะตัดคู่ที่ถูกต้องทิ้ง
5. **กรอบหนึ่งจับคู่ได้ครั้งเดียว** ไม่งั้นกรอบเดียวจะไป "ตอบ" รอยบุบสองจุดจนดูเหมือนเจอครบ
6. `TARGET_MEAN = 80.0` **ต้องตรงกับ `hik_burst.py`** (มีเทสต์เทียบสองไฟล์)

**`--selftest` ตรวจตัวเครื่องมือเอง 18 ข้อโดยไม่ต้องมีโมเดล/ภาพ** — และมันจับข้อผิดพลาด
ของผู้เขียนได้จริงตั้งแต่รันครั้งแรก (ใช้ภาพสี่เหลี่ยมทึบเป็นเคสทดสอบ ซึ่งขอบคมเกินกว่า
ที่การเบลอจะลบทิ้งได้ ⇒ เทสต์ไม่มีความหมาย. เปลี่ยนเป็น **ลายคาบ 4 px** ซึ่งการเฉลี่ย
ยาว 4 px จะกลืนหายพอดีในทางคณิตศาสตร์ = รู้คำตอบล่วงหน้า แล้ววัดได้ 3 px ✅)

**📊 ผลวัดจริงบนสถานี (21 ส.ค. 2026) — `bestX.pt` @ imgsz 1280, ภาพ NG 25/31 ใบ:**

| เบลอ (px) | 0 | 1 | 2 | 3 | 4 | 6 | 8 | 12 | 16 | 20 |
|---|---|---|---|---|---|---|---|---|---|---|
| ยังตรวจเจอ | 100% | 100% | 100% | 100% | 100% | 100% | **100%** | 83% | 49% | 32% |
| กรอบตรง (IoU) | 0.97 | 0.97 | 0.93 | 0.90 | 0.87 | 0.78 | 0.72 | 0.59 | 0.37 | 0.28 |

⇒ **โมเดลทนเบลอได้ ≤ 8 พิกเซล** — มากกว่าที่เดาไว้ (1-3 px) **4-8 เท่า**
⇒ ที่ 638 mm/s + 0.082 mm/px: exposure สูงสุด **1,029 µs** ⇒ **ต้องเพิ่มไฟแค่ ~2 เท่า
ไม่ใช่ 21 เท่า** (ตัวเลขนี้เปลี่ยนสเปกไฟทั้งโครงการ)

> ⚠️ **แนะนำให้ออกแบบที่ 4 px (ครึ่งหนึ่งของที่วัดได้)** ไม่ใช่ที่ขอบพอดี เพราะ IoU
> ตกจาก 0.97 → 0.72 ที่ 8 px (กรอบเริ่มเพี้ยน) และเบลอสังเคราะห์ไม่มี noise ของจริง
> ⚠️ **ยังไม่ได้วัด NG ปลอม** — เครื่องมือวัดแค่ "ยังเจอไหม" ไม่ได้วัด "เจอของที่ไม่มี"
> การเบลอสร้างเงายืดที่อาจดูเหมือนรอยบุบ ⇒ recall 100% ที่ 8 px **ยังไม่แปลว่าใช้งานได้**
> (ต้องไล่เบลอเดียวกันบนภาพ OK แล้วนับ NG ปลอม — ยังไม่ได้ทำ)
> ⚠️ **6/31 ภาพ โมเดลหาไม่เจอตั้งแต่ไม่เบลอ** ทั้งที่ถูกบันทึกเป็น NG โดยระบบเอง
> (ตอน live ที่ imgsz 480) — น่าสงสัยว่า imgsz 480 กับ 1280 ให้ผลคนละอย่าง ยังไม่ได้ไล่
> ✅ ภาพจาก DB เป็น **1224×1024 = ROI ครึ่งของ Hikrobot** ⇒ เป็นโดเมนของกล้องใหม่แล้ว
> ไม่ใช่ webcam ตัวเก่าอย่างที่เคยกังวล

**⚠️ ข้อจำกัด:** เบลอสังเคราะห์ ≠ เบลอจริง 100% (ของจริงมี noise จาก exposure สั้นปนมา)
⇒ ใช้ **ตั้งงบประมาณ** แล้ววัดซ้ำด้วยภาพจริงเมื่อมีไฟแล้ว · ภาพ NG ที่มีอยู่มาจาก
**กล้อง webcam ตัวเก่า** ⇒ เกณฑ์ที่ได้เป็นของโดเมนเก่า

### 🔍 วินิจฉัย "เฟรมหายไปตรงไหน" — เพิ่มในโหมดถ่ายรัว (21 ส.ค. 2026)

**เจอจากการรันจริง:** ถ่ายรัว 10 วินาทีได้ **38 ภาพ ทิ้ง 127 = 165 เฟรม = 16.5 fps**
ทั้งที่วัดได้ 69 fps เมื่อ 19 ส.ค. ⇒ **กระป๋องกว่าครึ่งไม่มีภาพที่เห็นเต็มใบเลย**
(ที่ 450 CPM: 69 fps → 1.88 เฟรมเต็มใบ/ใบ · 16.5 fps → **0.45**)

เฟรมหายได้ **2 จุด และวิธีแก้คนละเรื่องกันโดยสิ้นเชิง**:

| จุด | ชื่อบนหน้าจอ | แปลว่า | วิธีแก้ |
|---|---|---|---|
| ① | **"เฟรมหาย"** (`stats()["dropped"]` — เลขเฟรมของกล้องกระโดด) | กล้องส่งแล้วแต่**มาไม่ถึง** | packet size + Jumbo Frame |
| ② | **"ทิ้ง"** (`dataset.dropped` — คิวเต็ม) | มาถึงแล้วแต่**เขียนไม่ทัน** | every_n · ROI · หยุดโมเดล |

`meta.json` ของทุกชุดจึงเก็บเพิ่ม: `diag_start`/`diag_end` (ตัวนับกล้อง + สถิติเครือข่าย
ตอนเริ่มและตอนจบ) · `stage_ms` (encode/เขียน) · `fps_series` · **`framerate_enable`/
`framerate`/`packet_size`/`pixel_format`/`trigger_mode`** แล้ว `hik_burst.diagnose()`
สรุปเป็นประโยคพร้อมวิธีแก้ แสดงในแกลเลอรี

**🐛 ข้อบกพร่องในตรรกะของตัวเองที่เจอตอนทดสอบ (แก้แล้ว):** เดิมเขียนเป็น `if/elif`
⇒ รายงานแค่สาเหตุแรก. เคสจริงมี **สองปัญหาพร้อมกัน** (กล้องส่ง 16.5 fps **และ**
ดิสก์ทิ้ง 127 เฟรม) ถ้าโชว์แค่ "ดิสก์" ผู้ใช้จะไปแก้ดิสก์แล้วยังติดที่ 16.5 fps อยู่ดี.
ตอนนี้คืน `issues[]` **ทุกสาเหตุ** เรียงตาม **ลำดับที่ควรลงมือแก้** — เฟรมที่
"ไม่เคยมี" ก่อนเฟรมที่ "มีแล้วเขียนไม่ทัน" (เพราะพอกล้องเร็วขึ้น ดิสก์จะยิ่งตามไม่ทัน)

> 💡 **เพดานสาย GigE** คำนวณจาก `125 MB/s × ประสิทธิภาพ ÷ ขนาดเฟรม` โดยประสิทธิภาพ
> ขึ้นกับขนาดแพ็กเก็ต (Jumbo 8164 = 0.90 · 1500 = 0.70) ⇒ ที่ ROI ครึ่งได้ ~70-90 fps
> ⇒ ถ้าได้ 16.5 fps โดยไม่มีเฟรมหายและไม่มีการทิ้ง = **ลายเซ็นของ packet size ค่าโรงงาน**
> ⚠️ **ไม่มี `size` ใน meta = คำนวณเพดานไม่ได้ ⇒ ไม่กล่าวหาว่ากล้องช้า** (ไม่เดา)

### 📊 ผลรันจริงบนสถานี 21 ส.ค. 2026 — และ 3 จุดที่ระบบ "รายงานผิดแบบมั่นใจ"

**สิ่งที่แก้ได้แล้วจากรอบก่อน:** packet size 1500 → **8164 + Jumbo เปิด** · **ทิ้งเพราะดิสก์
127 → 0** · เก็บได้ 64/64 ภาพ

**สิ่งที่ยังเหลือ:** กล้องส่งมาแค่ **9.4-9.5 fps** ทั้งที่ 19 ส.ค. วัดได้ 69 fps ที่ ROI เดียวกัน
ต่างกันตรงที่ 19 ส.ค. วัดด้วย `diagnose_hikrobot.py` (ไม่มีอะไรอื่นรัน) แต่รอบนี้มี
**live inference (50 ms/เฟรม) + MJPEG ส่งภาพขึ้นหน้าเว็บ + หน้าเว็บ poll ทุกวินาที**
⇒ คอขวดอยู่ที่ **เครื่อง ไม่ใช่สาย** (encode ใช้ 24 ms/เฟรม = ซีพียูตึง)

**🐛 3 จุดที่ระบบรายงานผิดแบบมั่นใจ (แก้แล้วทั้งหมด — ทุกข้อคือกฎเหล็กข้อ 2):**

| อาการบนสถานี | ทำไมถึงผิด | แก้เป็น |
|---|---|---|
| ✅ **"คมพอ — กล้องหยุดการเคลื่อนที่ได้"** ทั้งที่กระป๋องวางนิ่ง | วัดได้ 0.53 px/เฟรม = **ต่ำกว่าพื้นสัญญาณรบกวน** (ด่านตรวจซ้ำยอมคลาด ±6 px) ⇒ เราไม่รู้ ไม่ใช่ "ช้าและคม" | ด่าน `MIN_MEANINGFUL_SHIFT_PX = 2.0` px/เฟรม → ขึ้นคำเตือนว่า **"ยังไม่ได้ตอบคำถามเรื่องความเบลอ"** |
| **exposure ที่เบลอ ≤1px = 217,391 µs** (0.22 วินาที!) | หารด้วยความเร็วที่เป็นสัญญาณรบกวน | ไม่คำนวณ exposure/ตัวคูณไฟเลยเมื่อ `motion != "ok"` |
| ป้าย **"เบลอแนวตั้ง"** บนภาพกระป๋องที่วางนิ่ง | ความต่างเล็ก ๆ ของอัตราส่วนแกนบนภาพนิ่งกลายเป็นป้ายที่ไม่มีมูล | ไม่ติดป้ายทิศทางเลยเมื่อทั้งชุดไม่มีการเคลื่อนที่ที่วัดได้ |
| คำแนะนำ **"เช็ค packet size"** ทั้งที่ packet = 8164 + Jumbo เปิดแล้ว | คำแนะนำไม่ได้ดูหลักฐานที่ตัวเองเก็บมา = ส่งผู้ใช้ไปแก้ของที่ไม่ได้พัง | คำแนะนำเปลี่ยนตามหลักฐาน → ชี้ไปที่ **CPU ของเครื่อง** และแนะให้ติ๊ก "หยุดโมเดล" |

> 💡 **บทเรียนที่ใช้ได้กับทุกตัววัดในโปรเจกต์นี้:** "วัดได้ค่าน้อย" กับ "วัดไม่ได้"
> ต้องแยกกันเสมอ. ตัวเลขที่ได้จากสัญญาณรบกวนแล้วเอาไปหารต่อ จะกลายเป็นคำตอบ
> ที่ดูน่าเชื่อถือมาก (217,391 µs) แต่ผิดสนิท — และผู้ใช้ไม่มีทางรู้

### 📊 ผลรันบนสถานี 24 ส.ค. 2026 — **fps แก้ได้แล้ว** + จุดที่ 5 ที่รายงานผิดแบบมั่นใจ

**ชุด `20260824_153002`** — 101 ภาพ · 39.3 MB · exposure **2882 µs** (เดิม 5055 — ผู้ใช้
**เปิดรูรับแสงที่เลนส์สุด** ⇒ ได้แสงเพิ่มจึงลด exposure ลงได้) · gain 7.7 dB · ROI 1224×1024

| ตัวชี้วัด | 21 ส.ค. | **24 ส.ค.** |
|---|---|---|
| กล้องส่งมาจริง | 9.4-9.5 fps | **68.3 fps** ✅ (= เพดานที่วัดได้ 69) |
| เพดานสาย GigE ที่ ROI นี้ | — | 89.8 fps (ใช้ไป 76%) |
| ทิ้งเพราะดิสก์ | 0 | **0** |
| เฟรมหายระหว่างทาง | 0 | **2 จาก 707 = 0.3%** |
| encode / เขียน | 24.0 / — ms | **45.1 / 3.0 ms** |

⇒ **คอขวดของ fps (ที่บันทึกไว้ 21 ส.ค. ว่าเป็น CPU ของเครื่อง) ถูกแก้แล้ว** — เหลือแค่
เฟรมหายเศษเสี้ยวเปอร์เซ็นต์ที่เกิดตอนลิงก์วิ่งที่ 76% ของเพดาน

**🐛 จุดที่ 5 ที่รายงานผิดแบบมั่นใจ (แก้แล้ว — ตระกูลเดียวกับ 4 จุดของ 21 ส.ค.):**
กล่องวินิจฉัยขึ้นว่า *"ตั้ง packet size ให้ใหญ่ขึ้นและเปิด Jumbo Frame บน NIC"*
**ทั้งที่บรรทัดหลักฐานใต้ข้อความเดียวกันบอกว่า `Jumbo Frame เปิด` และ `packet size 8164`**
— รอบ 21 ส.ค. แก้ให้สาขา `camera_rate` ดูหลักฐานแล้ว **แต่สาขา `transport` ถูกมองข้าม**

| | เดิม | ตอนนี้ |
|---|---|---|
| Jumbo **ปิด** | "ตั้ง packet size + เปิด Jumbo" | เหมือนเดิม (ถูกอยู่แล้ว) |
| Jumbo **เปิด** | "ตั้ง packet size + เปิด Jumbo" ❌ | อ้าง packet 8164 เป็นหลักฐาน → ชี้ไป **ฝั่งรับ**: Receive Buffers ของ NIC · packet delay · ภาระ CPU |
| หาย **< 1%** | ไม่บอกสัดส่วนเลย | บอก **"หายเพียง 0.3% ⇒ ยังไม่ใช่ปัญหาที่ต้องไล่ตอนนี้"** |

`text` รายงาน **เปอร์เซ็นต์** ด้วยเสมอ (2 เฟรมโดด ๆ แยกไม่ออกว่าร้ายแรงแค่ไหน).
เทสต์เพิ่ม 4 ตัวใน `test_hik_burst.py` (90 ตัว) ล็อกทั้งสองด้าน — รวมเทสต์ที่ยืนยันว่า
**หายเยอะต้องไม่ขึ้นข้อความ "ยังไม่ใช่ปัญหา"**
> ⚠️ เทสต์เดิม `test_diagnose_blames_the_network_when_frame_numbers_jump` **ล็อกบั๊กนี้ไว้เอง**
> (ตั้ง `packet_size=8164` แล้ว assert ว่า `"packet size" in fix`) — เตือนว่าเทสต์ที่เขียน
> จากพฤติกรรมปัจจุบันโดยไม่ถามว่า "คำแนะนำนี้ถูกไหม" จะกลายเป็นเกราะกันการแก้บั๊ก

**⏳ ยังไม่ได้ตอบ:** ความเร็ววัตถุที่วัดได้เป็น **0.46 px/เฟรม** (ต่ำกว่าเกณฑ์ 2 px)
⇒ ระบบปฏิเสธการสรุปเรื่องเบลอถูกต้องแล้ว. **แต่มีเฟรมเดี่ยวที่วัดได้จริง: 1,635 px/s →
เบลอ 4.71 px ที่ 2882 µs** (สอดคล้องกับสูตรเป๊ะ) ⇒ ที่ความเร็วไลน์ 7,800 px/s
exposure เดิมจะให้เบลอ **~22 px** และ **exposure ที่ให้ 4 px = 513 µs** (ยืนยันตัวเลขเดิม)

### 🐢 "ทำไมกล้องอุตสาหกรรมกระตุกกว่า USB มาก" — 5 จุด (24 ส.ค. 2026)

ผู้ใช้รายงานว่าภาพสด **กระตุกกว่าโหมด USB ชัดเจน**. ไล่ diff ทั้ง branch เทียบ `main`
พบว่า **ท่อภาพฝั่งเซิร์ฟเวอร์ไม่ถูกแตะเลย** (`capture_loop` และ `generate_frames`
เทียบไบต์ต่อไบต์แล้ว **IDENTICAL** · `inference_loop` เพิ่มสาขาเดียวที่ short-circuit)
⇒ ต้นเหตุอยู่ที่ **ขนาด/อัตราของข้อมูล และการแย่งทรัพยากร** ไม่ใช่ตรรกะที่เปลี่ยน

| # | ต้นเหตุ | หลักฐาน | แก้เป็น |
|---|---|---|---|
| **A** | **`HIK_LIVE_MAX_WIDTH = 1280` ไม่เคยทำงาน** ที่ ROI ครึ่ง 1224×1024 (`_post()` ย่อเมื่อ `w > maxw` — 1224 ไม่เกิน 1280) | ข้อมูลเข้าท่อ **256.8 vs 27.6 MB/s = 9.3 เท่าของ USB** บนเครื่อง RAM single-channel + iGPU ไม่มี VRAM | **960** (= 2× ของ imgsz 480 ที่โมเดลใช้จริง) |
| **B** | `LIVE_SMOOTH_VIDEO=False` ⇒ **จอรีเฟรชตามอัตราการตรวจ** — USB ไม่รู้สึกเพราะตรวจ ~20/วิ > เพดานจอ 15 แต่ Hik ตรวจช้ากว่า 15 | docstring ของ `generate_frames` เขียนไว้เอง: *"stuttery picture on heavy models"* | `HIK_LIVE_SMOOTH_VIDEO` **เฉพาะแหล่ง hik** ผ่าน `_live_smooth()` |
| **C** | แถบตัวเลข perf **ถูกซ่อนบนแท็บ hik** (`currentSource === 'usb'` เท่านั้น) | ข้อมูลถูกเก็บและส่งครบใน `/api/detection/status` → `perf` อยู่แล้ว | แสดงบนแท็บ hik ด้วย + `.stacked` กันทับ `#hikStats` |
| **E** | poll **2 ตัว** (1 วิ + 4 วิ) ยิง `/api/camera/hik/status` ซึ่งจบที่ `net_stats()` ที่ **จับ lock ตัวเดียวกับ `_grab_once()`** ถือไว้ตลอดช่วงจับเฟรม+แปลงสี | ตัว 4 วิของถ่ายรัววิ่ง **ตลอดเวลาที่กล้องเปิด** แม้ไม่ได้ดูแผงนั้น | แคช `net_stats` 2 วิ + `HikUI.getStats()` ให้ใช้ค่าร่วมกัน |
| **F** | **`detector` ตัวเดียวถูกเรียกจากหลายเธรดพร้อมกัน** (live · ปุ่มถ่ายรูป · STREAM · งานตรวจของถ่ายรัว 12 ใบรวด) | ultralytics ไม่รับประกัน thread-safety และบน iGPU ที่แชร์ RAM การยิงพร้อมกันช้ากว่าเข้าคิว | `self._infer_lock` (RLock) รอบ **การเรียกโมเดลเท่านั้น** |

**📊 วัดจริงบน container (โมเดลจำลอง 200 ms/เฟรม = เงื่อนไขของสถานี):**

| | inference | **disp_fps** |
|---|---|---|
| smooth **ปิด** (เดิม) | 201 ms (5.0/วิ) | **5.0** ← จอกระตุกตามการตรวจ |
| smooth **เปิด** (แก้ B) | 202 ms (5.0/วิ) | **13.8** ← ลื่นตามเพดานจอ |

⇒ **จออัปเดตดีขึ้น 2.8 เท่าโดยที่ความเร็วการตรวจเท่าเดิมเป๊ะ**

**ขับด้วย Chromium จริงบน SDK ปลอมแล้ว:** แถบสถิติสองแถบ **ไม่ทับกัน** (hik 138-161 ·
live 166-189) · `liveStats` ขึ้นบนแท็บ hik จริง (`cam 78fps / inf 23ms / enc 4.5ms /
disp 14fps`) · MJPEG เสิร์ฟที่ **960×803** (ย่อทำงานแล้ว) · ตัวประมาณถ่ายรัวยังคำนวณได้
จากค่าที่แชร์มา · คำขอ `/status` ลดเหลือ **1.00/วิ** (จาก 1.25) · **ไม่มี JS error**
(404 เดียวคือ `/favicon.ico` ตามปกติ)

**🐛 บั๊กที่เกือบสร้างเอง (จับได้เพราะเปิดเบราว์เซอร์ดู):** `#liveStats` กับ `#hikStats`
เป็น `position:absolute; left:10px; top:10px` **เหมือนกันเป๊ะ** ⇒ พอเปิดข้อ C ทั้งสอง
แถบจะทับกันจนอ่านไม่ออกทั้งคู่. เพิ่ม `#liveStats.stacked { top: 38px }`
> ⚠️ ย้ำบทเรียนเดิมของโปรเจกต์: **เทสต์ยูนิตจับ layout ไม่ได้เลย — ต้องวัดบนเบราว์เซอร์จริง**

**⚠️ 3 เทสต์เดิมที่ล็อกพฤติกรรมเก่าไว้ (แก้แล้ว):** `test_downscale_keeps_aspect_ratio`,
`test_stats_expose_the_numbers_needed_to_trust_the_feed` (ทั้งคู่ hard-code 1280) และ
`test_set_locked_param_restarts_stream_and_keeps_working` ซึ่ง **assert ตรง ๆ ว่า
"1224 เล็กกว่าเพดานจึงไม่ถูกย่อ"** = เขียนบั๊กข้อ A ไว้เป็นข้อกำหนด. ตอนนี้อ้างอิง
`config.HIK_LIVE_MAX_WIDTH` แทนตัวเลขตรง ๆ + มีเทสต์ยืนยันคุณสมบัติ
(`< 1224` และ `>= 2 × YOLO_IMGSZ`)
> 💡 **รูปแบบเดิมซ้ำเป็นครั้งที่ 2 ในสัปดาห์เดียว** (ครั้งแรกคือเทสต์ packet size ของ
> `diagnose()`): เทสต์ที่เขียนจากพฤติกรรมปัจจุบันโดยไม่ถามว่า "พฤติกรรมนี้ถูกไหม"
> จะกลายเป็นเกราะกันการแก้บั๊ก

**✅ สิ่งที่ไม่ถูกแตะ:** USB / RTSP / STREAM / Snapshot / Label / Artwork ·
ความละเอียดของ **"ถ่าย 1 เฟรม" · ชุดข้อมูล · ถ่ายรัว** (สามทางนั้น `view.copy()`
**ก่อน** บรรทัดที่ย่อใน `_post()`) · **การนับ/DB/verdict** (ใช้เฟรมที่โมเดลตรวจจริง
เสมอ ไม่ใช่ภาพที่เห็นบนจอ ⇒ ข้อ B เป็นเรื่องการแสดงผลล้วน)

**⏳ ข้อ D ที่เสนอแต่ผู้ใช้ยังไม่สั่งทำ:** `HIK_BURST_AUTODETECT_TOP = 0` — หลังถ่ายรัว
จบ หน้าเว็บสั่ง `compute_metrics` (101 ภาพ) ต่อด้วย `run_detect` 12 ใบที่ imgsz 1280
(370-468 ms/ใบ) **อัตโนมัติ** ⇒ แย่ง iGPU กับการตรวจสด ~8-14 วินาทีทุกครั้ง
(ข้อ F ทำให้เข้าคิวแทนแย่งกันแล้ว แต่ภาระรวมยังเท่าเดิม)

### 🎯 กรอบตามภาพไม่ทัน — **ผมทำพลาดเองตอนเลือก default ของข้อ B** (24 ส.ค. 2026)

หลังแก้ 5 จุดข้างบน ภาพลื่นขึ้นจริง **แต่ผู้ใช้เจอทันทีว่า "กรอบ Dent ตามภาพไม่ทัน"**
ซึ่งเป็นปัญหาที่โปรเจกต์นี้ **เคยตัดสินใจแก้ไปแล้ว** ด้วยการตั้ง `LIVE_SMOOTH_VIDEO = False`

**นี่ไม่ใช่บั๊กที่กลับมาเอง — เป็นการที่ผมตั้ง `HIK_LIVE_SMOOTH_VIDEO = True` เป็น default**
ทั้งที่ comment ของตัวเองเขียนไว้แล้วว่า *"กรอบตามช้าเล็กน้อยตอนวัตถุขยับ"* ⇒ ละเมิด
**กฎเหล็กข้อ 1** (flag ใหม่ default ต้อง = พฤติกรรมเดิม) และ **ข้อ 2** (กรอบที่ชี้ผิด
ตำแหน่ง = ผลที่ผิดแบบมั่นใจ — คนจะเชื่อสิ่งที่ระบบชี้แล้วมองข้ามของจริง)

| | ภาพ | **กรอบ** |
|---|---|---|
| `False` = **LOCKED (default ที่ถูกต้อง)** | อัปเดตตามอัตราการตรวจ | **ล็อกเป๊ะกับกระป๋อง** |
| `True` = SMOOTH | ลื่นตามกล้อง | **ตามไม่ทันตอนวัตถุขยับ** |

**แก้เป็น:** default กลับเป็น `False` + **ช่องติ๊กในแผงให้สลับสดโดยไม่ต้องรีสตาร์ต**
(`POST /api/camera/hik/live_smooth` — แพทเทิร์นเดียวกับ `/api/frame_capture`)
⇒ ผู้ใช้เลือกเองได้ตามสถานการณ์: **จัดมุมกล้อง/ดูภาพ** เปิดได้ · **ดูผลตรวจ** ต้องปิด

วัดจริงบน container (ผู้ชม MJPEG คนเดียว · โมเดลจำลอง 200 ms/เฟรม):
`disp_fps` **4.9 (ปิด) → 13.1 (เปิด)` โดย inference เท่าเดิม (202 vs 203 ms)

> ⚠️ **ถ้าจอกระตุกให้แก้ที่ต้นเหตุ ไม่ใช่ปิดตากรอบ** — โหมด LOCKED จะลื่นเองเมื่อ
> `inf_fps ≥ 15` (เพดานจอ). อ่านค่าจากแถบ perf ที่เพิ่งเปิดให้เห็นในข้อ C:
> ถ้า `inf` ยังสูงอยู่ ให้ลด `HIK_LIVE_MAX_WIDTH` · ลด ROI · ลดงานเบื้องหลังที่แย่ง iGPU
> ⚠️ **บทเรียน:** อาการ "ปัญหาเก่ากลับมา" ที่ผู้ใช้เจอ มักไม่ใช่โค้ดถอยหลัง แต่เป็น
> **การเลือกด้านหนึ่งของ trade-off ที่เคยตัดสินใจไปแล้ว โดยไม่ได้ไปอ่านว่าทำไมถึงเลือกด้านนั้น**
> — comment ของ `LIVE_SMOOTH_VIDEO` เขียนเหตุผลไว้ครบตั้งแต่แรก แต่ผมไม่ได้ใช้มันตัดสินใจ

### 📸 "ถ่าย 1 เฟรม" ช้าเกินกว่าจะเห็นรูป — แยกเป็น 2 เฟส (24 ส.ค. 2026)

ผู้ใช้ชี้ว่า *"ถ้ามีเซ็นเซอร์จริง ตอน trigger มันต้องถ่ายได้เร็วทันที"* — **ถูกต้อง และ
การถ่ายก็เร็วอยู่แล้ว** (`snap_full` = รอเฟรมถัดไป ~15 ms). ที่ช้าคือ **การตรวจ + ส่งกลับ**
ซึ่งเดิมอยู่ในคำขอเดียวกัน ⇒ ผู้ใช้ไม่เห็นรูปเลยจนกว่าโมเดลจะเสร็จ

**วัดต้นทุนทุกขั้นบนภาพ 2448×2048 จริง** (ไม่ใช่เดา):

| ขั้น | เวลา |
|---|---|
| จับภาพ (`snap_full`) | ~15-56 ms |
| ย่อลงจอ 2448→1280 | 7.8 ms |
| encode JPEG + base64 + JSON | 8.4 ms |
| **inference imgsz 1280** | **~420 ms** ← 96% ของเวลาทั้งหมด |

⇒ แยกเป็น **2 เฟส**: `POST /shot {detect:false}` คืนรูปทันที → `POST /shot/inspect` ค่อยตรวจ

**วัดจริงบน container (โมเดลจำลอง 420 ms · ขับด้วย Chromium):**

| | เดิม | **ใหม่** |
|---|---|---|
| กว่าจะเห็นรูปบนจอ | 1,683 ms | **140 ms** ⬆️ **12 เท่า** |
| ผลตรวจขึ้นครบ | 1,683 ms | 1,322 ms (เท่าเดิมโดยหลักการ) |

**🔍 การแยก `wait_ms` ออกจาก `infer_ms` เผยของที่มองไม่เห็นมาก่อน:**
> `โมเดล 424 ms · **รอคิวโมเดล 706 ms** · แสดงผล 28.9 ms`

การรอคิวเกิดจาก `_infer_lock` (ที่เพิ่งเพิ่มในข้อ F) — ช็อตต้องรอ `inference_loop`
ปล่อยโมเดล. **นี่คือราคาของการเข้าคิวแทนการแย่งกัน** และตอนนี้ **มองเห็นได้บนจอ**
แทนที่จะซ่อนอยู่ในตัวเลขเดียว ⇒ ผู้ใช้รู้ว่าต้องแก้ที่ "ลดภาระการตรวจสด" ไม่ใช่ "โมเดลหนัก"

**กติกาที่ยึด:** เฟส ① **ไม่ใส่ `verdict` มาเลย** และหน้าเว็บขึ้น **"⏳ กำลังตรวจ…"** —
ไม่แสดงอะไรที่ดูเหมือนผลตรวจตอนที่ยังไม่รู้ (กฎเหล็กข้อ 2) · `shot_id` กันการตรวจ
เฟรมผิดใบเมื่อถ่ายทับระหว่างรอ (คืน 409 ไม่ใช่ผลของเฟรมเก่าเงียบ ๆ) ·
`{detect:true}` (หรือไม่ส่ง) = พฤติกรรมเดิมทุกประการ

> ⚠️ **สำหรับระบบ triggered จริงที่ 450 CPM (คาบ 133 ms):** inference imgsz 1280
> = 420 ms ⇒ **ตรวจไม่ทันแน่นอน** ต้องเป็น `trigger → จับภาพ → เข้าคิว → worker ตรวจทีหลัง`
> หรือตรวจที่ imgsz 480 (~50 ms) ซึ่งพอดีกับ 7.5 ใบ/วิ

**🐛 บั๊กที่เกือบสร้างเองในเทสต์:** ตั้งชื่อ helper ใหม่ว่า `_start_hik` ซึ่ง**ชนกับตัวที่มี
อยู่แล้วในไฟล์เดียวกัน** (ตัวเดิมคืน response · ตัวใหม่คืน None) ⇒ ตัวใหม่บังตัวเดิม
ทั้งไฟล์ ทำให้เทสต์ burst **4 ตัวที่ไม่เกี่ยวข้องกันเลยพัง** (`NoneType has no
attribute 'status_code'`). แก้เป็น fixture `shotclient` ที่มีชื่อเฉพาะ
> 💡 และเทสต์ "เฟส ① ต้องไม่เรียกโมเดล" **นับ `detector.detect` ตรง ๆ ไม่ได้** เพราะ
> `inference_loop` เรียกมันอยู่ตลอดในเบื้องหลัง ⇒ ดักที่ `_hik_shot_inspect` แทน

### 🎯 เลือก "ภาพที่สมบูรณ์ที่สุด" ไม่ใช่ "ภาพที่คมที่สุด" (24 ส.ค. 2026)

**โจทย์ของผู้ใช้:** *"เก็บรูปรัว ๆ อย่างเดียวไม่ส่งตรวจ เพื่อความเร็วสูงสุด จัดเก็บรูปที่
สมบูรณ์ที่สุดของสิ่งของที่เคลื่อนที่ผ่านเฟรม แล้วค่อยตรวจครั้งเดียวตอนท้าย"*

ไล่โค้ดแล้วพบว่า **โครงหลักมีอยู่แล้ว** (ถ่ายที่อัตรากล้อง → วัด → ตรวจตอนท้าย ·
`hik_camera.py`/`hik_burst.py` **ไม่ import ultralytics เลย**) แต่ขาด 2 อย่าง:
`top_sharp_files` เรียงตาม **ความคมล้วน** ⇒ ภาพที่กระป๋องโผล่ครึ่งใบตรงขอบเฟรม
(ซึ่ง**คมกว่า**เพราะขอบวัตถุตัดกับฉากหลัง) ชนะภาพที่เห็นเต็มใบ · และการหยุดโมเดล
ระหว่างถ่ายต้อง opt-in สองชั้น

| ของใหม่ | รายละเอียด |
|---|---|
| `completeness(roi, w, h, max_area)` | **ยืมเกณฑ์จาก `app._can_complete()` ของโหมด USB** — กรอบต้องไม่แตะขอบเฟรม (เผื่อขอบ 2% เท่ากัน) ➕ ขนาดใกล้ใบใหญ่สุดของชุด ➕ ใกล้กลางเฟรม |
| `rank_key(rec, best_sharp)` | คะแนนรวม = **สมบูรณ์ × (ความคมสัมพัทธ์ ^ 0.5)** |
| `top_files()` | แทน `top_sharp_files` (คงชื่อเดิมเป็น alias) |
| เรียงในแกลเลอรี | เพิ่ม **"ดีที่สุด (สมบูรณ์ + คม)"** (ค่าตั้งต้น) และ **"สมบูรณ์ที่สุดก่อน"** |
| การ์ด | บรรทัด **"สมบูรณ์"** — เขียว/เหลือง/แดง · **"โดนขอบตัด"** · **"— วัดไม่ได้"** |
| `HIK_BURST_PAUSE_INFERENCE` | **`True`** + ช่องติ๊ก**ติ๊กไว้ให้เลย** (เหลือ opt-in ชั้นเดียว) |
| `HIK_BURST_JPEG_QUALITY` | 95 → **90** — encode คือคอขวดของ "ดิสก์เขียนทัน" (48.1 ms/ภาพ = 20.8 ภาพ/วิ) |

**⚠️ ความต่างสำคัญจากโหมด USB:** ฝั่ง USB รู้ว่า "กระป๋องเต็มใบ" เพราะ**โมเดลบอก**
(กล่องคลาส `can`) ⇒ ต้องรันโมเดลทุกเฟรม. โหมดถ่ายรัว **ตั้งใจไม่เรียกโมเดล** จึงใช้
`roi` จากการลบฉากหลังแทน — หยาบกว่าแต่ **มีข้อมูลอยู่แล้ว ไม่ต้องถ่ายใหม่**

**พิสูจน์บนข้อมูลสังเคราะห์ที่วิ่งผ่าน `compute_metrics` ตัวจริง:**

| เฟรม | คม | สมบูรณ์ | คะแนนรวม |
|---|---|---|---|
| ชิดขอบซ้าย ×3 | **16,425** (คมกว่า 2.2 เท่า) | **0.00** | **0.000** |
| กลางเฟรม ×3 | 7,572 | 1.00 | **0.678** ✅ |

⇒ ระบบเลือกเฉพาะ 3 ใบที่เห็นเต็ม — **เกณฑ์เดิมจะเลือกใบที่โดนขอบตัดทั้งสามใบ**

**กติกา "ไม่รู้ ≠ ไม่ดี" (กฎเหล็กข้อ 2):** เฟรมที่หาวัตถุไม่เจอคืน **`None` ไม่ใช่ 0.0** —
บนการ์ดขึ้น *"— วัดไม่ได้"* คนละอย่างกับ *"โดนขอบตัด"* · ในการเรียง `None` ไปท้ายสุด
ไม่ใช่ถูกมองเป็น 0 · **ถ้าทั้งชุดวัดไม่ได้เลย ลำดับจะกลายเป็น "เรียงตามความคม"
เหมือนเดิมโดยอัตโนมัติ** (มีเทสต์ล็อกทั้งสามข้อ)

**⚠️ ข้อจำกัดที่ต้องรู้ก่อนใช้กับไลน์จริง:** `_moving_rect` หาวัตถุด้วยการเทียบกับ
**ค่ามัธยฐานของทั้งชุด** ⇒ ถ้าวัตถุอยู่ตำแหน่งเดิมในเฟรมส่วนใหญ่ มัธยฐานจะกลืนวัตถุ
เข้าไปเป็น "ฉากหลัง" แล้วหาไม่เจอทั้งชุด (เจอตอนเขียนเทสต์: 6 เฟรมที่ 2 ตำแหน่ง
แบ่งครึ่งพอดี → อ่าน roi ผิดทุกใบ). **บนไลน์ที่กระป๋องวิ่งติด ๆ กันต้องวัดก่อนเชื่อ**

**🐛 บั๊กที่สร้างเองซ้ำเป็นครั้งที่ 2 ในวันเดียว — ชื่อชนกันในไฟล์เทสต์:**
ครั้งแรก `_start_hik` (บังของเดิม → เทสต์ burst 4 ตัวพัง) · ครั้งที่สอง **`W, H = 1000, 800`
ทับ `W, H = 320, 240` ที่บรรทัด 32** ซึ่งเป็นขนาดเฟรมทดสอบของทั้งไฟล์ →
**เทสต์ 5 ตัวที่ไม่เกี่ยวกันเลยพัง**
> 💡 **บทเรียน: ก่อนเติมชื่อระดับโมดูลในไฟล์เทสต์ที่ยาว ต้อง `grep` ชื่อนั้นก่อนเสมอ**
> อาการจะโผล่เป็นความล้มเหลวของเทสต์อื่นที่ดูไม่เกี่ยวข้อง ทำให้ไล่ผิดทาง

**⏳ ยังไม่ได้ทำ (รอผู้ใช้ยืนยัน):** **บัฟเฟอร์ใน RAM + คัดใบที่ดีที่สุดต่อหน้าต่างเวลา**
แทน `every_n` ที่ทิ้ง 3 ใน 4 เฟรมแบบสุ่ม (อาจทิ้งใบที่ดีที่สุดไป) — **แตะเธรดจับภาพ**
จึงต้องให้ผู้ใช้เห็นชอบก่อน

### 🏆 "คัดใบที่ดีที่สุดต่อหน้าต่างเวลา" แทน "เก็บ 1 ใน N" (25 ส.ค. 2026)

**ปัญหาของ `every_n`:** มันทิ้ง N-1 ใน N เฟรม **ตามลำดับ** ⇒ ใบที่ดีที่สุดของกระป๋อง
ใบนั้นอาจเป็นใบที่ถูกทิ้งพอดี. โหมดใหม่ให้กล้องวิ่งเต็มอัตราเหมือนเดิม แต่เก็บ
**ใบที่คะแนนสูงสุดของแต่ละช่วงเวลา** ⇒ ได้ไฟล์เท่ากันแต่เป็นใบที่ดีกว่า

**📊 พิสูจน์ด้วยการจำลองกระป๋องวิ่งผ่านเฟรมที่ 450 CPM (68.3 fps · คาบ 133 ms):**

| วิธี | เก็บได้ | **อยู่กลางเฟรม** | ระยะจากกลางเฉลี่ย |
|---|---|---|---|
| `every_n = 4` | 51 ภาพ | 9 (**18%**) | 197 px |
| **หน้าต่าง 133 ms** | **33 ภาพ** | **20 (61%)** | **64 px** |

⇒ **ไฟล์น้อยลง 35% แต่ได้ใบที่ใช้งานได้มากขึ้น 2.2 เท่า** (มีเทสต์แบบเวลาปลอมล็อกไว้)

**ทำที่ไหน:** ใน `_DatasetWriter.put()` เท่านั้น ⇒ **`capture_loop` และ `_post()`
ไม่ถูกแตะแม้แต่บรรทัดเดียว**

| จุด | รายละเอียด |
|---|---|
| `_sharpness(frame, crop)` | ย่อ 1/4 → Sobel เฉพาะ**กลางเฟรม** (60%) — วัตถุที่โผล่แค่ริมเฟรมแทบไม่เพิ่มพลังงานตรงกลาง = ตัวแทนหยาบ ๆ ของ "เข้ามากลางเฟรมแล้วและคม" โดยไม่ต้องเรียกโมเดล |
| RAM | **1 เฟรม** ("ดีที่สุดจนถึงตอนนี้" ไม่ต้องเก็บใบที่แพ้แล้ว) — ไม่ใช่ ring buffer |
| `HIK_BURST_WINDOW_MS` | **0 = ปิด = พฤติกรรมเดิม 100%** · ตั้ง 133 = คาบของ 450 CPM |
| `every_n` | **ถูกละเว้นเมื่อเปิดโหมดนี้** (ทำงานเดียวกัน — ใช้พร้อมกันได้ผลแย่ที่สุดของทั้งคู่) |

**🔑 ทำไมปลอดภัยกว่า "ตัวกรองที่ทิ้งเฟรม" ที่เคยปฏิเสธไป:**
**ทุกหน้าต่างเขียน 1 ใบเสมอ** ⇒ ถ้าคะแนนตัดสินผิด อย่างแย่ที่สุดคือได้ใบรอง
**ไม่ใช่ไม่ได้ภาพของกระป๋องใบนั้นเลย** · และ `stop()` **flush ใบที่ค้างเสมอ**
ไม่งั้นวัตถุใบสุดท้ายของทุกรอบจะหายเงียบ ๆ (มีเทสต์ล็อก)

**⚠️ กับดักที่ต้องแก้พร้อมกัน — ไม่งั้นการวินิจฉัยจะกล่าวหากล้องผิด:**
ในโหมดนี้ `saved × every_n` ใช้ประมาณอัตรากล้อง**ไม่ได้อีกต่อไป** (saved=26 แต่กล้อง
ส่งมา 200) ⇒ `_DatasetWriter` บันทึก **`considered`** (นับทุกเฟรมในเธรดจับภาพ) และ
`diagnose()` ใช้ค่านั้นก่อนเสมอ. **ยืนยันบนเบราว์เซอร์: `considered/elapsed = 47.5 fps`
= `delivered_fps` เป๊ะ และ `cause = "ok"`** — ถ้าไม่แก้ ระบบจะคำนวณได้ 6.2 fps
แล้วไปบอกให้ผู้ใช้แก้ packet size ที่ไม่ได้พัง

**🐛 บั๊กเก่าที่เจอระหว่างทาง (เทสต์ยูนิตมองไม่เห็น — เจอเพราะขับเบราว์เซอร์จริง):**
`loadSessions()` เป็นที่เดียวที่เปิด/ปิดแถวของฟีเจอร์ แต่มันถูกเรียก **เฉพาะตอนเปิด
แกลเลอรี** ⇒ ช่องติ๊ก **"⏸️ หยุดโมเดลระหว่างถ่ายรัว" ไม่เคยโผล่บนหน้าเว็บเลย**
จนกว่าผู้ใช้จะเผลอไปเปิดแกลเลอรีก่อน (บั๊กนี้มีมาตั้งแต่รอบก่อน).
แยกเป็น `applyCaps()` + `loadCaps()` แล้วเรียกตอน init และตอนกล้องเริ่มทำงาน

**ตัวประมาณบนหน้าเว็บ (วัดจริงบน container):**
`ปิด → 887 ภาพ · 825 MB` · `เปิด → 75 ภาพ · 70 MB (ใบที่ดีที่สุดทุก 133 ms · คัดจาก 12 เฟรม)`

### 🏭 ผลรันสถานี 25 ส.ค. 2026 — จุดที่ **6 และ 7** ที่รายงานผิดแบบมั่นใจ

**ชุด `20260825_095132`** — 96 ภาพ · 68.5 MB · exposure **2165 µs** · gain 7.7 dB ·
**ROI เต็มเฟรม 2448×2048** (ไม่ใช่ ROI ครึ่ง)

| ตัวชี้วัด | ค่า | อ่านว่าอย่างไร |
|---|---|---|
| กล้องส่งมาจริง | **20.2 fps** | ชนเพดานสาย GigE ที่เต็มเฟรม (22.4) |
| **encode / เขียน** | **101.6 / 1.7 ms** | เขียนได้แค่ **9.7 ภาพ/วิ** |
| ทิ้งเพราะดิสก์ | **98 เฟรม (51%)** | ผลโดยตรงของสองข้อบน |
| เฟรมหายระหว่างทาง | 37 (15.4%) | ลิงก์วิ่งที่ 90% ของเพดาน |

⇒ **เต็มเฟรมทำให้พังทุกด้านพร้อมกัน** — ต้องกลับไปใช้ ROI

**🐛 จุดที่ 6 — ไฟเขียวจากการทดสอบที่ช้ากว่าไลน์ 105 เท่า (ร้ายแรงที่สุดในตระกูลนี้)**

ระบบขึ้น ✅ **"คมพอ — กล้องหยุดการเคลื่อนที่ได้ที่ exposure นี้"** เพราะ
วัดได้ **74 px/วิ** × 2165 µs = เบลอ 0.16 px — **คำนวณถูกทุกหลัก แต่ไร้ความหมาย**
เพราะไลน์จริงวิ่ง 7,800 px/วิ. ด่าน `MIN_MEANINGFUL_SHIFT_PX` กรอง *สัญญาณรบกวน*
ได้ แต่ไม่เคยถามว่า **"ความเร็วที่ทดสอบใกล้เคียงกับที่สนใจไหม"**

| | เดิม | **ตอนนี้** |
|---|---|---|
| ป้าย | ✅ เขียว "กล้องหยุดการเคลื่อนที่ได้" | ⚠️ เหลือง "✅ คมพอ **ที่ความเร็วที่ทดสอบ** — แต่ยังไม่ได้ตอบเรื่องไลน์จริง" |
| บอกเรื่องไลน์ | ไม่บอกเลย | **🏭 ช้ากว่าไลน์ 105 เท่า · เบลอที่ไลน์ 16.89 px · ลด exposure เหลือ ~513 µs ⇒ เพิ่มไฟ ~4.2 เท่า** |

`HIK_BURST_LINE_SPEED_PX_S = 7800` (450 CPM) · `..._MIN_RATIO = 0.25`
> 💡 **`blur_at_line_px` คำนวณนอกเงื่อนไข `moving_enough` โดยตั้งใจ** — "exposure นี้
> จะเบลอกี่ px ที่ความเร็วไลน์" ขึ้นกับ exposure อย่างเดียว ⇒ ตอบได้แม้วัตถุนิ่งสนิท
> ✅ ตัวเลข **513 µs** ที่ได้ ตรงกับที่คำนวณด้วยมือไว้ก่อนหน้าเป๊ะ (คนละเส้นทาง)

**🐛 จุดที่ 7 — ลำดับ "ดีที่สุด" กลายเป็นลำดับตามชื่อไฟล์**

4 ใบแรกในแกลเลอรีคือ `00037-00040` เรียงกันเป๊ะ ทั้งที่คะแนนคมคือ
**435 / 1519 / 1876 / 1855** (ไม่ได้เรียงเลย) — เพราะทุกใบ "โดนขอบตัด"
(`complete = 0`) และ `rank_key` เดิมคืน `c × rel^0.5` ⇒ **0 เท่ากันหมด**

แก้เป็นคืน **tuple `(ระดับความสมบูรณ์, ความคมสัมพัทธ์)`** ⇒ ความคมเป็นตัวตัดสิน
เสมอเมื่อความสมบูรณ์เท่ากัน. ลำดับใหม่: `00039 · 00040 · 00038 · 00037` ✅
> ⚠️ เทสต์เดิมครอบเคส "ทั้งชุด**วัดไม่ได้** ⇒ ถอยไปเรียงตามความคม" ไว้แล้ว
> **แต่ไม่ได้ครอบเคส "ทั้งชุด**โดนขอบตัด**"** ซึ่งเป็นคนละทางในโค้ด — เพิ่มแล้ว

**+ คำแนะนำเรื่องดิสก์** เสนอ **"เก็บใบที่ดีที่สุดต่อช่วงเวลา"** เป็นทางแรก
(ได้ไฟล์น้อยลงโดยไม่ทิ้งใบที่ดีที่สุด) และ **ไม่เสนอซ้ำเมื่อเปิดอยู่แล้ว**

### 🎥 โหมด "ไม่ตรวจภาพสด" + ค่าตั้งต้นสำหรับงานจริง (25 ส.ค. 2026)

**สมมติฐานของผู้ใช้:** *"ตอน Live ไม่จำเป็นต้องตรวจ รอให้ถ่ายแล้วค่อยตรวจ น่าจะเร็วกว่า"*
⇒ **ถูกต้อง และวัดได้ 2.3 เท่า**

| | เห็นรูป | ครบ | โมเดล | **รอคิว** |
|---|---|---|---|---|
| ตรวจภาพสดอยู่ (เดิม) | 87 ms | **1,216 ms** | 421 ms | **656 ms** |
| **ปิดตรวจภาพสด** | 56 ms | **525 ms** | 423 ms | **0 ms** |

**เวลาโมเดลเท่ากันเป๊ะ (421 vs 423)** ⇒ ทั้งหมดที่หายไปคือการรอคิวที่ `_infer_lock`
— ยืนยันว่าเป็นการแย่ง iGPU ไม่ใช่โมเดลหนัก

| ของใหม่ | รายละเอียด |
|---|---|
| `GET/POST /api/camera/hik/live_detect` | เปิด/ปิดการตรวจภาพสด — **ภาพยังสดปกติ** |
| ช่องติ๊ก "⏸️ ไม่ต้องตรวจภาพสด" | + **กล่องเตือนสีแดงตลอดช่วงที่ปิด** |
| รีเซ็ตเป็น "ตรวจ" ทุกครั้งที่กด Start | ห้ามค้างในสภาพ "ไม่ตรวจ" ข้ามรอบ (จุดบอด QC) |

**🐛 บั๊กที่เจอตอนทำ (มีมาตั้งแต่ทำ "หยุดโมเดลระหว่างถ่ายรัว"):**
ระหว่างหยุดตรวจ `latest_det_frame` ไม่ถูกอัปเดต ⇒ โหมด LOCKED ของ `generate_frames`
**ค้างอยู่ที่เฟรมสุดท้ายที่ตรวจ = จอนิ่งสนิททั้งที่กล้องยังทำงาน**. แก้ให้เผยแพร่เฟรมดิบ
ต่อไปพร้อม `detections = []` ⇒ ภาพเดินต่อแต่ไม่มีกรอบค้าง
(ยืนยันบนเบราว์เซอร์: `disp_fps 14.3` ระหว่างไม่ตรวจ · มีเทสต์ล็อก)

### ⚙️ ค่าตั้งต้นที่เปลี่ยนให้ตรงกับงานจริง

| ค่า | เดิม | **ใหม่** | เหตุผล (วัดจริงบนสถานี) |
|---|---|---|---|
| `HIK_DEFAULTS` ROI | เต็มเฟรม | **1224×1024 + `roi_center`** | เต็มเฟรม 20.2 fps · encode 101.6 ms · ทิ้ง 51% · หาย 15.4% **vs** ครึ่งกลาง 68.3 fps · 45.1 ms · ทิ้ง 0% · หาย 0.3% |
| `HIK_BURST_WINDOW_MS` | 0 | **133** | คาบของ 450 ใบ/นาที ⇒ ~1 ภาพต่อกระป๋อง |

> ⚠️ **ค่าที่ผู้ใช้บันทึกเองจากหน้าเว็บ (`HIK_SETTINGS_FILE`) ทับ `HIK_DEFAULTS` เสมอ**
> ⇒ ถ้าเคยกด "เต็มเฟรม" แล้วบันทึกไว้ ต้องกด **"ครึ่งกลาง" + บันทึกใหม่**

**⚠️ เทสต์เดิม 4 ตัวที่ hard-code 2448 พังทันที** — แก้ให้อ้าง `cam.width` (ROI ที่กล้อง
ส่งจริง) แทนตัวเลขตรง ๆ ⇒ ทดสอบ *คุณสมบัติ* ("เต็มความละเอียดของ ROI ไม่ใช่ภาพที่ย่อ")
ไม่ใช่ค่าคงที่ + เพิ่มเทสต์ที่ล็อกค่าตั้งต้นใหม่ไว้ชัด ๆ
> 💡 **บทเรียนเครื่องมือ: `str.replace()` เงียบเมื่อไม่ match** — แก้ indent ผิด 4 ช่อง
> แล้วสคริปต์รายงาน "fixed" ทั้งที่ไม่ได้แก้อะไรเลย. **ต้อง `assert old in s` ทุกครั้ง**

### ⚡ "ตั้งค่าอะไรให้กล้องเร็วขึ้น" — ตัวจำกัด fps มี 4 ตัว (25 ส.ค. 2026)

**fps ที่ได้จริง = ค่าต่ำสุดของ 4 เพดาน** ⇒ ปรับผิดตัว = เสียเวลาโดยไม่ได้อะไรเลย
แผงตั้งค่าจึงมีกล่อง **"⚡ เพดาน N fps — ตัวจำกัดคือ ___"** ที่คำนวณสดจากค่าที่ตั้งอยู่

| เพดาน | สูตร | แก้ยังไง |
|---|---|---|
| **สายแลน GigE** | `125 MB/s × eff ÷ (W×H×ไบต์ต่อพิกเซล)`<br>eff = 0.90 (Jumbo 8164) · 0.70 (1500) | ลด **พิกเซล** (กว้างหรือสูงก็ได้) · packet 8164 + Jumbo |
| **เซนเซอร์** (`ResultingFrameRate` — กล้องบอกเอง) | อ่านทีละแถว | ลด **ความสูง** เป็นหลัก — ความกว้างแทบไม่ช่วย |
| **exposure** | `1 ÷ exposure` | ลด exposure (ต้องเพิ่มไฟ) |
| **จำกัดเอง** | `AcquisitionFrameRate` | **ปิด** `framerate_enable` |

**🔑 ข้อค้นพบจากการเทียบสูตรกับผลวัดจริง 2 จุด — ตัวจำกัดเปลี่ยนตาม ROI:**

| ROI | เพดานสาย | กล้องบอกเอง | วัดได้จริง | **ตัวจำกัด** |
|---|---|---|---|---|
| เต็มเฟรม 2448×2048 | **22.4** | 23.6 | **23.6** | 🔌 **สายแลน** |
| ครึ่งกลาง 1224×1024 | 89.8 | **69** | **69** | 📷 **เซนเซอร์** |

⇒ **ที่เต็มเฟรมต้องลดพิกเซล · ที่ ROI ครึ่งต้องลดความสูง** — คนละวิธีกันโดยสิ้นเชิง
(ถ้าไปลดความกว้างตอนติดเซนเซอร์ จะได้ fps เท่าเดิมแต่เห็นน้อยลงฟรี ๆ)

**ต้นทุนฝั่งเครื่องต่อเฟรม (วัดบน container ด้วย OpenCV บน array ขนาดจริง):**

| ROI | MP | แปลง Bayer | ย่อลงจอ | encode JPEG | **เพดานดิสก์** |
|---|---|---|---|---|---|
| 2448×2048 | 5.01 | 2.0 ms | 5.8 ms | 35.2 ms | **27/วิ** |
| 2448×1100 | 2.69 | 0.5 | 3.7 | 17.7 | 51/วิ |
| **1224×1024** | 1.25 | 0.2 | 2.9 | 8.3 | **97/วิ** |
| 1224×512 | 0.63 | 0.2 | 1.5 | 4.3 | 158/วิ |

> ⚠️ **สถานีช้ากว่า container 2.9-5.4 เท่า** (RAM single-channel + iGPU แชร์บัส) —
> เต็มเฟรม encode สถานี 101.6 ms vs container 35.2 ⇒ ใช้ตัวเลขสถานีเป็นหลักเสมอ

**ปุ่ม ROI เพิ่ม 2 แบบ:** **กว้าง-เตี้ย** (กว้างเต็ม × สูงครึ่ง — เห็นกว้างเท่าเดิม
แต่เซนเซอร์อ่านเร็วขึ้น) · **เร็วสุด** (กว้างครึ่ง × สูง 20% — ไล่หา fps สูงสุด)

**เทสต์กันค่าคงที่เพี้ยนข้ามไฟล์:** `GIGE_MB_S`/`GIGE_EFF_JUMBO`/`GIGE_EFF_SMALL`/
`JUMBO_MIN_PACKET` ถูกใช้ทั้งใน `hik_burst.diagnose()` (หลังถ่าย) และ `hik_camera.js`
(ก่อนถ่าย) ⇒ มีเทสต์อ่านไฟล์ JS มาเทียบ **พิสูจน์แล้วว่าจับ drift ได้จริง**
(แก้ค่าใน JS ให้ต่าง → เทสต์แดงทันที) ไม่งั้นผู้ใช้จะเห็นเพดานคนละตัวเลขจากหน้าจอเดียวกัน

### 🧪 ผลทดสอบ ROI 3 แบบบนสถานี (25 ส.ค. 2026) — **กว้าง-เตี้ยชนะ** + เจอของถดถอย

กดปุ่ม ROI ตามลำดับ **ครึ่งกลาง → กว้าง-เตี้ย → เร็วสุด** แล้วอ่านแถบสถิติสองแถบ:

| ROI | fps ที่กล้องส่ง | เพดานสาย | สนามภาพ | **เฟรมเต็มใบ/กระป๋อง** | `inf` | `disp` |
|---|---|---|---|---|---|---|
| ครึ่งกลาง 1224×1024 | **25.8** | 89.8 | 100×84 mm | **1.11** | 378 ms | 3 fps |
| **กว้าง-เตี้ย 2448×1024** | **24.8** | 44.9 | **201**×84 mm | **4.97** ✅ | 164 ms | 7 fps |
| เร็วสุด 1224×408 | **72.6** | 225 | 100×**33** mm | ❌ **กระป๋องไม่พอดีสูง** | 155 ms | 6 fps |

**🏆 ข้อสรุป: "กว้าง-เตี้ย 2448×1024" คือค่าที่เหมาะกับไลน์ที่สุด**
fps ต่ำกว่า "เร็วสุด" เกือบ 3 เท่า **แต่ได้เฟรมเต็มใบต่อกระป๋องมากกว่า 4.5 เท่า**
เพราะสนามกว้าง 2 เท่า ⇒ กระป๋องอยู่ในเฟรมนานกว่า 2 เท่า
> ⚠️ **"เร็วสุด" 1224×408 ใช้กับงานนี้ไม่ได้เลย** — สูง 408 px = **33 mm** ซึ่งเล็กกว่า
> ฝากระป๋อง (~73 mm) ⇒ **ภาพโดนตัดบน-ล่างเสมอ** (เห็นชัดในภาพหน้าจอ). fps สูงแต่ไร้ค่า
> — เตือนว่า **fps ดิบไม่ใช่ตัวชี้วัด** ตัวชี้วัดคือ *เฟรมที่เห็นกระป๋องเต็มใบต่อ 1 ใบ*

**⚠️ ทั้งสามค่ายังไม่ถึงเพดานของตัวเอง** (25.8 จาก 89.8 · 24.8 จาก 44.9) ⇒ **ตัวจำกัดจริง
ตอนนี้ไม่ใช่ ROI แต่เป็นเครื่อง** — ดูหัวข้อถัดไป

### 🚨 ของถดถอยที่เจอจากผลนี้: **inference ช้าลง 3-8 เท่า**

| | เคยบันทึกไว้ | **วัดได้ 25 ส.ค.** |
|---|---|---|
| `inf` (imgsz 480, live) | **45-50 ms** (iGPU/OpenVINO) | **155-378 ms** |
| ตรวจได้ต่อวินาที | 20-22 ครั้ง | **2.6-6.5 ครั้ง** |
| ต่อกระป๋องที่ 450 CPM | 2.67 ครั้ง | **0.35-0.86 ครั้ง** ❌ |

**สมมติฐานอันดับแรก: backend เร่งความเร็วตกกลับไป CPU** — ผู้ใช้เพิ่งกู้ `bestX.pt`
คืนมาหลังเผลอลบ ⇒ ไฟล์ `.pt` **ใหม่กว่า** `bestX_openvino_model/` และ `bestX.onnx`
⇒ ตัว stale-guard ใน `_maybe_openvino()`/`_maybe_onnx()` สั่ง **re-export** ถ้า export
ล้มเหลว (เช่นไม่มี openvino ในรอบนั้น) จะตกไป **ONNX CPU ~280 ms** หรือ **PyTorch ~315 ms**
ซึ่งตรงกับ 155-378 ms ที่วัดได้ ส่วน 45-50 ms คือของ iGPU

**วิธีตรวจ (เรียงตามความเร็วในการตัดเรื่อง):**
```
py -3.9 app.py            # ดู log ตอน start: ใช้ backend ไหน (OpenVINO/ONNX/PyTorch)
py -3.9 -c "import openvino; print(openvino.__version__)"   # ต้องได้ 2024.6.0
dir weights\can_dent      # bestX_openvino_model/ มีไหม · ใหม่กว่า bestX.pt ไหม
py -3.9 verify_openvino.py --weights weights\can_dent\bestX.pt --images sample_cans
```
> 💡 **ตัวเลข `inf` บนแถบ perf คือตัวชี้วัดที่ดีที่สุด** — ถ้ากลับไปที่ ~50 ms แปลว่า
> iGPU กลับมาแล้ว. เรื่องนี้กระทบ **ทุกอย่าง**: fps ที่กล้องส่งได้ · เฟรมที่ทิ้ง ·
> ความเร็วของ "ถ่าย 1 เฟรม" (เพราะต้องรอคิว) ⇒ **แก้ก่อนวัดอะไรต่อ**

**เฟรมหาย 6,700 → 8,153 → 13,169** (ตัวนับสะสมตั้งแต่เปิดกล้อง ไม่ได้รีเซ็ตตอนเปลี่ยน ROI
⇒ เทียบข้าม ROI ตรง ๆ ไม่ได้) แต่การที่มันโตเร็วขนาดนี้ยืนยันว่า **กล้องผลิตเฟรมเร็วกว่า
ที่เครื่องรับไหว** — อาการเดียวกับ inference ที่ช้า

### 🔧 แก้ต้นเหตุ inference ช้า — mtime → hash + ทำให้การถดถอย "มองเห็นได้" (25 ส.ค. 2026)

**ต้นเหตุ:** `_maybe_openvino()`/`_maybe_onnx()` ตัดสิน "ต้อง export ใหม่ไหม" ด้วย
`os.path.getmtime(pt) > os.path.getmtime(export)` ⇒ **การคัดลอกไฟล์เดิมกลับมา**
(กู้จากถังขยะ / คัดจากที่สำรอง) ทำให้ mtime กลายเป็น "ตอนนี้" ⇒ ใหม่กว่า IR เสมอ
⇒ re-export ทุกครั้งที่เปิดโปรแกรม · ถ้า export ล้ม → **ตกไป CPU เงียบ ๆ**

| ชั้น | แก้อะไร |
|---|---|
| **① ตัวตรวจ stale** | เทียบ **sha1 ของเนื้อ `.pt`** เก็บใน sidecar `<export>.src` แทน mtime ⇒ ไฟล์เนื้อเดิม = ไม่ export ใหม่ · เทรนใหม่จริง (เนื้อเปลี่ยน) = export ใหม่ตามเดิม |
| **② การถดถอยต้องดัง** | ตกไป backend ที่ช้ากว่า = `logger.error` พร้อม **คำสั่งที่ต้องรันเพื่อแก้** (เดิมเป็น warning ที่จมอยู่ในคอนโซล) |
| **③ มองเห็นบนหน้าเว็บ** | `detector.backend_label/backend_downgraded/backend_note` → `/api/detection/status` → **แถบ perf ต่อท้ายว่า `/ OpenVINO@intel:gpu` หรือ `/ PyTorch 🐢` (สีแดง + tooltip บอกเหตุผล)** |

**⚠️ จุดที่เกือบพลาด — ธง `downgraded` ตอนแรกไม่ทำงาน:** เส้นทางที่พบบ่อยที่สุดคือ
`_maybe_*` คืน `None` **ตั้งแต่ต้น** (import ไม่ได้ / ไม่พบอุปกรณ์ / export ล้ม) ซึ่ง
**ไม่เคยผ่าน `except` ของลูปเลย** ⇒ ถ้าดูแค่ลูปจะรายงานว่า "ปกติดี" ทั้งที่ตกไป CPU แล้ว.
แก้เป็น `_check_downgraded()` ที่เทียบกับ **สิ่งที่ config ขอไว้** ไม่ใช่ดูว่าลูปพังไหม

**ยืนยันบน container (จำลองเคสสถานี):**
```
🐢 กำลังใช้ **PyTorch** ซึ่งไม่ใช่ตัวที่เร็วที่สุดที่ตั้งไว้
   (OpenVINO@intel:gpu: ไม่พบอุปกรณ์นี้ในเครื่อง · ONNX: no export in container)
   ตรวจ: py -3.9 -c "import openvino;print(openvino.__version__)" · ต้องได้ 2024.6.0
แถบ perf: cam 265fps / inf 201ms / enc 1.6ms / disp 5fps / PyTorch 🐢   ← สีแดง
```

**4 เคสของตัวตรวจ stale ที่พิสูจน์แล้ว** (`tests/test_accel_backend.py` 11 ตัว):
① export ตรงกัน → ไม่ทำใหม่ · ② **กู้ไฟล์เดิม (mtime ใหม่กว่า) → ไม่ทำใหม่** ✅ ·
③ เทรนใหม่ (เนื้อเปลี่ยน) → ทำใหม่ · ④ อัปเกรดจากเวอร์ชันที่ยังไม่มี sidecar และ
export ใหม่กว่า `.pt` → ไม่ทำใหม่ + จด sidecar ให้เลย

> 🐛 **บั๊กที่สร้างเองในไฟล์เทสต์ใหม่:** stub `ultralytics` ด้วย `ModuleType` เปล่า
> ซึ่ง `__spec__ is None` ⇒ `importlib.util.find_spec()` **โยน ValueError**
> ⇒ `test_hik_routes.py` ทั้งไฟล์พังตอน collect. ต้องใส่ `ModuleSpec` ให้ stub เสมอ

### 🔬 `diagnose_accel.py` — "ทำไมไม่ใช้ iGPU" ตอบด้วยหลักฐาน (25 ส.ค. 2026)

**ที่มา:** แถบ perf บอกได้แค่ว่า *ตกไปตัวที่ช้ากว่า* (`ONNX 🐢`) แต่ **เหตุผลที่บันทึกไว้
เป็นข้อความตายตัวใบเดียว** `"ไม่พบอุปกรณ์นี้ในเครื่อง"` ทั้งที่ต้นเหตุมี 3 แบบซึ่ง
**แก้คนละวิธี คนละวัน** ⇒ บอกรวม ๆ = ส่งผู้ใช้ไปแก้ของที่ไม่ได้พัง (กฎเหล็กข้อ 2
รูปแบบเดียวกับ "แนะให้ตั้ง packet size ทั้งที่ตั้งไว้แล้ว" ของ `diagnose()`)

| เดิม | ตอนนี้ (`_openvino_device_status()` คืน `(ok, reason)`) |
|---|---|
| `"ไม่พบอุปกรณ์นี้ในเครื่อง"` ทุกกรณี | ① `ไม่ได้ติดตั้งแพ็กเกจ openvino (import ล้มเหลว: …) — แก้ด้วย pip install "openvino==2024.6.0"` |
| | ② `openvino 2024.6.0 ติดตั้งแล้ว แต่ **ไม่เห็นอุปกรณ์ GPU** (เห็นแค่ ['CPU']) — เป็นเรื่องไดรเวอร์/OpenCL ไม่ใช่แพ็กเกจ` |
| | ③ `openvino 2024.6.0 อ่านรายชื่ออุปกรณ์ไม่ได้ (…)` |

**สคริปต์พิมพ์ทุกจุดตัดสินของเส้นทางจริง** (ไม่ใช่การเดา): ค่าตั้งที่ใช้ตัดสิน ·
เวอร์ชัน openvino/onnxruntime/ultralytics/torch + **`available_devices` และชื่อเต็ม
ของแต่ละอุปกรณ์** · ไฟล์ export บนดิสก์พร้อมคำตอบว่า **"จะ export ใหม่ไหม"** จาก
ตรรกะตัวจริง (`_export_is_stale`) · โหลดโมเดลด้วยเส้นทางเดียวกับ `app.py` แล้ว
รายงาน `backend_label` + ทุกบรรทัด `ข้าม →` · **วัด ms/เฟรมที่ imgsz 480 และ 1280**
· สรุปพร้อมคำสั่งแก้ที่ตรงต้นเหตุ. `--quick` = อ่านอย่างเดียว ไม่โหลด/ไม่ export
· exit `0` = ได้ตัวที่เร็วที่สุดตามที่ตั้งไว้ · `1` = ตกไปตัวที่ช้ากว่า · `2` = รันไม่ได้

**พิสูจน์แล้วว่าแยกสาเหตุได้จริง** — ขับด้วย openvino ปลอม 3 แบบบน container:
ไม่มีแพ็กเกจ → เสนอ `pip install` · มีแต่เห็นแค่ `['CPU']` → ชี้ไปที่ **ไดรเวอร์
และห้ามเสนอ pip** · เห็น `GPU.0` → ผ่านด่าน (ชื่อแบบมีเลขต่อท้ายต้องนับว่าใช้ได้)

**เทสต์เพิ่ม 7 ตัวใน `tests/test_accel_backend.py` (รวม 18)** — และ**พิสูจน์ว่าจับ
การถอยหลังได้จริง**: เอาข้อความตายตัวเดิมกลับไปใส่ → `test_skip_reason_reaches_the_badge`
แดงทันที
> 💡 tooltip ของแถบ perf ตอนนี้ชี้ไปที่ `py -3.9 diagnose_accel.py` แทนคำสั่ง
> `import openvino; print(__version__)` เดิม ซึ่งตอบได้แค่ต้นเหตุ ① จาก 3 แบบ

**อัปเดต 25 ส.ค. (รอบที่ 2) — 2 บทเรียนจากการรันจริง:**

**① เครื่องมือให้ไฟเขียวผิดตัว** — default เดิมตรวจแค่ `config.MODEL_PATH`
(= `best.pt`) แล้วขึ้น ✅ ทั้งที่ระบบวิ่ง `bestX.pt` บน ONNX/CPU. โมเดลที่ใช้จริง
มาจาก **ตัวที่เลือกบนหน้าเว็บ** (`mode_registry.resolve_model_path`) และ
**แต่ละ `.pt` มี export แยกของตัวเอง** ⇒ ตอนนี้ **ตรวจทุก `.pt` ในโฟลเดอร์**
และพิมพ์ **คลาสของโมเดล** (`['dented','good']` = best · `['can','dent']` = bestX)
ให้เห็นว่ากำลังวัดตัวไหน. เทสต์ล็อก 2 ตัว
> 💡 นี่คือกฎเหล็กข้อ 2 ในเครื่องมือวินิจฉัยเอง — **เครื่องมือวัดที่วัดผิดทาง
> แย่กว่าไม่มีเครื่องมือ** (รูปแบบเดียวกับที่ `verify_ocr.py` เคยวัดเส้นทางเก่า)

**② เพิ่มการเปิด IR ดู `output` จริง** — ข้อความ `index 1 is out of bounds for
dimension 0 with size 1` อ่านแล้วเดาต้นเหตุไม่ออกเลย. ตอนนี้ข้อ ③ อ่าน
`<stem>_openvino_model/<stem>.xml` ด้วย `ov.Core().read_model()` แล้วรายงาน
**จำนวน output + รูปร่าง + task** (อ่าน task จาก sidecar `.onnx.task` — ไม่ต้อง
โหลด `.pt`) ⇒ **`task=segment` แต่มี output เดียว = IR เสีย** พร้อมบอกวิธีแก้
ทีละขั้น (ลบเฉพาะโฟลเดอร์ของโมเดลตัวนั้น → export ใหม่ → **verify ก่อนใช้จริง**)

### ⏸️ หยุดโมเดลระหว่างถ่ายรัว (`HIK_BURST_PAUSE_INFERENCE`, default **ปิด**)

`inference_loop` วิ่ง bestX ทุก ~50 ms บน iGPU ขณะที่ JPEG encode กิน CPU และ
**RAM ของสถานีเป็น single-channel** (คอขวดที่บันทึกไว้แล้ว) ⇒ แย่งกันจนเฟรมถูกทิ้ง.
ระหว่างเก็บข้อมูลเราไม่ต้องการผลตรวจสดอยู่แล้ว

**ความปลอดภัย 3 ชั้น (ฟีเจอร์นี้แตะ `inference_loop` ซึ่งเป็นหัวใจของการตรวจ):**
1. **opt-in สองชั้น** — ต้องเปิด `HIK_BURST_PAUSE_INFERENCE` ใน config **และ**
   ติ๊กในแผงต่อครั้ง (มีเทสต์ยิงทั้งสองชั้นแยกกัน)
2. **คลายเองเสมอ** — `_inference_paused()` ผูกกับ `_hik_burst_deadline` ⇒ ต่อให้
   เส้นทางปิดงานพังทั้งหมด (exception / กล้องหลุด / ปิดเบราว์เซอร์) การหยุดตรวจจะคลาย
   เองเมื่อถึงเวลาที่ตั้งไว้ + 3 วินาที **เป็นไปไม่ได้ที่ระบบจะค้างในสภาพ "ไม่ตรวจ" ตลอดไป**
3. **ล้างกรอบทิ้งขณะหยุด** — ไม่งั้น `generate_frames` จะวาดกรอบเก่าทับเฟรมใหม่
   = ชี้จุดผิดแบบมั่นใจ (กฎเหล็กข้อ 2) · และรีเซ็ตสถานะการนับกระป๋องเพื่อไม่ให้เกิด
   ขอบขาขึ้นปลอมตอนกลับมาตรวจ

⚠️ **ระหว่างหยุด การนับและการบันทึก DB หยุดด้วย** — หน้าเว็บจึงขึ้นคำเตือนสีแดงตลอดช่วงนั้น

**เทสต์ที่พิสูจน์ว่าทำงานจริง ไม่ใช่แค่ตัวแปรเปลี่ยนค่า:** `_CountingDetector` นับจำนวน
ครั้งที่ `detector.detect` ถูกเรียก ก่อน/ระหว่าง/หลังการหยุด (ต้องเป็น >0 / **0** / >0)
+ เทสต์ว่าถ่ายรัวแบบไม่ติ๊ก = ตรวจต่อตามปกติ + เทสต์ว่ากรอบถูกล้างจริง

### 🏭 "ทดสอบกับไลน์ 7 ใบ/วินาที ได้ไหม" — คำตอบเชิงตัวเลข (19 ส.ค. 2026)

| วิธี | ทำได้ไหมที่ 7 ใบ/วิ | ตัวเลขที่ตัดสิน |
|---|---|---|
| **กดปุ่ม "ถ่าย 1 เฟรม" รัว ๆ** | ❌ **ไม่ได้** | 1 ครั้ง ≈ **0.5 วินาที** (detect imgsz 1280 = 370-468 ms + encode/base64/HTTP) และเป็นคำขอแบบทีละอัน ⇒ เพดาน ~2 ครั้ง/วิ |
| **โหมด live free-running (ดูภาพ/ตรวจ)** | ⚠️ กล้องทัน แต่ **นับไม่ได้** | กล้องที่ ROI ครึ่ง = 69 fps ⇒ ~10 เฟรม/ใบ ✅ · แต่ inference bestX = 45-50 ms ⇒ ~20 ครั้ง/วิ = **2.9 ครั้ง/ใบ** และตัวนับต้องเห็น "เฟรมว่าง" ติดกัน `DEFECT_RESET_FRAMES=3` ⇒ **ต้องการช่องว่าง ≥150 ms แต่คาบทั้งใบมีแค่ 143 ms** ⇒ หลายใบถูกรวมเป็นใบเดียว |
| **เก็บภาพชุดข้อมูล + วัดออฟไลน์** | ✅ **ได้จริง — ใช้วิธีนี้** | การเก็บภาพวิ่งที่ **อัตรากล้อง** ไม่ขึ้นกับความเร็วโมเดล ⇒ 69 fps ที่ 7 ใบ/วิ = **9.9 เฟรม/ใบ** |

⇒ **`hik_offline_eval.py`** (ใหม่): เอาโฟลเดอร์ภาพที่เก็บมารันโมเดลตัวจริง แล้ว
**จัดกลุ่มเฟรมที่ติดกันเป็น "ใบ"** → รายงานจำนวนใบ · เฟรมต่อใบ (ต่ำสุด/กลาง/สูงสุด) ·
กี่ใบพบรอยบุบ · เวลา p50/p95 · และ **ตัดสินว่า "ถ้ารันสดจะตามทันไหม"** โดยเทียบคาบของไลน์
กับช่องว่างที่ตัวนับต้องการ (`--live-ms` ระบุเวลา inference จริงได้ ถ้าไปวัดบนเครื่องอื่น).
```
py -3.9 hik_offline_eval.py data\hik_dataset\<โฟลเดอร์> --fps 69 --save-dir out_ng
```
เทสต์ `tests/test_hik_offline_eval.py` **9 ตัว** ล็อกตรรกะการจัดกลุ่มและคำตัดสินไว้

**ตัวเก็บภาพถูกทำให้พร้อมใช้กับไลน์จริง:** ตั้ง **เวลา/ทุกกี่เฟรม/จำนวนสูงสุด** ได้จากหน้าเว็บ ·
หยุดเองเมื่อครบ · **หยุดเองเมื่อพื้นที่ว่าง < `HIK_DATASET_MIN_FREE_MB` (2 GB)** เพราะที่ ROI ครึ่ง
69 fps ≈ 30-40 MB/วินาที (60 วิ ≈ 2 GB) · รายงาน MB/s + จำนวนที่ **ทิ้งเพราะดิสก์ตามไม่ทัน**
(เฟรมที่ถูกทิ้ง = กระป๋องที่หายไปจากชุดทดสอบ ⇒ ต้องเห็นตัวเลขนี้ ห้ามหายเงียบ)

**การทดสอบที่ทำได้บน container (ทำแล้ว) vs ที่ต้องทำบนสถานี:**
- ✅ บน container: 51 เทสต์ผ่าน + รัน `app.py` ตัวจริงกับ **SDK ปลอม** แล้วขับด้วย
  **Chromium จริง** — แท็บสลับถูก, สร้างตัวควบคุม 19 ตัวจากช่วงจริงของกล้อง,
  Start→ภาพ MJPEG ขึ้น 1224×1024, ROI/exposure ตั้งได้ระหว่างสตรีม, เก็บชุดข้อมูลได้
  876 ภาพ, Stop แล้วปล่อยกล้องสะอาด, **ไม่มี JS error**
- ⏳ บนสถานี: ทุกอย่างที่เกี่ยวกับกล้อง/แสง/ความแม่นของ `bestX.pt` จริง

### 📏 ผลวัดจากกล้องจริง (19 ส.ค. 2026) — ตัวเลขเต็มอยู่ใน `docs/PLAN_LINE_DENT_INSPECTION.md` §0

| สิ่งที่วัด | ค่า | ผลต่อการออกแบบโหมดใหม่ |
|---|---|---|
| **packet size / delay** | ตั้งต้น **1500 / 400** → ตั้งเป็น **8164 / 0** แล้ว | ⚠️ **ค่านี้กล้องไม่จำ — ต้องตั้งทุกครั้งตอนเปิดกล้องใน `hik_camera.py`** ไม่งั้นได้ 15-17 fps + **เฟรมหาย 2-3/60 แบบเงียบ** |
| fps เต็มเฟรม (หลังตั้ง) | **23.65 · เฟรมหาย 0** | ✅ ได้ตามสเปก 24 fps |
| **ROI ครึ่งกลาง 1224×1024** | **69.0 fps · เฟรมหาย 0** | ✅ **คันโยกหลักของโหมดใหม่** — ที่ 12 ใบ/วิ ได้ ~5.5 เฟรม/ใบ (เท่ากล้อง CS016 ที่แผนเลือกไว้) และตรงกับ §7 ที่อยากให้ฝาเต็มเฟรม |
| binning | **ตั้งไม่ได้** (`0x80000109`) | ตัดออกจาก UI — ROI คือทางเดียว |
| แปลง Bayer→BGR | 7.7-10.6 ms/เฟรม ที่ 5MP | ใช้ BayerRG8 + แปลงบน PC (ขอ BGR8 จากกล้อง = แบนด์วิดท์ ×3) · ที่ ROI ครึ่งเหลือ ~2-3 ms |
| ExposureTime / Gain | **15–9,959,540 µs** / **0–23.98 dB** | เอาไปทำ min/max ของสไลเดอร์ได้ตรง |
| Gamma / DeviceTemperature | node เข้าไม่ถึง | ตัดออกจาก UI |
| **แสง @ gain 0** | 5000µs→**20.1**/255 · 200µs→**0.4**/255 | 🔦 **ต้องเพิ่มแสง ~200 เท่า** ถึงจะใช้ 200 µs ได้ ⇒ โหมดใหม่ตั้งต้นที่ exposure ยาว (ทดสอบบนโต๊ะ) และต้องโชว์ตัวเลขความสว่างให้จูนไฟ |

> ▶️ **เริ่มรอบหน้าตรงนี้ — เรียงตามลำดับที่ต้องทำ (25 ส.ค. 2026)**
>
> **① แก้ inference ที่ช้าลง 3-8 เท่า** (ปิดกั้นทุกอย่างที่เหลือ)
> ```
> py -3.9 diagnose_igpu.py                 # ไล่ทุกชั้น — บอกเลยว่าชั้นไหนขวางอยู่
> py -3.9 diagnose_igpu.py --fix           # ถ้าเป็นเรื่อง IR เสีย ให้มันซ่อมให้
> py -3.9 verify_openvino.py --weights weights\can_dent\bestX.pt --images sample_cans
> ```
> ⚠️ ข้อสุดท้าย **ห้ามข้าม** ทุกครั้งที่ IR ถูกสร้างใหม่ — "รันได้" ไม่ได้แปลว่า "ตรวจถูก"
> ✅ **เกณฑ์ว่าแก้สำเร็จ: `inf` บนแถบ perf กลับไป ~50 ms** (ตอนนี้ 155-378)
>
> **② วัด mm/px** (2 นาที — ปลดล็อกความแม่นของทุกตัวเลข)
> ปุ่ม **"ถ่าย 1 เฟรม"** พร้อมไม้บรรทัดที่ระนาบฝากระป๋อง → นับ 10 mm กินกี่พิกเซล
> → ใส่ `HIK_BURST_MM_PER_PX` และแก้ `HIK_BURST_LINE_SPEED_PX_S` ให้ตรง
>
> **③ วัด ROI ใหม่หลังแก้ ①** — กด "ครึ่งกลาง" vs "กว้าง-เตี้ย" แล้วอ่านกล่อง ⚡
> เอา `cam fps` ของทั้งสองมาคำนวณ **เฟรมเต็มใบ/กระป๋อง** ใหม่ แล้วค่อยตั้ง `HIK_DEFAULTS`
>
> **④ ถ่ายรัวเพื่อวัดความเบลอจริง** — ต้องโบกให้ได้ **≥1,950 px/วิ** (25% ของไลน์)
> ระบบถึงจะยอมสรุป · ทำที่ exposure 2165 / 1000 / 513 µs เทียบกัน
>
> **⑤ ตัดสินใจเรื่องไฟ** — ตัวเลขที่มีตอนนี้: ต้องเพิ่ม **~4.2 เท่า** เพื่อให้เบลอ 4 px
> ที่ความเร็วไลน์ (ยืนยันอีกครั้งหลังได้ mm/px จริงจากข้อ ②)

**⚠️ บั๊กที่เจอจากการรันจริงและแก้แล้ว (2 รอบ):** รอบแรกรายงานขึ้น `[0] ? SN=? (?)`
เพราะอ่าน struct ไม่ได้ **แล้วกลืน exception เงียบ ๆ** (ละเมิดกฎเหล็กข้อ 2 ตรง ๆ).
แก้ 2 ชั้น: (ก) พิมพ์เหตุผลออกมาเสมอ — ซึ่งรอบถัดมาชี้เป้าเองว่า
`'_MV_GIGE_DEVICE_INFO_' object has no attribute 'nMacAddrHigh'` ⇒ **SDK รุ่นนี้วาง MAC
ไว้บน `MV_CC_DEVICE_INFO` ชั้นนอก ไม่ใช่ใน `stGigEInfo`** (แก้ด้วย `getattr` สองชั้น) ·
(ข) **ยืนยันตัวตนจาก node ของกล้องเอง** (`DeviceModelName`/`DeviceSerialNumber`/
`GevCurrentIPAddress` ผ่าน `MV_CC_GetStringValue`) ซึ่งไม่ขึ้นกับ layout ของ struct เลย.
⇒ **`hik_camera.py` ต้องใช้ท่าเดียวกัน: อย่าเชื่อ struct ของ SDK เป็นแหล่งความจริงเดียว**
(`tests/fake_mvs/` ใช้ layout เดียวกับของจริงแล้ว มีเทสต์ล็อกไว้)

> 📜 *(ประวัติ — ทำเสร็จแล้ว)* ให้ผู้ใช้รันบนสถานี (**ปิดโปรแกรม MVS ก่อน** — GigE เปิดได้
> ทีละโปรแกรม) แล้วเอาผลมาตั้ง default ของโหมดใหม่:
> ```
> py -3.9 diagnose_hikrobot.py --save-dir hik_shots --json hik.json
> py -3.9 diagnose_hikrobot.py --exposure-scan 150,200,400,800,1600,2635,5000
> py -3.9 diagnose_hikrobot.py --fps-test
> ```
> สิ่งที่ต้องได้จากผล: packet size ที่เหมาะสม · **แพ็กเก็ต/เฟรมหาย = 0 หรือไม่** ·
> fps จริงที่เต็มเฟรม/ROI/binning · **ความสว่างเฉลี่ยที่ 150-200 µs** (ตัดสินว่าไฟพอไหม) ·
> ช่วง min/max ของทุก knob (เอาไปทำ UI)

**หมายเหตุการทดสอบ:** `tests/fake_mvs/MvCameraControl_class.py` คือ **MVS SDK ปลอม** —
ทำให้เส้นทาง Hikrobot ทั้งหมดรันได้บน container/CI ที่ไม่มีกล้องและไม่มี MVS.
เทสต์พิสูจน์แล้วว่า **จับปัญหาจริงได้** (MVS จองกล้องอยู่ · packet size เล็ก ·
แพ็กเก็ตหาย · เลขเฟรมกระโดด · ภาพมืด) ไม่ใช่ผ่านอย่างเดียว.

---

## 📍 สถานะงานก่อนหน้า (Artwork) — อ่านก่อนเริ่มงานต่อ (อัปเดต 19 ส.ค. 2026)

**Branch: `claude/artwork-mode-station-testing-llsrp0`** (ห้าม push main) ·
**commit ล่าสุด `88132a4`** · **CONFIG_VERSION: `2026.08.19-pixdiff`** ·
**pytest: 602 ผ่าน / 5 fail (pre-existing)** · `verify_artwork_features.py` 44/44 (บน container)

> 📜 *(งาน Artwork — คนละสายกับ Hikrobot)* pixel diff Phase 2 ยืนยันบนสถานีแล้ว เหลือข้อเดียวที่
> ยังไม่รู้คำตอบ — **รูป `pixdiff.png` ขึ้นบนหน้าเว็บจริงไหม** (ดูหัวข้อ
> "🏭 ยืนยันบนสถานีจริงแล้ว" ในส่วน pixel diff). งานที่ตั้งใจทำต่อคือ
> ใส่ `alt` ที่มีความหมาย + `onerror` เพื่อไม่ให้อาการนี้กำกวมอีก.

### โจทย์ตั้งต้นของรอบนี้
ผู้ใช้ถามว่า *"ถ้าผู้ใช้จริงวาดหลายโซนแล้วส่งตรวจ จะเจอความผิดพลาดอะไรบ้าง"* และ
*"ทำไมมีตัวอักษรแปลก ๆ โผล่มาทั้งที่ของจริงไม่มี"* → ไล่แล้วเจอ **3 จุดบอดที่ทำให้ผลตรวจ
ผิดแบบเงียบ** (ไม่ error, การ์ดขึ้นปกติ, แต่ผลใช้ไม่ได้) — แก้ครบแล้วทั้ง 3:

| # | จุดบอด | แก้ที่ | หัวข้อละเอียด |
|---|---|---|---|
| 1 | โซนเล็กถูกเรนเดอร์เล็กเกินไป → OCR อ่านไม่ออก (recall 1.2%) | `OCR_CROP_MIN_SIDE` | 🔎 คุณภาพการอ่าน |
| 2 | text layer เสียแต่เชื่อ `conf=1.0` | `PDFTEXT_GARBLED_CHECK` | 🔎 คุณภาพการอ่าน |
| 3 | **PASS ทั้งที่ชั้นเทียบข้ามแผงไม่เคยทำงาน** | `checks.check_coverage()` | 🧩 PASS ไม่ได้แปลว่าตรวจครบ |
| + | Gemini ครอบ ```` ```json ```` / N8N คืนหน้า HTML → ขยะกลายเป็น "ข้อความบนฉลาก" | `ocr_n8n._strip_fence` / `_looks_like_html` | 🔌 N8N |

### ✅ ทำเสร็จแล้ว (เรียงตาม commit)
| commit | เรื่อง |
|---|---|
| `7b5cf61` | OCR: เพิ่ม DPI ให้โซนเล็ก + ไม่เชื่อ text layer ที่เสีย |
| `810deef` | merge `origin/main` (login/ลงทะเบียน + ปุ่มแปลหลัก + N8N → 127.0.0.1) |
| `21c1b8b` | `_ocr_fingerprint()` เข้า cache key + `diagnose_n8n_ocr` รู้จัก "ทางที่ 6" |
| `e39a29e` | **`coverage`** — รายงานว่าชั้นไหนได้ตรวจจริง + เตือนตอนจัดโซน |
| `d460924` | **N8N**: ถอดรั้ว markdown · ปฏิเสธหน้า HTML · retry · `docs/N8N_OCR_PROMPT.md` |
| `20e3af1` | `verify_ocr`: เรนเดอร์ให้ตรง production + `--layers` |
| `008ca38` | `verify_ocr`: "ครบโควตา" ต้องไม่แสดงเป็น ERROR |
| `5d2f91a` | UX: verdict ตรง coverage · พอดีทั้งหน้า · วาดต่อเนื่อง · autosave โซน |
| `e45fff0` | N8N: กันโซนเดียวพังแล้วล้มการตรวจทั้งใบ (เจอจากทดสอบเชิงโจมตี) |
| `a99d31f` | แก้ CSS ที่ขาดบนหน้าประวัติ + เทสต์กันลืม |
| `f6602a4` | **`verify_artwork_features.py`** — ตรวจว่าเครื่องที่จะใช้งานจริงพร้อมไหม |
| `67d3f8f` | **pixel diff Phase 1** — โมดูล + เครื่องมือวัด + เทสต์ (ยังไม่แตะ UI) |
| `2479955` | pixdiff **โหมดโซน** — เทียบได้แม้หน้าคนละขนาด |
| `7b2ddda` `514a375` `dcd46e2` | วัด noise บนไฟล์จริง 11 ไฟล์ → **เบลอ 1.0 คือทางออก** |
| `88132a4` | **pixel diff Phase 2** — ใช้งานได้บนหน้าเว็บ + แก้ด่านสเกล |

### 🔬 pixel diff — **Phase 1 (วัดผลด้วย CLI)**

ผู้ใช้เลือก "วัดก่อนด้วย CLI แล้วค่อยทำ UI" และ "ขนาดหน้าไม่เท่ากัน = ไม่เทียบ + บอกเหตุผล".

- **`artwork_check/pixdiff.py`** — โมดูลใหม่ ไม่ import Flask · ไม่เขียนไฟล์เอง ·
  ไม่ถูกเรียกจาก `run_inspection` ⇒ **ยังไม่กระทบอะไรทั้งสิ้น**
- **`verify_pixdiff.py`** — `--selftest` (สร้าง PDF เอง ไม่ต้องมีไฟล์จริง) ·
  `--pair NEW OLD` · `--new-dir/--old-dir` (เทียบทั้งโฟลเดอร์ตามชื่อไฟล์) ·
  `--save-dir` เขียนภาพกรอบส้ม. exit `0`/`1`(พบความต่าง)/`2`
- **`tests/test_artwork_pixdiff.py` 45 ตัว** (23 ตอน Phase 1 → 45 หลังโหมดโซน + Phase 2)

**ตัวเลขที่วัดได้จริง (selftest ผ่าน 10/10):** ไฟล์เนื้อหาเดียวกัน → **0 พิกเซล
false positive** · จับได้ทั้ง **ตัวเลขเปลี่ยน / ขยับ 0.8pt / สีเปลี่ยน** ·
กรอบชี้เฉพาะแผงที่แก้จริง (แผงซ้าย-ขวาที่ไม่ได้แก้ไม่มีกรอบเลย) · **deterministic
รันซ้ำได้เลขเดิม** · **0.25 วิ/หน้า** ที่ 200 DPI (A4 → 2339x1653). ตรวจด้วยตาแล้ว
กรอบล้อม `85` ของ `NET WEIGHT 185 g` พอดี (เลข `1` ที่เหมือนกันไม่ติดกรอบ).

**ด่านความปลอดภัย 3 ชั้น (ทั้งหมดคือ "ไม่มั่นใจ = ไม่เทียบ" ตามกฎเหล็กข้อ 2):**
| ด่าน | เกณฑ์ | เหตุผล |
|---|---|---|
| ขนาดหน้า | `PAGE_SIZE_TOL_MM = 0.2` | ต่างแม้เศษ mm อาจแปลว่า **จัดเนื้อหาใหม่ทั้งหน้า** ⇒ ครอปแล้วทุกตัวอักษรเลื่อน = ต่างทั้งใบ (เคยวัดได้ 370 บริเวณปลอม) |
| ขนาดพิกเซล | `PIXEL_SIZE_TOL = 2` | **ต้องกว้างกว่า `PAGE_SIZE_TOL_MM` เมื่อแปลงเป็น px ที่ `PIXDIFF_DPI`** ไม่งั้นมีเคสที่ผ่านด่าน mm แล้วไปตกด่านพิกเซล = บอกเหตุผลผิดเรื่อง (มีเทสต์ล็อกไว้) |
| ต่างทั้งใบ | `MAX_DIFF_RATIO = 0.20` | ต่างเกิน 20% ของหน้า = คนละงาน/ทั้งหน้าเลื่อน ⇒ คืน `too_different` **ไม่พ่นกรอบเป็นร้อย** แต่ยังบอก % จริง |

**กับดักที่เจอตอนทำ (แก้แล้ว):** ค่าจาก numpy (`np.int32`/`np.float64`) ที่หลุดเข้า
`regions` ทำให้ **`json.dumps` โยน `TypeError` ตอนบันทึก `report.json`** — พังหลังบ้าน
แบบไม่มีใครเห็นจนกว่าจะถึงหน้าเว็บ. cast เป็น `float()`/`int()` หมดแล้ว + มีเทสต์คุม.

### 🧩 โหมดโซน — เพราะ **2 ใน 3 คู่ไฟล์จริงเทียบทั้งหน้าไม่ได้**

วัดบนสถานีด้วยไฟล์จริง (18 ส.ค.) แล้วพบว่าโหมดทั้งหน้าไม่พอ:

| คู่ที่ทดสอบ | ผล |
|---|---|
| ไฟล์จริง vs ตัวเอง (Star kist) | **0 พิกเซล / 0.63 วิ** ⇒ false positive floor ยืนยันบนไฟล์ production จริง |
| Final printer vs Original file | ต่าง **26.9%** → `too_different` (คนละ layout จริง) |
| Cosma A4 vs Original | **296x208 vs 757.6x454.8 mm** → `page_size_mismatch` |

⇒ `compare_zone(path_a, bbox_a, path_b, bbox_b)` — เทียบ **แผงต่อแผง** โดยเรนเดอร์
ทั้งสองที่ **สเกล mm เดียวกัน** (ไม่ใช่สัดส่วนของหน้า) แล้ว align ก่อนเทียบ:

- **ต้อง align เสมอในโหมดโซน** (ต่างจากโหมดทั้งหน้าที่ตำแหน่งมีความหมายจึงไม่ align) —
  ผู้ใช้ลากโซนด้วยมือ ขอบสองฝั่งไม่มีทางตรงกัน. ใช้โซนที่เล็กกว่าเป็น template
  หาตำแหน่งด้วย NCC (`MIN_MATCH_CONF=0.55`) · **คะแนนต่ำ = ไม่รายงาน**
- **การเลื่อนไม่ถูกซ่อน** — คืนใน `shift_mm` ให้ผู้ตรวจเห็นว่าเนื้อหาขยับไปเท่าไร
- **`ZONE_TOLERANCE_PX = 1` จำเป็น ไม่ใช่การผ่อนเกณฑ์** — แผงเดียวกันบนหน้าคนละ
  ขนาดตกลงบน "เศษส่วนพิกเซล" คนละค่า ⇒ ขอบตัวอักษรต่างกันทั้งแผง. วัดได้
  **13 บริเวณปลอม → 0** เมื่อเปิด tolerance ขณะที่ตัวเลขที่เปลี่ยนยังจับได้ 1 บริเวณ.
  `_diff_mask` ทำสองทางแล้วเอาค่ามากสุด ⇒ ผลไม่ขึ้นกับว่าไฟล์ไหนเป็น a หรือ b
- **`content_bbox()`** ตอบคำถามที่ตัดสินทุกอย่าง: *เนื้อหาข้างในขนาดจริงเท่ากันไหม*
  (เท่า = วางคนละที่บนแผ่นคนละขนาด เทียบได้เลย · ไม่เท่า = ไฟล์หนึ่งถูกย่อ)
  → `verify_pixdiff.py --auto-zone` ใช้ค่านี้หาโซนให้เอง **ไม่ต้องรู้พิกัด**

> ⚠️ **จุดอันตรายที่เจอตอนเขียนเทสต์: โซนว่าง vs โซนว่าง เคยขึ้น "ไม่พบความต่าง"**
> ทั้งที่ไม่ได้ตรวจอะไรเลย (template สีขาวล้วนจับคู่ได้คะแนนเต็มกับพื้นที่ขาวที่ไหน
> ก็ได้) = **ความมั่นใจปลอม** ตรงกับกฎเหล็กข้อ 2 เป๊ะ. ปิดด้วย `MIN_INK_RATIO`
> → `zone_blank` พร้อมบอก % หมึกจริงของทั้งสองฝั่ง.

### 📊 `pixdiff_noise_scan.py` — วัด noise ด้วย "ไฟล์เดียวกันเทียบตัวเอง"

`py -3.9 pixdiff_noise_scan.py --dir TEST [--save-dir img] [--out noise.json]`
ใช้ไฟล์เดียวกันทั้งสองฝั่ง ⇒ **ทุกบริเวณที่พบ = ของปลอม 100%** (มี ground truth
โดยไม่ต้องมีคู่เก่า/ใหม่) แล้วจงใจทำให้คลาดทีละแบบ: เลื่อน 0.25/0.5/1 px
(เรนเดอร์ที่ 4 เท่าแล้วย่อ = จำลองการตกบนเศษพิกเซลจริง) · สเกลเพี้ยน 0.2%/1% ·
พร้อม **สัญญาณจริง** (วาดทับ 4x2 mm ≈ ขนาดหนึ่งคำ) ที่ระบบต้องจับให้ได้เสมอ.
ไล่ทุกคู่ของ `tolerance × min_region` แล้วบอกว่าคู่ไหนใช้ได้.

### 🚨 ผลบนไฟล์จริง 11 ไฟล์ (18 ส.ค. 2026) — **หักล้างข้อสรุปจากไฟล์สังเคราะห์**

| กรณี | tol=0 | **tol=1** | tol=2 |
|---|---|---|---|
| ตรงเป๊ะ | **0 ทุกไฟล์** | **0 ทุกไฟล์** | **0 ทุกไฟล์** |
| เลื่อน **1.00 px** (จำนวนเต็ม) | 113-265 | **0 ทุกไฟล์** | **0 ทุกไฟล์** |
| เลื่อน **0.25 px** (เศษพิกเซล) | 64-250 | **20-356** ❌ | 19-50 |
| เลื่อน **0.50 px** (เศษพิกเซล) | 38-265 | **30-222** ❌ | 21-88 |
| สเกลเพี้ยน 0.2% | 38-173 | 21-137 | 18-145 |
| สัญญาณจริง 4x2 mm | 11/11 | **11/11** | 11/11 |

**ข้อสรุปที่เปลี่ยนจากรอบก่อน — ไฟล์สังเคราะห์ให้ภาพที่หลอก:**
ไฟล์สังเคราะห์ (ข้อความ/เส้นบนพื้นขาวล้วน) ให้ **0 บริเวณ** ที่ tol=1 ทุกเคส
จึงสรุปผิดว่า `ZONE_TOLERANCE_PX=1` แก้ noise การเลื่อนได้หมด. **ไฟล์งานจริง
ไม่เป็นแบบนั้น** เพราะมีภาพถ่าย/ไล่เฉด/พื้นสีทึบ ซึ่งการตกบนเศษพิกเซลทำให้
**ค่าสีของพิกเซลเปลี่ยนจริง ไม่ใช่แค่ขอบขยับ** — `dilate/erode` (ซึ่งแก้ได้แต่
"ตำแหน่ง") จึงดูดซับไม่หมด. เลื่อน **จำนวนเต็ม** พิกเซลยังได้ 0 ทุกไฟล์ (ยืนยันว่า
กลไก tolerance ทำงานถูก) แต่ **เศษพิกเซลไม่ใช่ปัญหาเชิงตำแหน่ง**.

> ⚠️ **บทเรียนสำหรับรอบหน้า: ห้ามสรุปเกณฑ์ของชั้นพิกเซลจากไฟล์ที่สร้างเอง**
> ต้องวัดบนไฟล์ artwork จริงเสมอ — ต่างกันคนละเรื่อง (0 vs 356 บริเวณ).

**อีกจุดที่ต้องระวังตอนอ่านผล:** tol=1 ให้ **จำนวนบริเวณมากกว่า** tol=0 ได้
(ไฟล์ Cosma Original: 250 → 356) ทั้งที่ `diff_px` ลดลง 8.7 เท่า (560k → 64k)
เพราะพื้นที่ต่างถูกตัดเป็นก้อนเล็กกระจาย ⇒ **ดู `diff_px` ควบคู่เสมอ**.

### ✅ ทางออกที่วัดได้แล้ว: **เบลอเบา ๆ ก่อนเทียบ (`blur_sigma = 1.0`)**

รันชั้น "เทียบแนวทาง" บนไฟล์จริง 11 ไฟล์ (เคสยากสุด = เลื่อน 0.5px):

| แนวทาง | noise สูงสุด/ไฟล์ | ไฟล์ที่ noise=0 | สัญญาณ 4x2mm |
|---|---|---|---|
| tol=1 (เดิม) | 222 | 0/11 | 11/11 |
| tol=2 | 88 | 0/11 | 11/11 |
| tol=1 + เบลอ 0.6 | 21 | 0/11 | 11/11 |
| **tol=1 + เบลอ 1.0** | **0** | **11/11** | **11/11** ✅ |
| **tol=2 + เบลอ 1.0** | **0** | **11/11** | **11/11** ✅ |
| tol=1 + เกณฑ์สี 96 | 3 | 7/11 | 11/11 |
| **tol=1 + เบลอ1 + สี64** | **0** | **11/11** | **11/11** ✅ |

**ทุกแนวทางที่ผ่านมี `blur 1.0` ทั้งหมด** ⇒ การเบลอคือกุญแจ ไม่ใช่ tolerance
หรือเกณฑ์สี. สอดคล้องกับต้นเหตุที่วัดได้: เศษพิกเซลเปลี่ยน **ค่าสี** ของพิกเซล
(ความถี่สูง) ซึ่ง Gaussian เบา ๆ ลบทิ้งได้ตรงจุด ส่วน dilate/erode แก้ได้แต่ตำแหน่ง.

**⛔ ไอเดียที่วัดแล้วปฏิเสธ — sub-pixel alignment (ประหยัดงานไปมาก):**
วัดว่า *ถ้า* อัปเกรด `_locate()` ให้ align ละเอียดระดับเศษพิกเซลจะช่วยแค่ไหน →
**tol=1 เหลือ 258 บริเวณ (แย่กว่าเดิมที่ 222)**. ยืนยันว่าปัญหา**ไม่ใช่ตำแหน่ง**
⇒ ไม่ต้องลงทุนทำ phase-correlation / parabolic peak fitting เลย.

**⚠️ ด่านสุดท้ายก่อนล็อกค่า: การเบลอทำให้ตาบอดต่อความต่างเล็ก ๆ ไหม**
เพิ่มชั้น `SIGNAL_SIZES_MM` วัดความไวตั้งแต่ 4x2 mm ลงไปถึง 0.3x0.3 mm
(จุดทศนิยมหาย · ® หาย · ตัวอักษรเดียวเปลี่ยน) — ถ้าเบลอ 1.0 ทำให้มองไม่เห็น
จุดทศนิยม นั่นอันตรายกว่า noise มาก (กฎเหล็กข้อ 2).
> 💡 ที่ 200 DPI: 0.6x0.6 mm = **22 px²** ซึ่ง **เล็กกว่า `MIN_REGION_PX=40`**
> ⇒ ถูกตัดทิ้งอยู่แล้วไม่ว่าจะเบลอหรือไม่. ถ้าต้องจับของขนาดนี้ **ต้องเพิ่ม DPI**
> ไม่ใช่ลดการเบลอ — สคริปต์พิมพ์ px² ของทุกขนาดให้เทียบกับเกณฑ์ตรง ๆ แล้ว.

**ผลที่วัดได้ (ชุดทดสอบสังเคราะห์ 3 แบบ: ข้อความหนาแน่น / ตาราง / กราฟิก) —
เก็บไว้เป็นบทเรียนว่าทำไมถึงหลอก:**

| ความคลาด | tol=0 | **tol=1** | tol=2 |
|---|---|---|---|
| ตรงเป๊ะ | 0 | 0 | 0 |
| เลื่อน 0.25 px | 29-120 บริเวณ | **0** | 0 |
| เลื่อน 0.50 px | 29-120 | **0** | 0 |
| เลื่อน 1.00 px | 29-120 | **0** | 0 |
| **สเกลเพี้ยน 0.2%** | 77-96 | **54-239** | 35-206 |
| สัญญาณจริง 4x2 mm | จับได้ 6/6 | **จับได้ 6/6** | จับได้ 6/6 |

**ข้อสรุป 2 ข้อที่เปลี่ยนการออกแบบ:**
1. ~~`ZONE_TOLERANCE_PX = 1` ฆ่า noise จากการเลื่อนได้หมดทุกไฟล์~~
   ⚠️ **ข้อนี้ผิด — ใช้ได้เฉพาะไฟล์สังเคราะห์** ดูหัวข้อ "ผลบนไฟล์จริง 11 ไฟล์"
   ด้านบน (ของจริงเหลือ 20-356 บริเวณ). ที่ถูกคือ tolerance ฆ่าได้เฉพาะการเลื่อน
   **จำนวนเต็มพิกเซล**.
2. **สเกลเพี้ยนต้องเป็น "ด่านปฏิเสธ" ไม่ใช่ "กรองทีหลัง"** — ไล่ `min_region`
   ทุกค่าแล้ว **ไม่มีค่าไหนกรอง noise จากสเกลออกได้โดยไม่ทิ้งสัญญาณจริงไปด้วย**
   (`min_region=800` ทำให้ noise เป็น 0 ก็จริง แต่สัญญาณหายหมด 0/6).
   ⚠️ ที่ต้องระวัง: **tol=1 ให้ "จำนวนบริเวณ" มากกว่า tol=0 ในเคสสเกล** (239 vs 96)
   เพราะพื้นที่ต่างถูกตัดเป็นก้อนเล็กกระจาย — ดู `diff_px` ควบคู่เสมอ อย่าดูแต่จำนวน.

⇒ `compare_zone()` จึงมีด่าน **`scale_mismatch`** ก่อนจับคู่ตำแหน่ง (align แก้ได้แค่
การเลื่อน ไม่ได้แก้การย่อ/ขยาย). เกณฑ์คิดจากพิกเซลจริงด้วย
`scale_allowance(zone_px) = ZONE_TOLERANCE_PX / zone_px` — โซน 800px ยอมได้ ±0.125%.
> ⚠️ `_ink_extent()` **คืน (0,0) เมื่อหมึกชนขอบภาพ** = วัดสเกลไม่ได้ (เนื้อหาถูกโซนตัด).
> เคยพลาดมาแล้ว: โซนที่ลากชิดขอบแผงพอดีอ่านสเกลได้ 0.93 เท่าทั้งที่เท่ากันเป๊ะ
> แล้วระบบปฏิเสธการเทียบทั้งที่เทียบได้ — มีเทสต์ล็อกไว้.

**ยืนยันบนไฟล์จริง:** คู่ Cosma → เนื้อหา **218x134 mm vs 746x447 mm (0.292x)** =
ไฟล์ A4 เป็นฉบับย่อ ~29.5% ⇒ ระบบปฏิเสธถูกต้องแล้ว (คะแนนจับคู่ 0.38 < 0.55).

### ✅ Phase 2 เสร็จแล้ว — ใช้งานได้บนหน้าเว็บ (`CONFIG_VERSION 2026.08.19-pixdiff`)

ปุ่ม **"🔍 เทียบภาพเก่า/ใหม่"** ข้างปุ่มส่งตรวจสอบ · จับคู่โซนด้วย `group`
(ชุดเดียวกับชั้นเทียบข้ามไฟล์ ⇒ ไม่ต้องเรียนกติกาใหม่) · การ์ดผลอยู่ใต้แถบ
coverage · คลิกรูปขยายได้ (lightbox เดิม).

| ส่วน | ของใหม่ |
|---|---|
| `POST /api/artwork/<id>/pixdiff` | **ต้องกดเอง** ไม่ถูกเรียกจาก `run_inspection` |
| `GET /api/artwork/<id>/pixdiff.png?zid=` | ภาพโซน + กรอบส้ม (display-only) |
| `pipeline.run_pixdiff/load_pixdiff/pixdiff_zone_png` | เขียนแยกที่ **`pixdiff.json`** |
| `awPixdiffHtml()` + `.aw-pd*` | CSS ครบ **ทั้ง 2 template** |

**ไม่แตะ `report.json` / `defects` / `verdict` / `summary` / การนับ / DB เลย** —
ผล pixdiff ถูกแนบ **ตอนตอบ `/report` เท่านั้น** (แบบเดียวกับชื่อผู้ตรวจ) ⇒ หน้า
ประวัติแสดงได้โดยไม่ปนกับผลตรวจ QC. มีข้อเช็คใน `verify_artwork_features.py`
ยืนยันว่า "เทียบภาพแล้ว report.json ต้องไม่ถูกสร้าง".

> ⚠️ **เทสต์ CSS เดิมมีช่องโหว่ที่เพิ่งอุด** — `test_artwork_report_css.py`
> สแกนเฉพาะ body ของ `renderReport()` ⇒ คลาสของ `coverageHtml` (`.aw-cov*`)
> **หลุดการตรวจมาตลอด** ทั้งที่เป็นเคสที่ทำให้เขียนเทสต์นี้ตั้งแต่แรก. ตอนนี้
> สแกนฟังก์ชันช่วยที่ `renderReport` เรียกต่อด้วย (`_HELPERS`) → 13 → 31 ตัว
> และพิสูจน์แล้วว่าจับได้จริง (ลบ `.aw-pd-dot` ออกจากหน้าประวัติ → เทสต์แดง).

**⚠️ ด่านสเกลที่แก้ไปแล้ว (เคยปฏิเสธคู่ที่เหมือนกันเป๊ะ):** เดิมตั้งเกณฑ์
ละเอียด (ขอบเลื่อน ≤1px ≈ 0.16% ของโซน 615px) แต่ **การวัดขนาดหมึกด้วย
bounding box มีความคลาด ±1px อยู่แล้ว** ⇒ แผงเดียวกันวัดได้ 615x421 กับ
614x420 = 1.0016 เท่า ซึ่งเกินเกณฑ์ 0.00163 ไปนิดเดียว → ระบบปฏิเสธการเทียบ
ทั้งที่เทียบได้. **เครื่องมือวัดหยาบกว่าเกณฑ์ที่มันต้องบังคับ**. แก้โดยแยก 2 เรื่อง:
- **สเกลต่างมาก** (A4 proof ย่อ 29.5%) → `GROSS_SCALE_TOL = 0.05` + คะแนน NCC
- **สเกลต่างเล็กน้อย (0.1-0.5%)** = **ความต่างจริงของงาน ไม่ใช่ noise** — สองไฟล์
  เรนเดอร์ที่สเกล mm เดียวกันจากต้นฉบับ vector ถ้าเนื้อหาขนาดไม่เท่ากันแปลว่า
  artwork ถูกย่อ/ขยายจริง ⇒ **ต้องรายงาน ไม่ใช่ซ่อน** (คืน `scale_ratio` เสมอ)

**⚠️ บั๊กในเครื่องมือวัดของตัวเอง (เจอตอนทำ Phase 2):** `_rescaled_pair()` ใน
`pixdiff_noise_scan.py` เดิม **ขยายแล้วย่อกลับเป็นขนาดเดิม** ⇒ เนื้อหากลับไป
อยู่ตำแหน่งเดิมทุกจุด = วัดได้แค่ "ความนุ่มจากการ resample" **ไม่ใช่การคลาด
จากสเกลเลย**. แก้เป็นครอปจากมุมเดียวกัน (เนื้อหาที่ระยะ d เลื่อน d×pct).
วัดใหม่แล้วได้ข้อสรุปที่ต่างออกไป: **การเบลอ *ไม่* ช่วยกับสเกลที่คลาดจริง**
(0.1% → 28-84 บริเวณ ทั้งเปิดและปิดเบลอ) ต่างจากการเลื่อนที่เบลอช่วยได้หมด.

### 🏭 ยืนยันบนสถานีจริงแล้ว (19 ส.ค. 2026) — Phase 2 ใช้งานได้

ผู้ใช้กดปุ่มบนหน้าเว็บของสถานีแล้วได้ผลจริง:

> 🔍 เทียบภาพเก่า/ใหม่ระดับพิกเซล — **พบความต่าง 1 โซน / เทียบได้ 1 โซน** ·
> ไม่พบความต่าง 0 · **เทียบไม่ได้ 0** · ที่ 200 DPI · **ใช้เวลา 0.23 วินาที**
> ⚠️ โซน 1 · กลุ่ม A · **ต่าง 1 บริเวณ (4425 พิกเซล)** · **เนื้อหาขยับ 0,0 mm**

อ่านผลนี้ได้ว่า **ทุกด่านที่กลัวว่าจะพังบนของจริง ผ่านหมด**: จับคู่โซนด้วย `group`
ทำงาน (เทียบได้ 1 จาก 1) · ไม่ตกด่าน `page_size_mismatch`/`scale_mismatch`/
`zone_blank`/`align_failed` (เทียบไม่ได้ = 0) · align แม่นจนขยับ **0,0 mm** ·
ความเร็วบนสถานีตรงกับที่วัดใน CLI (0.23 วิ ต่อการเทียบ 1 โซน).

**⚠️ ข้อเดียวที่ยังไม่ได้ยืนยัน — เปิดงานรอบหน้าด้วยข้อนี้:**
ผลที่ผู้ใช้ paste มาจบด้วยคำว่า `pixdiff` โดด ๆ ซึ่งคือค่า **`alt` ของ `<img>`**
ในการ์ด ⇒ **แยกไม่ออกระหว่าง "ก๊อปข้อความมาแล้วรูปไม่ติดมาด้วย" กับ "รูปโหลด
ไม่ขึ้นจริง"** (`GET /api/artwork/<id>/pixdiff.png?zid=` ล้มเหลว = เบราว์เซอร์
แสดง alt แทน). ยังไม่ได้แก้โค้ด — ตั้งใจไว้ว่าจะ:
- เปลี่ยน `alt="pixdiff"` เป็นข้อความไทยที่มีความหมาย (เช่น
  `alt="ภาพโซน <label> พร้อมกรอบส้มชี้บริเวณที่ต่าง"`)
- เพิ่ม `onerror` ให้ขึ้นข้อความว่า **โหลดภาพไม่สำเร็จ** แทนที่จะเงียบ ⇒ ครั้งหน้า
  อาการนี้จะ **บอกตัวเองได้** ไม่ต้องมาเดา (ตรงกฎเหล็กข้อ 2: ไม่มั่นใจต้องบอก)

> 💡 วิธีเช็คเร็วสุดบนสถานี: เปิด DevTools แท็บ Network แล้วกดปุ่มเทียบภาพ —
> ถ้า `pixdiff.png?zid=z1` เป็น 200 + มีขนาดไฟล์ = รูปมาจริง (แค่ paste ไม่ติด),
> ถ้า 404/500 = ต้องไล่ที่ `pipeline.pixdiff_zone_png()` (bbox ที่เก็บใน
> `pixdiff.json` หรือสิทธิ์เจ้าของ).

### 🔜 บริบทตอนตัดสินใจเริ่มงานนี้ (เก็บไว้เป็นประวัติ — **ทำเสร็จหมดแล้ว**)
ตอนนั้นผู้ใช้ตอบว่า **"เก็บไว้เป็นแผนถัดไป"** (มีไฟล์ฉบับที่อนุมัติแล้ว).
สิ่งที่ทดลองไว้ก่อนลงมือจริง:
- เรนเดอร์ PDF 2 ฉบับที่ DPI เดียวกันแล้ว `absdiff` → **deterministic 0 พิกเซล**
  (false positive 0) · จับความต่างเล็กถึง **0.8pt** ได้ · ~0.29 วิ/หน้า ที่ 300 DPI
- **ข้อจำกัดที่ต้องแก้ก่อนใช้จริง: ขนาดหน้าต้องเท่ากัน** — ย่อ/ขยายแบบ naive แล้ว
  เทียบได้ **370 บริเวณปลอม** ⇒ ต้อง align/normalize (หรือบังคับให้สองไฟล์เป็น
  หน้าขนาดเดียวกัน) ก่อนเสมอ
- จุดแข็ง: ไม่ง้อ OCR / ไม่ง้อภาษา / ไม่ง้อการลากโซน และจับได้มากกว่าข้อความ
  (ฟอนต์เปลี่ยน สีเพี้ยน โลโก้ขยับ แท่งบาร์โค้ดเปลี่ยน)
- ต้องเป็น **โหมดใหม่แยก** ไม่ใช่แทนที่ของเดิม และ **advisory ก่อน** (กฎเหล็กข้อ 1)

> ✅ ทั้ง 4 ข้อปิดครบแล้ว: ข้อจำกัด "ขนาดหน้าต้องเท่ากัน" → ด่าน
> `page_size_mismatch` + **โหมดโซน** (เรนเดอร์ที่สเกล mm เดียวกันแล้ว align) ·
> "370 บริเวณปลอม" ที่กลัว → วัดจริงบนไฟล์สถานีได้ **0 บริเวณ** ·
> โหมดแยก + advisory → ปุ่มแยก ไม่แตะ `report.json`/`verdict`/DB.

### ⚠️ กติกาที่รอบนี้ยึด และรอบหน้าต้องยึดต่อ
- ทุกอย่างที่เพิ่มเป็น **advisory 100%** — ไม่แตะ `defects` / `verdict` / การนับ / DB
- flag ใหม่ทุกตัว **ปิดแล้วได้พฤติกรรมเดิมเป๊ะ** (พิสูจน์ด้วย md5 ของ crop และเทสต์)
- **ไม่แตะโหมด** Live / RTSP / STREAM / Snapshot / Label เลยแม้แต่บรรทัดเดียว

### ✅ ผลทดสอบซ้ำแบบอิสระ (18 ส.ค. 2026, branch `claude/artwork-mode-station-testing-llsrp0`)

ทดสอบ **บน container Linux + Chromium จริง** (ไม่ใช่สถานี Windows) โดยรัน `app.py`
ตัวจริงพร้อม stub `ultralytics`/`pyodbc` — ดูวิธีในหัวข้อ "🧪 วิธีทดสอบ UI จริง" ด้านล่าง.
ทั้ง 6 ข้อผ่านหมด **วัดจาก DOM/สีจริง ไม่ใช่การอ่านโค้ด**:

| # | สิ่งที่วัด | ผลที่ได้จริง |
|---|---|---|
| 1 | 3 โซนคนละกลุ่ม → แถบเตือน | `aw-cov warn`, `rgb(255,248,225)` = เหลือง, "ไม่มีโซนใดตั้ง กลุ่ม ตรงกันเลย" |
| 2 | ตั้ง 2 โซนเป็นกลุ่ม `A` | แถบเทา `rgb(248,250,252)` + "ทำงาน (กลุ่ม A)" + จับ `MISMATCH_PANELS` 2 (170 vs 185 g) |
| 3 | verdict ตาม coverage | มีชั้นขาด → "✅ PASS — **ไม่พบประเด็นในชั้นที่ตรวจ**" · ไม่มีชั้นขาด → "✅ PASS — ไม่พบประเด็น" |
| 4 | autosave + F5 | แถบเขียว `rgb(232,245,233)` "💾 พบงานที่ค้างไว้ — 3 โซน" · กู้คืนได้ครบ **ทั้ง id และกลุ่ม** (`z1[A] z2[A] z3[C]`) · "ทิ้ง" ล้าง localStorage แล้วไม่เสนออีก |
| 5 | หน้าประวัติ | `.aw-img-pair` = `grid 551px 551px`, การ์ดมีกรอบ, `scrollWidth == innerWidth` (ไม่ล้นจอ), แถบ coverage แสดงครบ |
| 6 | ปุ่มซูม + วาดต่อเนื่อง | พอดีความกว้าง 86% (ยังล้นแนวตั้ง) · **พอดีทั้งหน้า 63% (ไม่ล้นทั้งสองแกน)** · ติ๊กแล้วลากรวด 3 โซนโดยกดปุ่มครั้งเดียว · Esc ออกได้ · ไม่ติ๊ก = พฤติกรรมเดิมเป๊ะ |
| + | **N8N ล่ม 1 โซน ไม่ล้มทั้งใบ** (`e45fff0`) | โซนพื้นที่ว่างยิง N8N ที่ต่อไม่ติด → `UNREADABLE` 1 รายการ **แต่ชั้นเทียบข้ามแผงยังทำงานและรายงานออกครบ** |

`diagnose_n8n_ocr.py --ping-only` แสดง `N8N_OCR_RETRIES` / `N8N_OCR_STRICT_RESPONSE`
ครบ และ retry ทำงานถูกต้อง (ต่อไม่ติด → ลองซ้ำ 1 ครั้ง แล้วรายงานสาเหตุตรงจุด).
pytest: **539 ผ่าน / 9 skipped / 5 fail** (5 ตัวเดิมของ Label Paper `NameError: FieldResult`).
ระหว่างทดสอบทั้งหมด **ไม่มี HTTP 500 และไม่มี JS error** (404 เดียวคือ `/favicon.ico` ตามปกติ).

**⚠️ ทดสอบที่นี่ไม่ได้ ต้องทำบนสถานี:** ยิง N8N จริง (`--ping-only` ข้อ ②) ·
`verify_ocr.py --layers probe` กับโฟลเดอร์ `TEST` · ทุกโหมดที่ใช้กล้อง/SQL Server.

### 🧰 `verify_artwork_features.py` — "เครื่องนี้พร้อมใช้งานจริงไหม" (รันบนสถานีได้เลย)

`py -3.9 verify_artwork_features.py [--n8n] [--only B,C] [--verbose]` — **อ่านอย่างเดียว**
(ชั้น B ใช้ temp dir ไม่แตะ `data/`), ไม่ต้องมีกล้อง/SQL Server/เบราว์เซอร์.
ต่างจาก `pytest` ตรงที่ตอบว่า *เครื่องที่จะใช้งานจริงพร้อมไหม* — วัดจากโค้ดบนดิสก์,
คอนฟิกที่ resolve ได้จริง, แพ็กเกจที่ติดตั้งจริง และเรียก `pipeline.run_inspection` ตัวจริง.
6 ชั้น: **A** commit ครบ 10 ตัว + flag ใหม่ 4 ตัว + `pyspellchecker` (จุดบอด QC ถ้าไม่มี) ·
**B** coverage/MISMATCH_PANELS/verdict จาก PDF 3 แผงที่สร้างสด + จำลอง backend ระเบิด ·
**C** แกะคำตอบ N8N 10 แบบผ่าน **mock HTTP server ในเครื่อง** (ยิงผ่าน `requests` จริง
รวม retry 500 / ไม่ retry 404 / URL ผิดรูป) · **D** text layer เสีย + เพิ่ม DPI โซนเล็ก ·
**E** ของที่ "พังเงียบ": CSS ครบ 2 template, `ZOOM_MIN`↔`min=`, `HL_*`, `GROUP_LETTERS`,
id ที่ JS อ้าง, ตำแหน่ง guard · **F** ยิง N8N จริง (`--n8n`).
exit `0`/`1`/`2`. **พิสูจน์แล้วว่าจับของจริงได้** ไม่ใช่ผ่านอย่างเดียว: รันบน worktree
ที่ `d460924` (ก่อนงาน UX) → **ไม่ผ่าน 8 ข้อ** ตรงจุด (commit ขาด, CSS หน้าประวัติ,
`#awZoomFitPage`/`#awDrawContinuous`/`#awRestore` หาย, ข้อความ verdict, autosave) ·
รันด้วย `N8N_OCR_STRICT_RESPONSE=0` → จับได้ว่าหน้า HTML จะถูกใช้เป็นข้อความ.
> ⚠️ ตัวอย่างข้อความในชั้น D ต้องมี **คำยาว ≥ 8 ตัวอักษร อย่างน้อย 8 คำ** ไม่งั้น
> `text_looks_garbled` จะไม่ยอมตัดสินเลย แล้วทั้งเคสดี/เคสเสียได้ `False` เหมือนกัน =
> ผ่านแบบไร้ความหมาย (มีข้อเช็คกันไว้ในสคริปต์แล้ว).

### 🏭 ผลรันบนสถานีจริง (18 ส.ค. 2026, Python 3.9.13, `f6602a4`)

`py -3.9 verify_artwork_features.py --n8n` → **ผ่าน 57 / ไม่ผ่าน 0** ·
footer/`CONFIG_VERSION` = `2026.08.16-aw-ux` · commit ครบ 10 ตัว · `pyspellchecker` มี.
**N8N ทำงานถูกต้อง**: ยิงจริง HTTP 200, อ่านภาพทดสอบได้ (`DIAGNOSE 12345`),
และ **คืน `blocks` พร้อม bbox 2 ก้อน**.

**⚠️ ข้อเดียวที่ยังเปิดอยู่ — tesseract binary ยังไม่ได้ติดตั้งบนสถานี**
(`pytesseract` รายงาน *"tesseract is not installed or it's not in your PATH"*).
ไม่ใช่จุดบอด QC (ผลตรวจ PASS/FAIL เท่าเดิมทุกอย่าง) แต่ทำให้ **กรอบแดงหายไปมากกว่าที่คิด**
เพราะชั้น ③ ต้องใช้ Tesseract "พิสูจน์" bbox ของ LLM:
`_verify_boxes(..., require_positive=True)` มี `except: return [] if require_positive`
⇒ **ไม่มี Tesseract = bbox ของ Gemini ถูกทิ้งทุกครั้ง** แม้ N8N จะคืน bbox มาให้แล้วก็ตาม.

| ไฟล์ | ชั้นที่ใช้ได้ตอนนี้ | กรอบแดง |
|---|---|---|
| PDF ที่มี text layer | ① PDF word box | ✅ ขึ้นปกติ |
| PDF outline / ภาพถ่าย | ② ไม่มี · ③ ถูกทิ้งเพราะพิสูจน์ไม่ได้ | ❌ **ไม่ขึ้นเลย** |

แก้ด้วยการติดตั้ง UB-Mannheim installer + `py -3.9 -m pip install pytesseract`
(+ `ARTWORK_HIGHLIGHT_TESS_LANG=eng+ara` ถ้ามีฉลากอาหรับ) — `_find_tesseract_cmd()`
หา exe เองไม่ต้องตั้ง PATH.

### 🎯 วัด hallucination ของ Gemini บนไฟล์จริง (18 ส.ค. 2026)

`verify_ocr.py --files TEST --engines n8n --layers probe --n8n-limit 25` (11 ไฟล์จริง):
**โซนที่แต่งขึ้น 0/25 · ยิงสำเร็จ 25 / ล้มเหลว 0** — ทุกโซนว่างคืน **0 ตัวอักษร**
(response 51 bytes = `{"text":"","blocks":[]}`) ⇒ **prompt ที่ใช้อยู่ผ่านข้อที่อันตรายที่สุด
ของงาน QC แล้ว ไม่ต้องแก้เรื่อง "ห้ามเดา"**. เท่ากับ Tesseract ที่เคยวัดได้ 0/36.
- ⚠️ verdict พิมพ์ว่า **"สรุปไม่ได้"** = ถูกต้องตามออกแบบ (ข้าม GROUND TRUTH ตาม
  `--layers probe`) **ไม่ใช่ความล้มเหลว** — ยังไม่ได้วัด recall/precision.
  ปิดช่องนี้ด้วยรันที่สอง: `--layers truth --n8n-limit 30`
- โควตา: 11 ไฟล์ × 3 โซน = 33 ⇒ `--n8n-limit 25` ทำให้ **8 โซนสุดท้ายถูกข้าม** (ใช้ 33 ถ้าอยากครบ)

**ข้อค้นพบจากชั้น TRIAGE ที่กระทบการใช้งานจริง** — แบนเนอร์ "✅ ไฟล์นี้มี text layer"
เป็น **ระดับหน้า** แต่ความจริงคือ:

| ไฟล์ | text layer ปกคลุมหน้า | สิ่งที่เกิดขึ้นจริง |
|---|---|---|
| 4 ไฟล์ | **0%** | outline ทั้งใบ — ทุกโซนยิง N8N |
| 3 ไฟล์ | **1-6%** | แบนเนอร์บอกว่า "มี" แต่**เกือบทุกโซนยังยิง N8N** |
| 4 ไฟล์ | 10-30% | ผสมกัน — ต้องดูรายโซน |

⇒ งานจริงส่วนใหญ่วิ่งผ่าน N8N ไม่ใช่ text layer ⇒ (ก) โควตา/เวลา Gemini คือคอขวดจริง
(ข) **การไม่มี Tesseract กระทบมากกว่าที่คิด** เพราะไฟล์ 0% พวกนี้คือกลุ่มที่กรอบแดงหายทั้งหมด.

### ⚖️ N8N (Gemini) vs Tesseract บนเฉลยชุดเดียวกัน (18 ส.ค. 2026)

**ติดตั้ง Tesseract v5.5.3 บนสถานีแล้ว** (`eng+ara+deu`) ⇒ **ช่องว่างกรอบแดงปิดแล้ว**
(ชั้น ② กลับมาใช้ได้ และชั้น ③ พิสูจน์ bbox ของ Gemini ได้แล้ว).

`verify_ocr.py --engines tesseract,n8n --layers truth --zones 4 --n8n-limit 30`
→ 13 โซนจาก 6 ไฟล์ที่มี text layer (อีก 5 ไฟล์ outline ทั้งใบ ไม่มีเฉลย):

| engine | recall | precision | ผล |
|---|---|---|---|
| **n8n (Gemini)** | **98.4%** | **95.8%** | ✅ ผ่านทั้งสองเกณฑ์ (≥95%) |
| tesseract | 92.0% | 89.6% | ❌ ไม่ผ่าน |

**ความล้มเหลวของ Tesseract กระจุกอยู่ที่ 2 โซนเท่านั้น** (โซน 3 ของไฟล์ Cosma ทั้งสองฉบับ:
75.4%/68.1% และ 78.9%/79.1% เทียบกับ n8n 96.1% และ 100%) — โซนที่เหลือสูสีกันมาก
(98.0/97.3 · 100/99.5 · 100/94.4 · 100/100). **ตั้งข้อสังเกตว่าเป็นเรื่องภาษา ไม่ใช่คุณภาพ engine**:
เฉลยของโซนนั้นมีคำโปแลนด์ (`SKŁAD` = ส่วนผสม) แต่รันนี้โหลดแค่ `eng+ara+deu`
⇒ **ยังไม่ยืนยัน** ปิด loop ด้วย `--tess-lang eng+ara+deu+pol+ces+hun+slk` (และลอง `--tess-psm 3`).

**⚠️ ตัวเลข 92% ของ Tesseract ไม่ได้แปลว่าผลตรวจแย่ลง** — production **ไม่เคยใช้ Tesseract
อ่านข้อความ** (ใช้ text layer หรือ N8N เท่านั้น ดู `ocr.read_zone`). Tesseract มีหน้าที่
**วัดตำแหน่งคำเพื่อวาดกรอบแดง** (ชั้น ②/③) ซึ่งเป็น display-only. ⇒ ข้อสรุปเชิงนโยบาย:
**ใช้ N8N เป็น engine อ่านต่อไป · Tesseract คงบทบาทชั้นกรอบแดง · อย่าเอามาแทนกัน**.

**⚠️ อ่านคอลัมน์ "เฉลย" อย่างระวัง** — `[?] เฉลยจาก text layer มีคำผิดรูป` บางครั้งเป็น
**false alarm**: รหัสงานจริงบนฉลาก (`3AAOSO34JARNTEA82W`, `5F0JU260N000002603`) หน้าตา
เหมือน token ที่ฟอนต์แมปผิดพอดี. ในไฟล์ [1] n8n อ่านรหัสพวกนี้ได้ครบ 100% ส่วน Tesseract
ตก 2 คำ (85.7%) ⇒ ตรงนั้นเฉลย**ถูก**แล้ว ไม่ใช่เฉลยเสีย.

### 🔀 กับดัก branch: `main` **ไม่มี** 5 commit สุดท้ายของรอบที่แล้ว
PR #37 ถูก merge ตอน branch อยู่ที่ `d460924` ⇒ `origin/main` (`90430b7`) ยังเป็น
**`CONFIG_VERSION = 2026.08.15-n8n-parse`** และ **ไม่มี** `20e3af1` `008ca38` `5d2f91a`
`a99d31f` `e45fff0` (verify_ocr `--layers` · UX 4 ตัว · CSS หน้าประวัติ · กันโซนเดียวล้มทั้งใบ).
⇒ **ถ้าสถานี `git pull` จาก `main` แล้ว footer จะขึ้น `2026.08.15-n8n-parse` ตลอด
ไม่ว่ารีสตาร์ตกี่รอบ** — ต้อง checkout branch ที่มีงานจริง (`claude/artwork-multi-zone-errors-k8linm`
หรือ `claude/artwork-mode-station-testing-llsrp0` ซึ่ง merge ไว้แล้ว) หรือรอ merge เข้า main.

---

## 🧪 วิธีทดสอบ UI จริงบนเครื่อง dev (ไม่มี ultralytics/กล้อง)

`app.py` import `ultralytics` → เครื่อง dev ที่ไม่มี torch รันไม่ได้. แต่โหมด Artwork
เป็น blueprint แยก จึงยกมารันเดี่ยวได้ **โดยใช้ template/static/route ตัวจริง**:

```python
# scratchpad/aw_server.py
import sys; sys.path.insert(0, "/path/to/Digital_Vision2026")
from flask import Flask
from artwork_check.routes import artwork_bp
import config as appcfg
ROOT = "/path/to/Digital_Vision2026"
app = Flask(__name__, template_folder=ROOT+"/templates", static_folder=ROOT+"/static")
@app.context_processor
def _ctx():                      # base.html ต้องการตัวแปรพวกนี้
    return {"config_version": appcfg.CONFIG_VERSION, "current_user": None,
            "auth_enabled": False, "has_perm": lambda *a, **k: True}
app.register_blueprint(artwork_bp)
app.run(host="127.0.0.1", port=5990, threaded=True)
```
แล้วขับด้วย Playwright + Chromium ที่มีอยู่ในเครื่อง
(`executable_path="/opt/pw-browsers/chromium-1194/chrome-linux/chrome"`).

**อีกท่าที่ตรงกับ production มากกว่า (ใช้ในรอบทดสอบ 18 ส.ค.):** รัน `app.py` **ตัวจริง**
โดย stub เฉพาะแพ็กเกจที่เครื่อง dev ไม่มี แทนที่จะยก blueprint มารันเดี่ยว —
ได้ทดสอบ `base.html`/context processor/ลำดับการลงทะเบียน blueprint ตัวจริงไปด้วย:
```bash
mkdir -p stubs                      # ultralytics.py = class YOLO ที่ raise, pyodbc.py = connect() ที่ raise
PYTHONPATH=stubs AUTH_ENABLED=0 FLASK_PORT=5000 python3 app.py
```
`load_model()` / `Database` มี fallback อยู่แล้ว จึงได้แค่ log ERROR แล้วเว็บขึ้นปกติ
(โหมด Artwork ไม่แตะทั้งสองอย่าง). `AUTH_ENABLED=0` เพราะ auth ต้องใช้ SQL Server จริง.
⚠️ ไม่มี cert ⇒ `USE_HTTPS=True` จะ fallback เป็น HTTP เอง (ตามโค้ดใน `app.py`) — ปกติ.

**กับดักที่เจอตอนเขียนเทสต์ (อย่าเสียเวลาซ้ำ):**
1. **`template_folder` ต้องเป็น path เต็ม** — Flask หา template จากโฟลเดอร์ของสคริปต์
   ไม่ใช่ `os.chdir()`.
2. **ต้อง stub `has_perm`** ไม่งั้น `base.html` โยน `UndefinedError`.
3. **รอภาพโหลดจริงก่อนวัด** — `wait_for_selector` ไม่พอ ต้อง
   `wait_for_function("i.complete && i.naturalWidth>0")` ไม่งั้นวัดขนาดได้ค่าเก่า.
4. **ต้อง `scroll_into_view_if_needed()` ก่อนลากเมาส์** — กล่องภาพสูงกว่าจอ พิกัดที่
   คำนวณจาก `bounding_box()` อาจติดลบ (อยู่นอก viewport) แล้ว mousedown ตกน้ำเงียบ ๆ
   (เสียเวลาไล่หาสาเหตุนานเพราะไม่มี error อะไรเลย).
5. **404 `/favicon.ico` เป็นเรื่องปกติ** — โปรเจกต์ไม่เคยมีไฟล์นี้ ไม่ใช่บั๊กใหม่.
6. ล้าง `data/artwork_check/inspections/2026*` หลังทดสอบ (อยู่ใน `.gitignore` git
   จึงไม่เตือน แต่ค้างไว้จะไปโผล่ในหน้าประวัติของผู้ใช้).

---

## 🧠 โมเดล (สำคัญมาก — เคยพลาดตรงนี้)

- **`best.pt`** = YOLOv8 **detection** ธรรมดา (3M params). classes: `dented`, `good`. โหมด can_dent default.
- **`bestX.pt`** = **YOLOv8m-SEGMENTATION** (27M params!) — **นี่คือโมเดล production ที่ผู้ใช้ใช้จริง**.
  classes: `dent`, `can`. output = 2 tensor `((1,38,21504),(1,32,256,256))`. verdict NG/OK ใน
  `classify_frame_bestx`. `is_bestx_mode` = เช็คชื่อไฟล์ == "bestx.pt".
- คลาส **non-defect** = `{"good", "can"}` (`_NON_DEFECT_CLASSES` ใน app.py) = "กระป๋องทั้งใบ".
  ตอน NG กล่อง `can` ถูก **ซ่อนตอนแสดงผล** แต่ยังอยู่ใน raw detections (ใช้เช็ค "ครบใบ" ได้).

---

## ⚡ ONNX acceleration (`USE_ONNX=True` — ปัจจุบันเป็นชั้น fallback ใต้ iGPU)

- Export `.pt`→`.onnx` (FP32, `dynamic=True`, opset 17) ครั้งเดียว แล้วรันผ่าน onnxruntime (~2x เร็วขึ้น).
  ultralytics ถอดผล/NMS เอง → **ผลตรวจเท่า PyTorch เป๊ะ** (verify แล้ว IoU 1.0, Δconf 0.0).
- **⚠️ Python 3.9 pin ที่ต้องรู้:**
  - `onnxruntime==1.19.2` = wheel cp39 ตัวสุดท้ายบน Windows (1.20+ ตัด py39 ทิ้ง).
  - `onnx` pip เลือก cp39 ให้เอง (~1.16/1.17). `onnxslim` (py3.8+) แนะนำติดตั้ง.
  - ติดตั้ง: `py -3.9 -m pip install onnxruntime==1.19.2 onnxslim onnx`
- **⚠️ กับดัก segmentation:** export ONNX ทิ้ง task tag → `YOLO(onnx)` เดาเป็น `detect` → ถอด output
  ของ seg ผิด = **ไม่มีกรอบเลย**. แก้แล้วใน `yolo_detector._accel_task()`: อ่าน task จาก `.pt`
  แล้วโหลด `YOLO(onnx, task='segment')` (cache ใน `<onnx>.task` sidecar).
- **`verify_onnx.py`** = ตาข่ายนิรภัย. รันเทียบ `.pt` vs `.onnx` ให้ PASS ก่อนเปิด `USE_ONNX`.
  ต้องรันต่อโมเดล: `py -3.9 verify_onnx.py --weights weights\can_dent\bestX.pt --images <โฟลเดอร์>`
- **fallback หลายชั้นใน `load_model()`**: pkg หาย/export/load/smoke-test ล้มเหลว → กลับ PyTorch อัตโนมัติ.
  stale guard: `.pt` ใหม่กว่า `.onnx` → re-export.

## 🚫 OpenVINO — ค่าเริ่มต้นปิด (`USE_OPENVINO=False`) — เคยพังเงียบ

เคยเปิด `openvino==2025.3.0` บน **py3.9 (off-spec: 2025 ต้องการ Python ≥3.10)** → export/โหลด
สำเร็จ **แต่ตรวจไม่เจอทุกโหมดแบบเงียบๆ** (ไม่ error). นี่คือ failure mode ที่อันตรายที่สุด:
**ห้ามเชื่อว่าใช้ได้แค่เพราะโค้ดรันไม่ error — ต้องผ่าน `verify_onnx.py` เท่านั้น**.

## 🎮 iGPU (Iris Xe) acceleration — ✅ VERIFIED & ENABLED บนสถานี (2 ก.ค. 2026)

**สถานะ: เปิดใช้จริงใน production แล้ว** — `OPENVINO_DEVICE = "intel:gpu"`,
`CONFIG_VERSION = "2026.07.02-ov-igpu-ON"`, `openvino==2024.6.0` ติดตั้งบนสถานี
(pip ถอน 2025.3.0 ตัว off-spec ออกให้ตอนติดตั้ง).

**ตัวเลขจริงบนสถานี (จาก log `YOLO inference avg` + verify_openvino.py):**
- **bestX (seg, production) live 480: ~45-50ms/เฟรม (~20-22 FPS) ≈ เร็วขึ้น ~6 เท่า**
  จากเพดาน ONNX CPU เดิม ~280ms (~2.7 FPS). เร็วกว่าตอน verify (137ms) เพราะเฟรม
  live 640x480 ไม่มี cost ย่อภาพใหญ่. snapshot 1280: 420ms (เดิม ONNX ~1739ms).
- best.pt (detect) บน GPU: ~14ms (~70 FPS).
- **Coverage (โจทย์ตั้งต้น): จบ** — 1-2 วิในเฟรม × ~21 FPS = 20-40+ ครั้ง/ใบ (เป้า 4-5).
- verify_openvino.py (bestX): **PASS ทั้ง intel:cpu+intel:gpu × 480+1280** —
  GPU มี FP16 drift จริงแต่เล็กมาก (IoU 0.9809-0.9913, Δconf ≤0.0053, กล่อง/คลาสตรงหมด;
  CPU ตรงเป๊ะ IoU 1.0/Δconf 0.0).

**⚠️ งานค้าง (บันทึกไว้ตามจริง — ควรปิดเมื่อสะดวก):**
1. ชุดภาพ verify ตอนเปิดใช้มีแค่ 1 รูป (ต่ำกว่ามาตรฐาน ≥10-20 รูปของ repo) — ปิด loop:
   `py -3.9 dump_defect_images.py --limit 30` (ดึงภาพ NG จริงจาก DB → sample_cans)
   แล้วรัน verify_openvino.py กับ bestX อีกรอบ.
2. `best.pt` (โหมด detect) วิ่งบน GPU ด้วยแต่ยังไม่เคยผ่าน verify_openvino.py แยกของตัวเอง
   — รัน `py -3.9 verify_openvino.py --weights weights\can_dent\best.pt --images <โฟลเดอร์>`.

**Rollback:** ตั้ง `OPENVINO_DEVICE = None` + รีสตาร์ต = กลับ ONNX CPU เดิม 100% ทันที.
ถ้า GPU พังเองระหว่างรัน load_model() fallback → ONNX → PyTorch อัตโนมัติ (ดู log).

**ข้อเท็จจริงเวอร์ชัน (re-verify จาก PyPI + ซอร์ส ultralytics v8.4.41, ก.ค. 2026):**
- **`openvino==2024.6.0` = ตัวที่ถูกต้องสำหรับ py3.9** — รุ่นสุดท้ายที่มี wheel
  `cp39-win_amd64` **และ**อยู่ในช่วง `openvino>=2024.0.0` ที่ exporter ของ
  ultralytics 8.4.41 ต้องการอย่างเป็นทางการ (ไม่ต้องใช้ 2023.x ที่เสี่ยง mismatch).
- ultralytics 8.4.41 รองรับ `device="intel:gpu"` ตอน predict ในตัว (`select_device`
  ส่งผ่าน string `intel:*` ตรงๆ, OpenVINOBackend parse เป็น device_name="GPU").
- `onnxruntime-openvino` ตัดทิ้ง: รุ่นใหม่ต้อง py≥3.10, รุ่นเก่าชน onnxruntime 1.19.2.
- **⚠️ GPU plugin ของ OpenVINO default รันภายในเป็น FP16** แม้ IR เป็น FP32 →
  ความแม่นตัดสินด้วย `verify_openvino.py` เท่านั้น. ถ้า FAIL เพราะ drift → แผนสำรอง
  คือบังคับ `INFERENCE_PRECISION_HINT=f32` (ช้าลง, ต้องแก้เพิ่ม — ยังไม่ทำ).

**โครงสร้างโค้ด (เปิดใช้แล้ว):**
- `config.OPENVINO_DEVICE = None` (opt-in; ตั้ง `"intel:gpu"` เพื่อเปิด) — แยกจาก
  `USE_OPENVINO` เดิม. default None = ทุกโหมดทำงานเท่าเดิม 100%.
- `_select_backend()` เปลี่ยนเป็นคืน **candidate list**: OpenVINO@device (ถ้าตั้ง
  flag) → ONNX CPU → OpenVINO (legacy) → PyTorch; `load_model()` ไล่ลองทีละตัว
  (load + smoke test) → **fallback GPU→ONNX→PyTorch อัตโนมัติ**. flag ปิด = ลำดับเดิมเป๊ะ.
- `_maybe_openvino()`: เช็ค device มีจริงผ่าน `ov.Core().available_devices` ก่อน
  (กัน OpenVINO เงียบๆ fallback ไป AUTO/CPU เอง = ตัวเลขความเร็วหลอก) + stale guard
  (`.pt` ใหม่กว่า IR → re-export) + โหลดด้วย task จาก `_accel_task()` เหมือน ONNX.
- **`verify_openvino.py`** = ตาข่ายนิรภัย (เกณฑ์ import จาก `verify_onnx.py` ชุดเดียวกัน):
  เทียบ PyTorch vs OpenVINO ทั้ง `intel:cpu`+`intel:gpu` ที่ 480+1280 + วัดความเร็ว
  PyTorch/ONNX/OpenVINO ในรันเดียว:
  `py -3.9 verify_openvino.py --weights weights\can_dent\bestX.pt --images <โฟลเดอร์>`

**ถ้าต้องเปิดใช้ใหม่บนเครื่องอื่น (ตามลำดับ ห้ามข้าม):** (1) `py -3.9 -m pip install
"openvino==2024.6.0"` (2) เช็ค `ov.Core().available_devices` มี GPU (3) รัน
verify_openvino.py ต้อง PASS ทุก device×imgsz (4) เช็คตัวเลขความเร็วคุ้ม
(5) ตั้ง `OPENVINO_DEVICE="intel:gpu"` + รีสตาร์ต + เช็ค footer/log.

**Route B (ไม่ต้องใช้แล้ว — Route A ผ่าน):** Python 3.11 ใน venv แยก เก็บไว้เป็น
ความรู้เผื่ออนาคต (เช่นถ้าจำเป็นต้องอัป openvino/onnxruntime เกินรุ่นสุดท้ายของ py3.9).

**บังคับทุก Route:** (ก) verify เทียบ PyTorch ผ่าน (IoU ≥0.97, Δconf ≤0.05, จำนวนกล่องตรง,
เคส "GPU เจอ 0 แต่ PyTorch เจอหลายกล่อง" = FAIL) (ข) fallback อัตโนมัติกลับ CPU-ONNX→PyTorch
(ค) opt-in flag default ปิด. **⚠️ RAM single-channel = คอขวด iGPU** → คาดจริง ~120-180ms
(ไม่ใช่ 80ms) ถ้าเอาเต็มควรอัป RAM dual-channel ก่อน.

---

## 📷 กล้อง & imgsz

- Live: 640x480 @ 30fps, `CAMERA_FOURCC=None` (YUY2 — "MJPG" ทำภาพแตกบน MSMF). กล้องตัน 720p ผ่าน MSMF.
- **imgsz: live=480, snapshot=1280. ⚠️ ห้ามต่ำกว่า 480** — dent เป็นฟีเจอร์เล็ก ลดเป็น 320 = ตรวจไม่เจอเลย.
- **Exposure/Brightness** (`CAMERA_AUTO_EXPOSURE`/`CAMERA_EXPOSURE`/`CAMERA_BRIGHTNESS`, opt-in default
  None): เฉพาะกล้อง **live** (ส่งผ่าน ctor ที่ site สร้างกล้อง live; snapshot/RTSP ไม่แตะ). best-effort.
  - **⚠️ กล้องสถานีนี้: EXPOSURE/GAIN/GAMMA ตั้งไม่ได้ผ่าน OpenCV แต่ `BRIGHTNESS` (0-255) ได้** —
    พิสูจน์ด้วย `diagnose_exposure.py` (เทสต์ว่าภาพสว่างเปลี่ยนจริงต่อ knob/backend).
  - ปรับสดขณะรัน: สไลเดอร์ในแผง USB → `POST /api/camera/control` {control, value} →
    `Camera.set_control(name, value)` (brightness/contrast; มี `_cap_lock` กัน race กับ
    `capture_loop`). StreamCamera ไม่มี method นี้ (endpoint คืน error). CONTRAST = knob ทดลอง
    (อาจช่วยหรือแย่ลง — domain shift; ยังไม่ยืนยันว่ากล้องรับ) ต้องเทียบผลตรวจจริงก่อนใช้.
- `Camera` class ใช้ร่วมทั้ง live+snapshot (แยกด้วย ctor params). RTSP → `_initialize_rtsp` (ไม่ทำ exposure).

---

## 🏗️ สถาปัตยกรรมสำคัญ (app.py)

- **Live USB/RTSP** = 2 thread: `capture_loop` (อ่านกล้อง → `latest_raw_frame`) +
  `inference_loop` (infer, นับ, log DB). `generate_frames` = MJPEG generator.
  - `LIVE_SMOOTH_VIDEO`: `False`=วาดกรอบบนเฟรมที่ infer จริง (กรอบเป๊ะ, ภาพตามอัตรา infer) /
    `True`=วาดกรอบล่าสุดบนเฟรมดิบล่าสุด (ภาพลื่น, กรอบตามช้าตอนขยับ). **เปิด Frame Capture =
    บังคับ smooth อัตโนมัติ** (`smooth = frame_capture_enabled or LIVE_SMOOTH_VIDEO` ประเมินสด
    ทุก loop) — ความแม่นกรอบไปอยู่ที่เฟรมที่แช่ (re-infer แล้ว) ส่วนภาพสดแค่ monitor.
- **STREAM** = client-side ล้วน: เบราว์เซอร์เปิดกล้องตัวเอง (`getUserMedia`, ต้อง HTTPS) →
  POST เฟรมไป `/api/stream/infer` → คืน JSON กรอบ → วาดบน canvas. **per-client isolation**
  (ไม่แชร์กล้อง/pipeline). JS อยู่ใน `templates/index.html` (ค่าคงที่ `STREAM_*`).
- **นับ 1 กระป๋อง = 1 การตรวจ (edge-triggered)**: state `none/ok/ng` ต่อกระป๋อง, นับ+log DB
  ครั้งเดียวตอน rising edge (none→ng), กระป๋อง "หายไป" หลัง `DEFECT_RESET_FRAMES` เฟรมว่าง.
  USB/RTSP อยู่ใน `inference_loop`; STREAM อยู่ใน JS (`streamInferLoop`).
- **Frame Capture** (display-only, USB/RTSP): `capture_loop` ให้คะแนนความคมทุกเฟรมดิบ
  (candidate pool) เฉพาะตอน `frame_capture_enabled AND pool_collecting`; `inference_loop`
  ตั้ง `pool_collecting = defect AND _can_complete()` (กระป๋องครบใบ), รีเซ็ต pool ต่อกระป๋อง,
  ตอนกระป๋องผ่านไป **re-infer เฟรมที่เลือก** (ให้กรอบตรง) → publish เป็น JPEG แช่ 5 วิ ใน
  `generate_frames`. **ไม่กระทบการนับ/DB**. Toggle ผ่าน `POST /api/frame_capture`.

---

## 🖍️ Artwork — กรอบแดงชี้ "คำที่มีปัญหา" (display-only)

การ์ด "รายการที่พบ" วาด **กรอบแดงบนคำที่ผิดจริง** ในรูป crop. **แสดงผลอย่างเดียว 100%** —
ไม่แตะ OCR/ผลตรวจ/verdict/การนับ. โค้ดอยู่ใน `artwork_check/highlight.py` (โมดูลใหม่),
เรียกจาก `pipeline.zone_crop_jpg()` เมื่อ `/api/artwork/<id>/crop?...&hl=<คำ>&zid=<โซน>`.

**สถาปัตยกรรม 4 ชั้น** (`highlight.locate_all()` — ไล่จากแม่นสุด ชั้นแรกที่เจอชนะ):

| ชั้น | วิธี | ใช้เมื่อ | ความแม่น (ไฟล์จริง) |
|---|---|---|---|
| 1 | **PDF text-layer word box** (`pdf_ingest.zone_words()`) | โซน `engine == "pdf-text"` | **เป๊ะระดับ vector, ทุกภาษา** |
| 2 | **Tesseract** (`_tess_boxes`) | ไฟล์ outline / ภาพถ่าย | 89% (benchmark), วัดจากพิกเซลจริง + self-verify |
| 3 | `blocks[].bbox` จาก OCR backend (`_block_boxes`) | Tesseract หาไม่เจอ/ไม่มี | **พิกัดจาก LLM = การประมาณ ต้องผ่าน `_verify_boxes` ก่อน** |
| 3b | **พิสูจน์ "แถว" แทนการอ่านคำ** (`_verify_boxes_by_row`) | คำที่ Tesseract ไม่มี traineddata | วัดแถวจากคำ ASCII ข้างเคียง |
| 4 | ไม่วาด | ไม่มั่นใจ | — |

⚠️ **ลำดับนี้เคยสลับกันแล้วพัง:** เดิมให้ `blocks[].bbox` มาก่อน Tesseract และ **ไม่ตรวจสอบเลย** →
บนสถานีจริงที่ Gemini คืน bbox มา กรอบไปโผล่คนละแถวในตารางโภชนาการ (LLM ให้พิกัดแบบ
*ประมาณ* ไม่ใช่ *วัด*). เทสต์ตอนนั้นตั้ง `blocks=[]` ตลอดจึงไม่เคยเจอ — **ถ้าจะเพิ่ม/สลับชั้น
ต้องมีเทสต์ที่ป้อน bbox ที่ "เพี้ยนไปคนละแถว" ด้วยเสมอ**.

- **ชั้น ① ดีที่สุดและฟรี** — ไม่ต้อง OCR/traineddata, รองรับฮีบรู/อาหรับ/จีน/ไทยทันที.
  ตรวจด้วย `ArtworkDocument.zone_words(bbox)` → `[(text, (fx0,fy0,fx1,fy1))]` เป็น**สัดส่วนในโซน**
  → `rotate_frac_box()` (ตามการหมุนโซน) → `frac_to_px()`.
- **`_cv_box` (projection profile) = ชั้นสำรองสุดท้าย ปิดไว้ (`HIGHLIGHT_USE_PROFILE=False`)** —
  benchmark พบว่า **วาดผิดคำ ~40%** บนตารางหนาแน่น. ห้ามเปิดเป็น default.
- **วาดทุกจุดที่คำปรากฏ** (`HIGHLIGHT_MAX_BOXES=6`) — คำผิดมักพิมพ์ซ้ำหลายแถว
  (จริง: `Cude` โผล่ 3 จุดในตารางเดียว) วาดจุดเดียว = ผู้ตรวจแก้ไม่ครบ.

**⚠️ ชั้น 3b — ทำไมบางครั้ง "เจอคำผิดแต่ไม่มีกรอบแดง" (เคสจริง ส.ค. 2026):**
สถานีลง traineddata แค่ `eng` → คำอาหรับที่สะกดผิด (`كربوهيدات كلية`) **ไม่มีกรอบเลย**
ขณะที่ `24%` บนการ์ดถัดไปมีกรอบปกติ. ไล่แล้วพบว่าไม่ใช่บั๊กของการจับคู่ — ชั้น ② อ่านอาหรับ
ไม่ออก (0 กรอบ) ส่วนชั้น ③ **หา bbox เจอ (1 กรอบ) แต่ถูก `_verify_boxes(require_positive=True)`
ทิ้งทุกครั้ง** เพราะการ "อ่านซ้ำเพื่อพิสูจน์คำ" เป็นไปไม่ได้เมื่อไม่มี traineddata ของภาษานั้น
(อ่านอาหรับด้วย `eng` ได้แต่ขยะ). วัดจากเคสจำลองที่สร้างตามภาพจริง:

| ภาษาของคำ | ชั้น ② Tesseract | ชั้น ③ bbox | ผ่านพิสูจน์ | วาดจริง |
|---|---|---|---|---|
| อาหรับ (`eng` อย่างเดียว) | 0 | 1 | **0** | **0 ← บั๊ก** |
| อาหรับ (`eng+ara`) | 1 | 1 | 1 | 1 |
| `20%` / อังกฤษ | 1 | 1 | 1 | 1 |

- **ทางแก้ที่ดีที่สุด = ติดตั้ง traineddata แล้วตั้ง `ARTWORK_HIGHLIGHT_TESS_LANG=eng+ara`**
  (ตรงกับผลทดสอบเดิมของ repo: 25/25 ด้วย `eng+ara` เทียบ 23/25 ด้วย `eng`).
- **ชั้น 3b = ตาข่ายรองรับเมื่อไม่มี traineddata**: พิสูจน์ **"แถว"** แทน **"คำ"** —
  บรรทัดเดียวกันมีคำ ASCII ("Total carbohydrate 0 g 0%") ที่ Tesseract อ่านได้ ใช้พิกเซล
  ของคำนั้นล็อกย่านแนวตั้งของแถว แล้วรับ bbox ของ LLM เฉพาะที่ **กึ่งกลางตกในแถวนั้น**
  และ **ไม่สูงเกิน 1.2 เท่าของแถว**. นี่คือแกนที่ bbox ของ LLM เคยพลาดจริง (ไปโผล่คนละแถว)
  จึงเป็นการพิสูจน์ที่ตรงจุด ไม่ใช่การผ่อนเกณฑ์.
- **เงื่อนไขที่ทำให้ "ไม่วาด" (ตั้งใจให้เข้มไว้ก่อน)**: บรรทัดของคำต้องไม่ซ้ำในข้อความ backend ·
  คำ anchor ต้องยาว ≥4 ตัว ไม่ใช่ตัวเลข และปรากฏครั้งเดียวในภาพ · ใช้เฉพาะคำ **ที่ไม่ใช่ ASCII**
  (คำอังกฤษที่พิสูจน์ไม่ผ่าน = กรอบน่าสงสัยจริง ต้องคงพฤติกรรมเดิมคือไม่วาด).
- Config: `ARTWORK_HIGHLIGHT_ROW_VERIFY` (default `1`; ตั้ง `0` = กลับพฤติกรรมเดิม 100%).
- เทสต์: `tests/test_artwork_highlight.py` 11 ตัว (mock Tesseract ให้ "อ่านภาษานั้นไม่ออก"
  จึง deterministic ไม่ต้องพึ่ง binary) — รวมเคส **bbox เลื่อนไปคนละแถวต้องไม่วาด**.

**⚠️ กับดักที่เจอมาแล้ว (อย่าทำซ้ำ):**
1. **fuzzy match กับคำสั้น/CJK = กรอบผิดคำ** — จีน `灰分`(เถ้า) เคยจับกรอบเดียวกับ `水分`(ความชื้น)
   เพราะต่างกัน 1 ตัวอักษร. `_all_word_matches()` จึง **fuzzy เฉพาะคำ ascii ยาว ≥5** เท่านั้น
   (พอสำหรับ typo อังกฤษที่ตั้งใจจับ) — คำสั้น/CJK/RTL ต้อง exact/substring. **กรอบผิดแย่กว่าไม่มีกรอบ**.
2. **คืน "tier เดียว"** — ถ้ามี literal match แล้ว ห้ามเอา fuzzy มาปน (ไม่งั้นคำที่แค่คล้ายจะติดกรอบมาด้วย).
2b. **`found` ของ MISMATCH_* เป็น "ทั้งบรรทัด" ไม่ใช่คำเดียว** — จับคู่ทีละคำแล้วกระจายกรอบ
   ผิดแถวทันที (เคสจริง: `دهون كلية` ได้ 5 กรอบ เพราะ `دهون`/`كلية` ไปโผล่แถวอื่น).
   `_match_boxes()` จึงแยกทาง: คำเดียว → ทุก occurrence; **หลายคำ → ต้องเป็น run ที่
   ติดกันและอยู่บรรทัดเดียวกัน** (`_phrase_matches` + `_same_line`) แล้ว union เป็นกรอบเดียว.
   วลีเปิด fuzzy ได้ (budget 15% ของความยาว) เพราะถูกล็อกด้วย adjacency แล้ว — จำเป็นจริง:
   Tesseract อ่าน `كربوهيدرات` ตกเป็น `كربوهيدات` (หาย 1 ตัว) บ่อย.
3. **ตั้ง `TESS_LANG` เป็นภาษาที่ไม่ได้ติดตั้ง = Tesseract error ทั้ง call → กรอบหายหมดแม้แต่อังกฤษ**.
   `_resolve_langs()` จึงกรองเหลือเฉพาะภาษาที่ `get_languages()` ยืนยัน (fallback → `eng`).
4. **bbox ของ Gemini มีหลาย convention** (0..1 / 0..1000 / pixel) แยกไม่ออกถ้าไม่รู้ขนาดภาพ →
   `ocr.read_zone()` เก็บ **`ocr_wh`** (ขนาด crop ที่ OCR เห็นจริง) ให้ `_infer_scale()` ตัดสิน **ต่อโซน**
   จากพิกัดใหญ่สุดของทุก block (block เดียวใกล้มุมตัดสินไม่ได้).
5. **`_otsu()` คืน threshold 0 ได้** บนภาพ bimodal สะอาด → ต้องใช้ `gray <= thr` (ถ้าใช้ `<` ชั้น CV ตาย
   เงียบ คืน None ตลอด).
6. **ขนาด crop เป็นตัวชี้เป็นชี้ตายของ Tesseract** — โซนเล็กเรนเดอร์ที่ OCR_DPI ได้ ~490px แล้ว
   Tesseract อ่านมั่ว (0/8 คำ, อ่าน "NUTRITIONAL INFORMATION" เป็น "ANO/V/OLES") → ตกไปใช้ bbox
   ของ backend ที่คลาดเคลื่อน = กรอบผิดแถว. แก้ด้วย `CROP_MIN_SIDE=1200` (PDF เรนเดอร์ใหม่ DPI สูงขึ้น
   = ได้รายละเอียดจริง) + `_upscale_for_ocr()` (ภาพถ่าย ขยายในหน่วยความจำก่อน OCR แล้วหารพิกัดกลับ).
7. **PSM ของ Tesseract สำคัญมากกับ "ตาราง"** — default (psm 3, auto layout) อ่าน *ชื่อรายการ*
   ได้หมดแต่ **ทิ้งคอลัมน์ตัวเลขทั้งคอลัมน์** (หา `24%`/`170`/`475` ไม่เจอเลย). `_PSM_ORDER=(11,3)`
   ลอง **psm 11 (sparse text)** ก่อน แล้วค่อยถอยไป psm 3 (ดีกว่ากับข้อความยาวต่อเนื่อง เช่นบล็อกอาหรับ).
   วัดจาก 7 โซนจริง: psm3=38/44, psm11=42/44, ลองทั้งคู่=43/44.
8. **การอ่านทั้งภาพไม่เสถียรระดับ ±1 พิกเซล** — crop 1455px อ่าน `24%` เป็น `72`, crop 1456px
   อ่านถูก. แต่ **ครอปเฉพาะเซลล์ (75x38) อ่านถูกทุก psm**. จึงมี `_row_refine()`: เมื่อหาไม่เจอ
   ทั้งภาพ ให้ใช้คำข้างเคียงในบรรทัดเดียวกัน (จากข้อความ OCR ของ backend) หา**แถบแถว** แล้ว
   อ่านซ้ำเฉพาะแถบนั้น. anchor ต้องเจอ **ครั้งเดียว** ในภาพ (ไม่งั้นชี้แถวไม่ได้) และผลต้องผ่าน
   `_verify_boxes(require_positive=True)` → เป็นไปไม่ได้ที่จะไปโผล่คนละแถว.
9. **`_verify_boxes` ต้องใช้ psm 7/8 ไม่ใช่ default** — ครอปขนาดเท่าคำเดียวถ้าอ่านด้วย psm 3
   จะได้ค่าว่าง/ขยะ แล้วไป**ตัดกรอบที่ถูกต้องทิ้ง** (เคสจริง: กรอบ `24` ที่ถูกต้องถูกตัดทุกครั้ง).
10. **การ "พิสูจน์ว่าผิด" ต้องเชื่อถือได้ก่อนถึงจะใช้ตัดสิน** — อ่านซ้ำครอปแคบของอาหรับ/CJK
   ไม่น่าเชื่อถือ (กรอบอาหรับที่ถูกอ่านซ้ำได้ `Yoda كلية`) → กรอบที่ "วัดมา" (Tesseract) ของคำ
   non-ASCII จึง**เก็บไว้เสมอ** ส่วน bbox ของ LLM ต้องพิสูจน์ว่าถูกเท่านั้นถึงวาด (asymmetric).
11. **cache ผล OCR ต่อรูป** (`_WORDS_CACHE` key = hash เนื้อภาพ+lang+psm) — การ์ด defect
   หลายใบในโซนเดียวกันจะไม่ OCR ซ้ำ (วัดจริง เร็วขึ้น ~2.5 เท่า). **เป็น `OrderedDict` +
   `threading.Lock`** — Flask `threaded=True` ทำให้ 2 request ชนกันได้จริง (dict ธรรมดา
   จะ evict มั่ว/`RuntimeError` ตอน iterate); ตัด LRU ที่ `_WORDS_CACHE_MAX=12`.
12. **ตัวเลขอาหรับ-อินดิก (`٤٧٥`) ไม่ใช่ `475`** — Tesseract โหมด `ara` คืนตัวเลขเป็น
   `٠-٩` ส่วน defect ที่ฟ้องมาเป็นเลขอารบิกปกติ → จับคู่ไม่ติด **เงียบ ๆ** (ไม่มีกรอบ ไม่มี error).
   `_norm()` จึงพับ `٠-٩`+`۰-۹` (เปอร์เซีย) เป็น `0-9` ก่อนเทียบทุกครั้ง.
13. **เลือก "แถว" ด้วย substring = ชี้ผิดแถว** — `_row_refine()` เดิมหาบรรทัดด้วย
   `key in line`: คำเป้าหมาย `0` (จาก `0 g`) ไปเจอใน `10%` ของอีกบรรทัดทันที.
   ตอนนี้เทียบ **ทั้ง token** (คำสั้น) และถ้าเจอ **มากกว่า 1 บรรทัด = กำกวม → ไม่วาด**
   (กฎเหล็ก 2: ไม่มั่นใจ ไม่แสดง).
14. **คำที่ขอบโซนตัดผ่าน** — PDF text-layer คืนกรอบ **เต็มคำบนหน้ากระดาษ** แม้คำนั้นโผล่ในโซน
   แค่เสี้ยวเดียว → clamp แล้วได้แถบบางติดขอบโซน = ดูเหมือนวาดผิดที่. `frac_to_px()` จึงทิ้ง
   กรอบที่หลุดออกนอกโซน > `_MAX_CLIP_FRAC` (25%).

**⚠️ ข้อจำกัดเชิงกายภาพ (แก้ด้วยโค้ดไม่ได้ — ต้องบอกผู้ใช้):** วัดจากไฟล์จริง กรอบแดงต้องการ
ตัวอักษรในภาพ crop สูงราว **9-20 px**. โซนที่ลากเป็น **แถบกว้างทั้งแผ่น** (เช่น 1600x339)
ตัวหนังสือเล็กเหลือ ~8px → หาคำไม่เจอเลย (0/14) และ **เร่ง resolution ก็ตันที่ 6/14**
(เพราะแถบกว้างมีทั้งกราฟิก/ภาพถ่าย/หลายภาษาปนกัน). โซนเดียวกันที่ลากกระชับรอบตาราง
(1455x990) ได้ **14/14**. ที่วัดได้ชัดคือ **ด้านสั้นของ crop** (พัง: 339/487 · ผ่าน: 895/906/988/1186)
→ เกณฑ์เตือน `zones.HL_MIN_SHORT_SIDE=700` + `HL_MAX_ASPECT=4.0`.
ข้อค้นพบสวนสามัญสำนึก: **ใหญ่ขึ้นไม่ได้ดีขึ้นเสมอ** (คำสูง 32px ได้ 11/14 แพ้ 9-20px ที่ได้ 14/14)
และ **ย่อภาพ raster ลงคือหายนะ** (1/14). วัดแล้วยัง **ปฏิเสธ** 3 ไอเดียของตัวเอง: ปิด dictionary
(ไม่มีผล), Sauvola (แย่ลง), โหลดหลายภาษา (ความแม่นเท่าเดิม แค่ช้าลง ~3.5 เท่า).

**เตือนผู้ใช้ 2 จุด (advisory ล้วน — ไม่แตะ verdict/การนับ/ข้อความ OCR):**
- **ตอนจัดโซน** (ก่อนส่งตรวจ): `renderHlHint()` ใน `artwork_check.js` คำนวณขนาด crop
  ที่จะได้จาก **เรขาคณิตอย่างเดียว** (ไม่เรนเดอร์ ไม่ OCR = ฟรี) แล้วขึ้นบรรทัดเตือนใน
  แผง properties ทันทีที่เลือกโซน — ผู้ใช้แก้ได้เลยก่อนเสียเวลาตรวจ.
- **ในการ์ด "รายการที่พบ"**: `pipeline._tag_highlight_risk()` ตั้ง `z["hl_risk"]`
  (`"wide"`/`"small"`) ลง `report.json` ตอนตรวจ → JS แสดงเหตุผลว่าทำไมไม่มีกรอบแดง
  + วิธีแก้ (ลากโซนให้กระชับแล้วส่งใหม่).
- **ค่าคงที่ต้องตรงกันสองฝั่ง** (`zones.HL_*` ↔ `HL_*` ใน `artwork_check.js`) — แก้ข้างเดียวแล้ว
  คำเตือนตอนจัดโซนกับตอนดูผลจะไม่ตรงกัน.

**Tesseract (ชั้น ②) — optional dependency:**
- `_find_tesseract_cmd()` **auto-detect ให้** ตามลำดับ: env `ARTWORK_TESSERACT_CMD` → PATH →
  `C:\Program Files\Tesseract-OCR\tesseract.exe` → `%LOCALAPPDATA%\Programs\Tesseract-OCR\`.
  **ไม่ต้องตั้ง PATH เอง**.
- ติดตั้ง: UB-Mannheim installer (ติ๊ก Additional language data: Arabic/Hebrew/Chinese/Thai) +
  `py -3.9 -m pip install pytesseract`. **ไม่ติดตั้ง = ไม่มีกรอบ แต่ระบบทำงานปกติ** (ไม่ error).
- หลายภาษา: `ARTWORK_HIGHLIGHT_TESS_LANG=eng+ara+heb+chi_tra+tha`.
- **ติดตั้งที่ server เท่านั้น** — วาดกรอบฝั่ง server ส่ง JPEG ให้ client (เครื่อง client ไม่ต้องลงอะไร).

**Config (ทุกตัว opt-out ได้ — `artwork_check/config.py`):**
`HIGHLIGHT_DEFECT_WORD` (ปิดทั้งฟีเจอร์) · `HIGHLIGHT_USE_PDF_TEXT` · `HIGHLIGHT_USE_TESSERACT` ·
`HIGHLIGHT_USE_PROFILE` (default False) · `HIGHLIGHT_TESSERACT_LANG` · `HIGHLIGHT_MAX_BOXES` ·
`CROP_MIN_SIDE` (=1200 ด้านยาวขั้นต่ำของ crop ในการ์ด — ตัวชี้เป็นชี้ตายของ Tesseract, ดูกับดักข้อ 6)

**เครื่องมือ diagnose บนสถานี:** `py -3.9 diagnose_highlight.py <inspection-id>` — พิมพ์ config,
path/ภาษาของ tesseract, ชั้นที่ใช้ต่อโซน, จำนวนกรอบต่อ defect และ**อ่านซ้ำทีละกรอบ**ว่าในกรอบ
คือคำอะไร (`--save` เขียนไฟล์ `diag_<id>_<n>_<zone>.jpg` ออกมาดูด้วยตา). ใช้ตอบคำถาม
"ทำไมกรอบไม่ขึ้น/ขึ้นผิดที่" ได้โดยไม่ต้องเดา.

**ผลทดสอบ end-to-end (production path, 5 artwork จริง + 1 ภาพถ่าย):**
- Cosma/GimCat (มี text layer) → ชั้น ① **8/8** เป๊ะ · StarKist/TerraMadre/JohnWest (outline) → ชั้น ②
- รวม **25/25** เมื่อตั้ง `eng+ara` (23/25 ด้วย `eng` ล้วน — อาหรับ MISS = ไม่วาด ไม่ error)
- ภาพถ่ายกล้องจริง (Puffy Nee Nee): **9/9** รวม `Cude`/`Phosphours` ที่เป็นคำผิดจริง
- **ไม่มีกรอบวางผิดแม้แต่จุดเดียวในทุกไฟล์**
- ⚠️ อ่อนกับ **โลโก้/badge สไตล์ไลซ์บนกราฟิก** (SHINYCAT/GLUTEN FREE) — ปกติ ไม่ค่อยถูกฟ้องเป็น defect
- ⚠️ **ทุกครั้งที่ "ดูเหมือนพัง" ตอนทดสอบ = พิกัดโซนผิด ไม่ใช่เมธอด** — verify โซนด้วยการ render ดูก่อนเสมอ

---

## 🖼️ Artwork — เลย์เอาต์หน้าจอ (คอลัมน์เดียว กว้าง 1600px กึ่งกลาง)

หน้า `/artwork_check` เป็น **คอลัมน์เดียว**: ① อัปโหลด/จัดโซน แล้ว ② ผลการตรวจสอบ
อยู่ล่าง (เดิม 2 คอลัมน์ 7/5 ใน `max-width:1380px` → ① ได้แค่ ~710px ทำงานกับ artwork ใหญ่ไม่ไหว).
`.aw-wrap { max-width:1600px; margin:24px auto }` = **กว้างพอทำงาน แต่ยังอยู่กึ่งกลางจอ**
(เคยลองปลดเพดานเป็นเต็มจอ 1848px แล้ว ผู้ใช้บอกว่าชิดขอบเกินไป). บนจอ 1920 กล่องจัดโซน
กว้าง **1522px (×2.1 ของเดิม)** เว้นขอบข้างละ ~200px → A3 พอดีที่ 61% (เดิมต้องย่อเหลือ 29%).

- **ความกว้างของ panel ไม่เข้าไปในสูตรพิกัดโซนเลย** — โซนเป็นสัดส่วน 0..1, ภาพถูกกำหนดความกว้าง
  เป็น px จาก `applyZoom()` และ `.aw-stage img { max-width:none }` ⇒ กล่องกว้างขึ้น = **เห็นภาพ
  มากขึ้นที่ซูมเท่าเดิม ไม่ใช่ย่อภาพ**. ความละเอียดจึงลดลงไม่ได้เชิงโครงสร้าง.
  พิสูจน์แล้วด้วยเบราว์เซอร์จริง (เทียบก่อน/หลังแก้): วาดโซนที่ zoom 100/60/250% ได้ค่าคลาดเคลื่อน
  **0.781/0.797/0.781 px เท่ากันทุกหลัก** และ zoom mapping (60% → 1488px), wheel-zoom เท่าเดิมเป๊ะ.
- **`.main-content { max-width:1400px }` อยู่ใน `static/css/style.css` = ของกลางทุกหน้า** —
  ปลดเพดานได้เฉพาะใน `{% block extra_css %}` ของ `artwork_check.html` (render เฉพาะหน้านี้)
  **ห้ามแก้ style.css** ไม่งั้นกระทบ Live/Label/Dashboard ทั้งหมด.
- **`.results-wide` + `setResultsWide()` กลายเป็น no-op** (คอลัมน์เดียว) — คงคลาสและจุดเรียกทั้ง 3
  ไว้ตามเดิม ไม่ต้องแตะ JS.
- **แถบเครื่องมือ (`#awZoomBar`) อยู่ "นอก" กล่องภาพ** — เดิมอยู่ข้างในและ `position:sticky; top:0`
  ซึ่งตรึงได้เฉพาะแนวตั้ง ⇒ พอเลื่อนภาพไปทางขวา **ปุ่มไฟล์หลัก/ชิ้นงานหลุดออกนอกจอ**.
  ย้ายออกมาแล้วปุ่มอยู่นิ่งเสมอ (พิสูจน์แล้ว: เลื่อนภาพ 500px แถบยังอยู่ที่เดิมทุกพิกเซล).
- **⚠️ ตอนย้ายแถบออก ทำให้ `zoomRange.closest(".aw-stage-box")` คืน `null`** → wheel-zoom /
  ปุ่มพอดีความกว้าง / การลากเลื่อนภาพ **ตายเงียบพร้อมกันทั้งหมด**. ตอนนี้อ้างกล่องด้วย
  `$("awStageBox")` (id) แทน — **ห้ามกลับไปใช้ `closest()` จาก element ในแถบเครื่องมือ**.
- **ลากเมาส์เพื่อเลื่อนภาพ (pan)** — เปลี่ยนแค่ `scrollLeft/scrollTop` ของ `.aw-stage-box`
  ⇒ **scrollbar เดิมทำงานเหมือนเดิมทุกอย่าง** ไม่แตะสูตรพิกัดโซนเลย. 2 ท่า:
  - **ปุ่มซ้ายลากบนพื้นที่ว่าง** (ไม่ใช่บนโซน + ไม่ได้กด "เพิ่มโซน") — ท่านี้เดิมไม่ทำอะไรเลย
    จึงเอามาใช้ได้โดยไม่ทับของเดิม. โซนมี `stopPropagation` ใน `startDrag` อยู่แล้ว
    event จึงไม่ไหลมาถึง handler ของ pan ตอนลากย้าย/ย่อขยายโซน.
  - **ปุ่มกลาง (ล้อ) ลาก** — ใช้ได้เสมอแม้อยู่บนโซนหรือกำลังวาดโซน. ต้องมี
    `if (ev.button !== 0) return;` ทั้งใน `startDrag` และ handler `mousedown` ของ `stage`
    ไม่งั้นปุ่มกลางจะไป **ย้ายโซน/วาดโซน** แทนที่จะเลื่อนภาพ.
  - `ev.preventDefault()` ใน `panStart` **จำเป็นทั้งสองปุ่ม**: กัน native image drag ของ
    เบราว์เซอร์ (ปุ่มซ้าย) และกัน autoscroll วงกลมของ Windows (ปุ่มกลาง).
  - `canPan()` เช็ค **ทั้งแนวนอนและแนวตั้ง** — กด "พอดีความกว้าง" แล้วภาพยังสูงเกินกล่อง
    (A3 ที่ 72% = 1786x1263 ในกล่องสูง 840) ⇒ ยังต้องลากเลื่อนแนวตั้งได้.
  - `updatePannable()` (เรียกจาก `applyZoom()` + `ResizeObserver` + `window.resize`) คุม
    คลาส `.aw-pannable` = เคอร์เซอร์มือ. ขึ้นมือทั้งที่เลื่อนไม่ได้ = ผู้ใช้ลากแล้วงง.
- ปุ่ม **"⤢ พอดีความกว้าง"** (`#awZoomFit`): `floor(stageBox.clientWidth-6 / natW * 100)` clamp 30-300
  — **ปัดลงเสมอ** (ปัดขึ้น 1% = ภาพล้นกล่อง มี scrollbar แนวนอนทั้งที่กด "พอดี") และต้อง sync
  `zoomPct` + `zoomRange.value` + ป้าย % พร้อมกัน ไม่งั้นสไลเดอร์ค้างคนละค่ากับภาพ.
- ② อยู่ล่างแล้ว → `scrollToResults()` พาจอไปที่ผลตรวจหลังกด "ส่งตรวจสอบ" (ทั้งกรณีสำเร็จและ error)
  ไม่งั้นผู้ใช้ที่อยู่ตรงกล่องจัดโซนจะเหมือนกดแล้วไม่มีอะไรเกิดขึ้น.
- **ปุ่ม "⛶ พอดีทั้งหน้า"** (`#awZoomFitPage`) = `min(กว้าง, สูง)` ต่างจาก "พอดีความกว้าง"
  ที่ยังเหลือ scroll แนวตั้ง. ⚠️ **ต้องลดพื้นล่างของซูมจาก 30% เป็น 10% ด้วย**
  (`ZOOM_MIN` ใน JS ↔ `min=` ของ `#awZoomRange` ใน template — **สองฝั่งต้องตรงกัน**):
  A3 ในกล่องมาตรฐานต้องย่อเหลือ ~26% ถ้ายัง clamp ที่ 30% ปุ่มจะ**โกหก** (ภาพยังล้นกล่อง
  ทั้งที่ปุ่มบอกว่าพอดี) และสไลเดอร์จะค้างคนละค่ากับภาพ. วัดจริงบนเบราว์เซอร์:
  พอดีความกว้าง = 32% (img 1432x860 ในกล่อง 1480x718 → ล้นแนวตั้ง) ·
  พอดีทั้งหน้า = 26% (img 1164x699 ในกล่อง 1480x707 → ไม่ล้น).
- **โหมดวาดโซนต่อเนื่อง** (`#awDrawContinuous`, default **ปิด** = เดิมเป๊ะ): ติ๊กแล้ว
  `setDrawMode()` จะค้างโหมดไว้หลังวาดแต่ละโซน. **คลิกเปล่า (กรอบ < 8px) ปิดโหมดเสมอ**
  = ทางออกที่ไม่ต้องหา Esc (Esc ยังใช้ได้ผ่าน `cancelDraw()`).
- **autosave โซนลง `localStorage`** (`aw.session.v1`, อายุ 7 วัน) — เดิมรีเฟรชหน้า = โซนหายหมด.
  เขียนจาก `renderZones()` (จุดเดียวที่ครอบคลุมทุกการเปลี่ยนโซน) แบบ debounce 400ms.
  ⚠️ **ไม่กู้คืนเงียบ ๆ เด็ดขาด** — ขึ้นแถบ `#awRestore` ให้กดยืนยันก่อน เพราะการเอาโซน
  ของงานคนละใบมาวางทับโดยผู้ใช้ไม่รู้ตัว = ส่งตรวจด้วยโซนผิดใบ (อันตรายกว่าลากใหม่มาก).
  ก่อนเสนอกู้คืนต้อง **โหลด `preview.png` ของ id นั้นให้ผ่านก่อน** (ไฟล์ถูกลบ / เป็นของ
  คนอื่นตามด่านสิทธิ์ → ไม่เสนอ).
- **ยังไม่ได้ทำ (ถ้าอยากให้ภาพ "คมขึ้น" จริงระดับพิกเซล):** `PREVIEW_DPI=150` คือเพดานของภาพใน
  ตัวแก้โซน ซูมเกิน 100% = ขยายภาพเบลอ. ต้องเพิ่มไฟล์ **display-only** แยก (เช่น `PREVIEW_DISPLAY_DPI`
  → `preview_hi.png`) **ห้ามเปลี่ยน `preview.png`** เพราะถูกใช้ต่อโดย `propose_zones` / `snap_bbox` /
  `autopair_bbox` และ `draw_overlay` (ซึ่งใช้ `putText` fontScale คงที่ 0.5 → DPI สูงขึ้น = ป้ายบน
  overlay เล็กลงเชิงสัดส่วน).

---

## 🔐 Artwork — ประวัติการตรวจ "เห็นเฉพาะของตัวเอง"

หน้า `/artwork_check/history` แสดงเฉพาะการตรวจที่ผู้ใช้คนนั้นเป็นคนอัปโหลด
(role ใน `HISTORY_ADMIN_ROLES` เห็นทั้งหมด). **ขอบเขต: โหมด Artwork เท่านั้น** —
Label Paper / Live / Dashboard / `/api/defects` ไม่ถูกแตะ และ **ไม่ต้องแก้ SQL schema เลย**.

- **เจ้าของเก็บใน `owner.json` แยกจาก `report.json`** (`{user_id, username, saved_at}`) เพราะ
  `report.json` เกิดตอนกด "ส่งตรวจสอบ" เท่านั้น แต่ระหว่างจัดโซนมี endpoint ที่ต้องเช็คสิทธิ์แล้ว
  (preview/crop/propose/snap/autopair) — ถ้ารอ report.json ช่วงนั้นจะไม่มีเจ้าของให้เทียบ.
  เขียนตอน `pipeline.start_inspection(owner=...)`; `routes.py` เป็นคนหา user จาก `g.current_user`
  → **`pipeline.py` ไม่ import Flask** (ยังเทสต์ได้ตรง ๆ).
- **ด่านเดียวคุมทุก endpoint: `@artwork_bp.before_request`** อ่าน `rec_id` จาก `request.view_args`
  ⇒ ครอบคลุม **13 route** ที่มี `<rec_id>` ทั้งหมด **รวมถึง route ที่จะเพิ่มในอนาคต**.
  ⚠️ **การกรองเฉพาะรายการ (`/api/artwork/history`) ไม่ใช่การป้องกัน** — ถ้าไม่มีด่านนี้ ใครที่รู้ id
  ก็เปิด `/api/artwork/<id>/report` ของคนอื่นได้ตรง ๆ. เทสต์ `test_http_other_user_blocked_on_every_rec_route`
  **ไล่จาก `url_map` จริง** ไม่ใช่ลิสต์ที่เขียนมือ → เพิ่ม route ใหม่แล้วลืมกัน = เทสต์แดงทันที.
- **นโยบายอยู่ที่เดียวใน `artwork_check/ownership.py`** (ไม่มี Flask): ปิด flag → ผ่านหมด ·
  ไม่มีระบบล็อกอิน (`viewer is None`) → ผ่านหมด · admin → ผ่านหมด · **บันทึกเก่าที่ไม่มี `owner.json`
  → admin เท่านั้น** · เจ้าของ → ผ่าน.
  - `viewer is None` = auth ปิด ≠ `viewer == {}` = auth เปิดแต่หาผู้ใช้ไม่เจอ (**ไม่มีสิทธิ์อะไรเลย**) —
    ต้องเช็คด้วย `is None` ห้ามใช้ความ falsy ไม่งั้นสองเคสนี้จะรวมกันเป็น "ผ่านหมด".
  - เทียบ id ต้อง `bool(oid) and bool(vid) and oid == vid` — ไม่งั้น `"" == ""` ทำให้ทุกคนเป็นเจ้าของ
    ของบันทึกที่ `user_id` ว่าง.
- **`AUTH_ENABLED=False` ต้องไม่กรอง** ไม่งั้นหน้าประวัติว่างเปล่าทั้งที่ระบบทำงานปกติ.
- **ผลข้างเคียงที่ตั้งใจ:** role `Manager`/`Viewer` (ซึ่งมี `view_history`) จะเห็นเฉพาะงานของตัวเอง
  ด้วย — `Viewer` ที่ไม่เคยอัปโหลดจะเห็นตารางว่าง. ถ้าต้องการให้เห็นทั้งหมด เพิ่มชื่อ role ใน
  `ARTWORK_HISTORY_ADMIN_ROLES` (env, คั่นด้วย comma) ไม่ต้องแก้โค้ด.
- **ผูกกับ "ชื่อ" role ตามที่ผู้ใช้เลือก** ⇒ ถ้ามีคนเปลี่ยนชื่อ role `Admin` ในหน้าจัดการผู้ใช้
  **สิทธิ์เห็นทั้งหมดจะหยุดทำงานเงียบ ๆ** ต้องมาแก้ค่าคอนฟิกให้ตรงกัน.
- **ปุ่มลบใช้ด่านเดียวกัน** (DELETE มี `rec_id`) ⇒ เจ้าของ + admin เท่านั้น. JS ไม่ต้องซ่อนปุ่ม
  เพราะรายการที่แสดง = รายการที่ลบได้อยู่แล้ว (server กรองมาให้).
- **ชื่อผู้ตรวจแสดง 2 ที่**: คอลัมน์ "ผู้ตรวจ" ในตารางประวัติ · บรรทัด `👤 <ชื่อ>` ในหัวรายงาน
  ตอนเปิดดูรายละเอียด (และหลังกด "ส่งตรวจสอบ" บนหน้าตรวจ). `routes._with_owner()` แนบ
  ชื่อ **ตอนตอบเท่านั้น** ไม่เขียนลง `report.json` ⇒ เจ้าของมีแหล่งความจริงเดียวคือ
  `owner.json` (ไฟล์เดียวกับที่ด่านสิทธิ์ใช้) ไม่มีทางไม่ตรงกัน. บันทึกเก่า = ค่าว่าง →
  UI ไม่แสดงบรรทัดนั้น (ดีกว่าโชว์ "ไม่ทราบ").
- `list_inspections(limit, can_view=None)` — `can_view=None` = เส้นทางเดิมเป๊ะ. ตอนกรองมีเพดาน
  `_MAX_SCAN=2000` กันผู้ใช้ใหม่ที่ยังไม่มีบันทึกต้องไล่อ่านทั้งคลังทุกครั้ง. เพิ่ม field `owner`
  (ชื่อผู้ตรวจ) ในผลลัพธ์ = คอลัมน์ใหม่ในตาราง (JS `COLS=7` ต้องตรงกับ `<th>` ใน template).
- **Config:** `ARTWORK_HISTORY_PER_USER` (default `true`; ตั้ง `false` + รีสตาร์ต = กลับพฤติกรรมเดิม
  100% ทันที) · `ARTWORK_HISTORY_ADMIN_ROLES` (default `Admin`).
- **ตอน deploy ครั้งแรก:** บันทึกเก่าทั้งหมดจะหายจากสายตาผู้ใช้ทั่วไปทันที (เห็นได้เฉพาะ admin)
  และงานที่ค้างอยู่ระหว่างจัดโซนตอนรีสตาร์ตจะกลายเป็น "ไม่มีเจ้าของ" → เจ้าตัวเปิดต่อไม่ได้
  ต้องอัปโหลดใหม่ ⇒ **ควร deploy ตอนไม่มีคนใช้งาน**.

---

## 🔑 Login — ปุ่ม "ลงทะเบียน" (self-service registration)

หน้า `/login` มีปุ่ม **ลงทะเบียน** เปิด modal (ฟอร์มเดียวกับ "เพิ่มผู้ใช้ใหม่" ของแอดมิน
แต่ตัด **ชื่อผู้ใช้** + **บทบาท (role)** ออก). โค้ด: `auth/registration.py` (กติกาล้วน ไม่มี Flask)
+ `POST /api/auth/register` ใน `auth/routes.py` + modal ใน `templates/login.html`/`static/js/login.js`.

- **username = email (lowercase เสมอ)** — `normalize_email()` พับเป็นตัวเล็กก่อนเทียบ/บันทึก
  ไม่งั้น `A@x.com` กับ `a@x.com` กลายเป็น 2 บัญชีบน collation ที่ case-sensitive.
- **role fix ฝั่งเซิร์ฟเวอร์** (`AUTH_REGISTER_ROLE`, default `Viewer`) — body ที่ส่ง `role` มา
  **ถูกละทิ้ง** (เทสต์ `test_client_cannot_choose_role` กันไว้). endpoint นี้ยกระดับสิทธิ์ไม่ได้.
- **โดเมนอีเมลเทียบแบบตรงทั้งโดเมน** (`@thaiunion.com` เท่านั้น) → subdomain
  (`mail.thaiunion.com`) และโดเมนหลอก (`evil-thaiunion.com`) ต้องไม่ผ่าน — มีเทสต์ทั้งคู่.
- **อีเมลยาว > 64 ตัว = ปฏิเสธ** เพราะ `AuthUsers.Username` เป็น `NVARCHAR(64)`
  (username = email) ไม่งั้นไปพังที่ SQL Server. ค่าคงที่อยู่ที่ `ac.USERNAME_MAX_LEN`
  **ต้องตรงกับ `Connection_sql/auth_schema.sql`** และกับเลข 64 ใน `login.js`.
- **`/api/auth/register` ต้องอยู่ใน `_PUBLIC_PATHS` ของ `access.py`** ไม่งั้นคนที่ยังไม่ล็อกอิน
  โดน 401 = ปุ่มลงทะเบียนใช้ไม่ได้เลย (เทสต์ `test_register_endpoint_is_public` กันไว้).
- **สิทธิ์ที่เห็นหลังล็อกอิน = สิทธิ์ของ role นั้นล้วน ๆ** — ถ้าอยากให้บัญชีที่สมัครเองเห็นแค่
  *ตรวจ Artwork* ให้ไปติ๊กสิทธิ์ของ role `Viewer` ที่หน้า `/admin/users` (การ์ด "บทบาทและสิทธิ์
  การใช้งาน") **ไม่ต้องแก้โค้ด** — และการแก้ตรงนั้นกระทบบัญชี Viewer เดิมทุกคนด้วย.
- กันสแปม: throttle ต่อ IP ในหน่วยความจำ (`AUTH_REGISTER_MAX_PER_IP_HOUR`, `0` = ปิด)
  **นับเฉพาะคำขอที่อีเมลฟอร์แมตถูก** — พิมพ์อีเมลผิดจะได้ไม่กินโควตาของคนที่สมัครจริง.
- **kill switch:** `AUTH_REGISTER_ENABLED=0` = ซ่อนปุ่ม **และ** API ตอบ 403 (ซ่อน UI อย่างเดียว
  ไม่ใช่การป้องกัน — เทสต์ยิงตรงเข้า endpoint ตอนปิด flag).
- เทสต์: `tests/test_auth_registration.py` 39 ตัว (mock `store`+bcrypt → ไม่ต้องมี SQL Server).
- **บั๊กเก่าที่แก้ไปพร้อมกัน:** `.pw-strength{display:flex}` / `.pw-rules{display:grid}` ใน
  `auth.css` **ชนะ attribute `hidden`** → แถบวัดความแข็งแรงรหัสผ่านโผล่เป็นแถบเทาว่างบนหน้า
  login ตั้งแต่ยังไม่พิมพ์อะไร. เพิ่ม `.pw-strength[hidden], .pw-rules[hidden]{display:none}`.

---

## 🔌 N8N Artwork OCR — เคส "ไม่มีการยิง HTTP ออกไปเลย"

**คนละอาการกับ "ยิงแล้วพัง"** — ที่เคยแก้ไปก่อนหน้านี้คืออาการ *ยิงแล้วไปไม่ถึง*
(`a34c4c2` ตั้ง default เป็น `localhost` ผิด → แก้กลับเป็น IP สถานี, ต่อมา `578d751`
ย้ายมา `127.0.0.1` เพราะ N8N มารันบนสถานีเอง). ถ้า **ไม่มี log `[N8N→OCR] POST` เลย**
แปลว่าโค้ดตัดสินใจไม่ยิงตั้งแต่ต้น ซึ่งมี **5 ทาง** (ส่วนใหญ่ถูกต้องตามออกแบบ ไม่ใช่บั๊ก)
บวก **1 ทางที่กลับด้าน คือยิงทั้งที่มี text layer**:

| # | เงื่อนไข | ที่เกิด | เป็นบั๊กไหม |
|---|---|---|---|
| 1 | โซน `type == "ignore"` | `ocr.read_all_zones()` ข้ามก่อนถึง `read_zone` | ไม่ (ตั้งใจ) |
| 2 | **PDF text layer ≥ `EMBEDDED_TEXT_MIN_CHARS` (12)** → `engine="pdf-text"` | `ocr.read_zone()` บรรทัดแรก | **ไม่ — และพบบ่อยสุด** (แม่นกว่า OCR + ฟรี) |
| 3 | `vertex_client.is_enabled()` เท็จ → `engine="none"` | `OCR_BACKEND` ถูกตั้งเป็นค่าอื่น หรือ URL ว่าง | ใช่ (ตั้งค่าผิด) |
| 4 | `crop.size == 0` (bbox ตัดออกนอกหน้า) | `ocr.read_zone()` | ใช่ (โซนผิด) |
| 5 | **cache `ocr_only.json` ยัง valid** | `pipeline.run_ocr_only()` (แท็บ "ข้อความ + คำแปล") | ไม่ (ตั้งใจ) |
| **6** | **มี text layer พอ แต่เป็นคำผิดรูป → ปฏิเสธแล้ว "ยิง OCR แทน"** | `ocr.read_zone()` (`PDFTEXT_GARBLED_CHECK`) | **ไม่ (ตั้งใจ) — กลับด้านกับข้อ 2** |

- **ทาง ② คือคำตอบของ "ทำไมบางไฟล์ยิง บางไฟล์ไม่ยิง"** — artwork ที่ยัง**ไม่ได้ outline**
  ตัวหนังสือ (มี text layer) จะไม่แตะ N8N เลยทั้งไฟล์ ส่วนไฟล์ outline/ภาพถ่ายจะยิงทุกโซน.
  **ไฟล์เดียวกันยังผสมกันได้** (บางโซนมี text layer บางโซนเป็นกราฟิก) → ดูรายโซนเท่านั้น.
- **ทาง ⑤ ทำให้ "กดแล้วเงียบ"** — key = hash ของ (id/type/group/bbox/doc/rotate ของทุกโซน +
  auto_rotate + **ค่าตั้ง OCR ที่เปลี่ยนผลการอ่าน** ดู `pipeline._ocr_fingerprint()`).
  ขยับโซนแม้นิดเดียว = cache หลุด. ลบ `data/artwork_check/inspections/<id>/ocr_only.json`
  = บังคับ OCR ใหม่. **`run_inspection()` (ปุ่ม "ส่งตรวจสอบ") ไม่ใช้ cache นี้** — ยิงใหม่เสมอ.
  > ⚠️ ก่อนหน้านี้ key มีแค่ layout โซน ⇒ แก้ค่า OCR แล้ว cache ไม่หลุด แท็บแปลเสิร์ฟ
  > ข้อความเก่าตลอดไปแบบเงียบ. **ถ้าเพิ่มค่าตั้งที่กระทบการอ่าน ต้องใส่ใน `_ocr_fingerprint()` ด้วย**
  > (มีเทสต์ `test_artwork_ocr_cache.py` ไล่รายชื่อคีย์กันลืม).
- **`_resolve_backend()` auto-เลือก `n8n` เมื่อ `OCR_BACKEND` ว่างและมี URL** ⇒ การตั้ง env
  `OCR_BACKEND=stub`/`vertex` ทับ จะปิดการยิงทั้งระบบแบบเงียบ ๆ (ไม่ error, ได้ข้อความ stub).

**เครื่องมือ:** `py -3.9 diagnose_n8n_ocr.py [<inspection-id>] [--no-ping|--ping-only|--scan]`
— อ่านอย่างเดียว ไม่แตะ report/cache/verdict. ทำ 4 อย่าง:
① พิมพ์ config ที่ตัดสินใจจริง (backend ที่ resolve ได้, URL ทั้ง OCR+translate, เตือนเมื่อ
host สองตัวไม่ตรงกัน / ใช้ `localhost` / ใช้ **Test URL** `/webhook-test/` ที่ตายนอกโหมด
listen) ② ยิงภาพ JPEG เล็ก ๆ ผ่าน `ocr_n8n.ocr_image()` = เส้นทางเดียวกับของจริง แล้วแปล
error ให้ (connection refused = N8N ไม่ได้รัน · 404 = workflow ไม่ได้ Activate/path ผิด ·
timeout = Gemini ช้า/โควตา · 413 = payload เกิน) ③ ไล่ทีละโซนของการตรวจจริง **โดยไม่เรียก OCR**
บอกว่าโซนไหน "จะยิง" โซนไหนไม่ยิงเพราะข้อไหนใน 5 ทาง พร้อมเทียบกับ `engine` ที่บันทึกไว้ใน
`report.json` และเช็คสถานะ cache ทาง ⑤ ④ **`--scan` กวาดทุกการตรวจ** หาไฟล์ที่
"น่าจะได้ text แต่ยังยิง OCR" — เคสที่ต้องจับคือโซนที่มีข้อความ **1..11 ตัว**
(ต่ำกว่า `EMBEDDED_TEXT_MIN_CHARS`) = ทิ้งข้อความจริงไปเดาด้วย OCR ทั้งที่โซนแค่ลากคาบเกี่ยว;
โซนที่ได้ 0 ตัว = outline/กราฟิกจริง ยิง OCR ถูกแล้ว.

**⚠️ การตัดสินเป็น "รายโซน" ไม่ใช่ "รายไฟล์"** — `embedded_text(bbox)` อ่านเฉพาะในกรอบโซน
⇒ ไฟล์ที่มี text layer เต็มหน้ายังมีโซนที่ยิง OCR ได้ (โซนวางบนโลโก้/ภาพถ่ายที่ไม่มีตัวอักษร
เป็น text) และไฟล์ outline ทั้งไฟล์จะยิงทุกโซน.

**ลำดับการไล่ที่เร็วที่สุด:** ดู log บนคอนโซลก่อน — `[artwork] zone z1 engine=... chars=...`
บอกทาง ①②③④ ครบอยู่แล้วต่อโซน ส่วน `[N8N→OCR] POST ...` คือหลักฐานว่ายิงจริง.
ถ้า log หายไปทั้งคู่ = request ไม่เคยถึง pipeline (ดูฝั่ง route/สิทธิ์แทน).

### ⚠️ "ยิงแล้ว ตอบกลับแล้ว แต่ได้ขยะ" — ที่มาของ "ตัวอักษรแปลก ๆ ที่ของจริงไม่มี"

คนละอาการกับสองข้อบน (ไม่ยิง / ยิงแล้วพัง). ตรงนี้ **HTTP 200 ไม่มี error** แต่สิ่งที่
เอาไปเทียบใน MISMATCH/SPELL เป็นขยะ. พิสูจน์ด้วย mock server ที่ตอบแบบที่ N8N ตอบจริง:

| คำตอบจาก N8N | เดิมได้อะไรเป็น "ข้อความบนฉลาก" | ตอนนี้ |
|---|---|---|
| ` ```json{"text":"..."} ``` ` (Gemini ครอบรั้ว) | **ทั้งก้อนรวมรั้ว** → คำว่า `json`/`text`/`blocks`/`{`/`}` | ✅ ถอดรั้วแล้วได้ข้อความจริง |
| `<!DOCTYPE html>...` (workflow ไม่ Activate) | `DOCTYPE html title Error Workflow could not be started` | ✅ **ปฏิเสธ → UNREADABLE** |
| plain text ล้วน | ใช้เป็นข้อความ (เงียบ) | ใช้ได้เหมือนเดิม **แต่ติดธง `note` ให้ผู้ตรวจเห็น** |

- **`_strip_fence()` คือตัวที่แก้อาการที่ผู้ใช้บ่นตรง ๆ** — LLM ครอบ ` ```json ` เป็นปกติ
  วิสัย. ถอดรั้วทั้ง 3 จุด: body ทั้งก้อน · ค่าใน `data/result/output/...` · ค่าใน `text` เอง.
  แกะได้ **12 รูปแบบ** (`tests/test_n8n_ocr_response.py` 35 ตัวคุมไว้).
- **`_looks_like_html()` ตั้งเกณฑ์แคบมากโดยตั้งใจ** (content-type มี `html` หรือขึ้นต้นด้วย
  `<!doctype`/`<html`) — การตัดสินผิดว่า "ไม่ใช่ข้อความ" ทำให้โซนที่อ่านได้จริงกลายเป็น
  UNREADABLE ฟรี ๆ. เทสต์มีเคส `INGREDIENTS <500 mg` (มี `<` กลางข้อความ) ต้องไม่โดนจับ.
- **`warning` เคยถูกตั้งแล้วทิ้ง** — `ocr_image()` ตั้ง `warning` มาตั้งแต่แรกแต่**ไม่มีใครอ่าน**
  ⇒ `read_zone()` ตอนนี้แปลงเป็น `note` ของโซน และ `renderReport()` กาง `<details>`
  "ข้อความ OCR ต่อโซน" ให้อัตโนมัติเมื่อมีโซนที่มี `note` (ไม่งั้นซ่อนอยู่ ไม่มีใครเห็น).
- **retry เฉพาะความล้มเหลวชั่วคราว** (`N8N_OCR_RETRIES=1`): ต่อไม่ติด / timeout / 5xx.
  **ไม่ลองซ้ำกับ 404** (workflow ไม่ Activate) **หรือ 413** (payload ใหญ่) เพราะยิงกี่ครั้ง
  ก็ผลเดิม = ทำให้ผู้ตรวจรอฟรี. ตั้ง `0` = ปิด = พฤติกรรมเดิมเป๊ะ.
- **`--ping-only` ตอนนี้ตรวจ "เนื้อหา" ด้วย ไม่ใช่แค่ "ต่อติดไหม"** — ภาพทดสอบมีคำว่า
  `DIAGNOSE 12345`; ถ้าผลที่ได้ไม่มีคำนี้ = prompt สั่งให้ทำอย่างอื่น (แปล/สรุป) ซึ่งเป็น
  ความผิดพลาดที่เดิมมองไม่เห็นเลยเพราะ HTTP 200.

**⚠️ "โซนเดียวพัง = การตรวจทั้งใบล่ม" — เจอตอนทดสอบเชิงโจมตี (17 ส.ค. 2026):**
`read_zone()` เรียก `vertex_client.ocr_image()` **โดยไม่มี try/except ครอบ** ⇒ ถ้า
backend โยน exception ผู้ตรวจได้ HTTP 500 แทนรายงาน — **ไม่ได้อะไรเลยแม้แต่โซนที่อ่านสำเร็จ**.
ไล่ยิงคำตอบเพี้ยน 21 แบบผ่าน `ocr_image()` พบ 2 ทางที่หลุดจริง:
- **`"conf": "high"`** (LLM คืน conf เป็นคำ) → `float()` เปลือยโยน `ValueError`
- **`requests.post` โยนสิ่งที่ไม่ใช่ `RequestException`** (URL ผิดรูป → `ValueError`)

แก้ครบแล้ว: `_normalize_blocks` แปลง conf แบบกันพัง · `ocr_image` ดัก `Exception` กว้าง
(แต่ **ลองซ้ำเฉพาะ `RequestException`** — `ValueError` ยิงกี่ครั้งก็ผลเดิม) · `read_zone`
ครอบ try/except แล้วแปลงเป็น **UNREADABLE เฉพาะโซนนั้น** · `text_looks_garbled(None)`
ไม่โยนแล้ว · `_strip_fence` ถอดรั้วซ้อนได้ 3 ชั้น.
เทสต์ล็อกไว้ใน `test_n8n_ocr_response.py` + `test_artwork_ocr_quality.py`.

**⛔ ชั้นที่โค้ดฝั่งเราแก้ไม่ได้: LLM เดาคำที่ไม่มีในภาพ (hallucination).** ถ้า Gemini คืนคำ
ที่ดูสมเหตุสมผลแต่ไม่มีบนฉลาก ไม่มีทางแยกออกจากคำจริง — ต้องแก้ที่ **prompt ใน N8N**
เท่านั้น. prompt ที่แนะนำ + เหตุผลรายบรรทัด + วิธียืนยันอยู่ใน **`docs/N8N_OCR_PROMPT.md`**
(หัวใจคือ *ห้ามแก้คำผิดให้* — prompt OCR ทั่วไปสั่งให้ "อ่านให้ถูก" ซึ่งทำลายงาน QC โดยตรง
เพราะงานนี้คือ *หาคำผิด*; และ **`temperature=0`** ไม่งั้นวัดซ้ำไม่ได้).

---

## 🔎 Artwork — คุณภาพการอ่านข้อความ (OCR) และ `verify_ocr.py`

สองอาการที่ทำให้ **ผลตรวจผิดแบบเงียบ** (ไม่ error, การ์ดขึ้นปกติ, แต่ข้อความที่เอาไปเทียบเป็นขยะ)
วัดจากไฟล์จริง 11 ไฟล์ในโฟลเดอร์ `D:\Digital 2026\Vision-Defect\TEST` แล้วแก้ทั้งคู่.

### ① โซนถูกเรนเดอร์เล็กเกินไป → OCR อ่านไม่ออก (`OCR_CROP_MIN_SIDE`)

`zone_crop_jpg()` (ภาพในการ์ด) มี `CROP_MIN_SIDE=1200` มานานแล้ว แต่ **`ocr.read_zone()`
ซึ่งเป็นตัวที่อ่านข้อความจริง กลับไม่มีขั้นต่ำเลย** — เรนเดอร์ที่ `OCR_DPI=450` ตรง ๆ.
artwork ที่ถูกย่อลงหน้า A4 (เช่น `A4-TUG5311`) ตัวหนังสือจึงเหลือ ~9px แล้ว **recall ตกเหลือ 1.2%**.

**เส้นโค้ง "ความสูงบรรทัดเป็นพิกเซล → recall" (วัดจริง ไม่ใช่ประมาณ):**

| ความสูงบรรทัดใน crop | 9.0 px | 12.4 px | 17.2 px | 22.1 px | 31.0 px |
|---|---|---|---|---|---|
| recall | **1.2%** | 23.2% | 93.9% | 98.5% | 100% |

หน้าผาอยู่ระหว่าง 12–17px → `verify_ocr.MIN_LINE_PX = 15.0` เป็นเกณฑ์เตือน.
แก้ด้วย `_render_for_ocr()`: ถ้าเป็น **PDF** และด้านยาวของ crop < `OCR_CROP_MIN_SIDE` (1200)
ให้ **เรนเดอร์ใหม่ที่ DPI สูงขึ้น** (เพดาน `OCR_DPI_MAX_FACTOR=4.0` เท่า) — ยืนยันบนสถานีแล้ว
`--dpi 1600` ได้ **97.6%** จากไฟล์เดียวกันที่เคยได้ 1.2%.
- ⚠️ **ภาพ raster (ภาพถ่าย/PNG) ห้ามขยาย** — `if not doc.is_pdf: return crop`. การ upscale
  พิกเซลที่ไม่มีข้อมูลเพิ่มไม่ช่วยอะไร (ซ้ำรอยกับดัก "ย่อภาพ raster ลงคือหายนะ" ในหัวข้อกรอบแดง).
  ฝั่งกรอบแดงมี `_upscale_for_ocr()` แยกของตัวเองอยู่แล้ว — คนละชั้น อย่าสับสน.
- ตั้ง `ARTWORK_OCR_CROP_MIN_SIDE=0` = ปิด กลับพฤติกรรมเดิม 100%.

### ② text layer มีจริงแต่พัง → เชื่อเต็ม 100% (`PDFTEXT_GARBLED_CHECK`)

ทาง ② ในตาราง N8N คืนค่า `engine="pdf-text"`, **`conf=1.0` โดยไม่เคยตรวจสอบเลย** —
ถ้า PDF ฝัง encoding มาเสีย (คำออกมาเป็น `A1b2C3`-style ปนเลขกลางคำ) ระบบจะเอา *ขยะ*
ไปเทียบใน MISMATCH/SPELL ด้วย**ความมั่นใจสูงสุด** = ละเมิดกฎเหล็กข้อ 2 เต็ม ๆ.

`ocr.text_looks_garbled()` นับ token ยาวที่ "มีทั้งเลขและตัวอักษร **และมีเลขอยู่กลางคำ**"
(`_malformed`) — เกิน `PDFTEXT_GARBLED_RATIO` (0.30) ของ token ทั้งหมด = ปฏิเสธ text layer
แล้ว **ตกไปใช้ OCR แทน** (ทาง ⑥). ถ้าไม่มี OCR backend → คืนข้อความพร้อม `error` flag
⇒ กลายเป็น **UNREADABLE** ไม่ใช่ "ผ่าน".
- **`PDFTEXT_GARBLED_MIN_TOKENS = 8` คือหัวใจกันเดา** — ต่ำกว่านี้ไม่ตัดสิน. วัดจริง:
  จับได้ **28/29** เคสเสีย, **false positive 0/35** เคสดี. ถ้าลดเลข token ลง FP จะโผล่ทันที
  เพราะโซนสั้น ๆ อย่างรหัสงาน (`AWN202500022003`) หน้าตาเหมือน token ผิดรูปพอดี.
- ตั้ง `ARTWORK_PDFTEXT_GARBLED_CHECK=0` = ปิด กลับพฤติกรรมเดิม 100%.
- **⛔ ไอเดียที่ทดสอบแล้ว "ปฏิเสธ": ใช้ ToUnicode CMap เป็นตัวชี้วัด** — ฟังดูถูกหลักการ
  (ไม่มี ToUnicode = ถอดตัวอักษรกลับไม่ได้) แต่ไฟล์ Cosma ตัวจริง **0/14 ฟอนต์มี ToUnicode
  ทั้งที่ extract ข้อความออกมาถูกเป๊ะ** ⇒ ใช้ตัดสินไม่ได้เลย. อย่าเสียเวลาทำซ้ำ.

### `verify_ocr.py` — ตาข่ายนิรภัยของ OCR (คู่กับ `verify_onnx.py`/`verify_openvino.py`)

`py -3.9 verify_ocr.py <ไฟล์.pdf|โฟลเดอร์> [--dpi 450] [--engines pdf-text,tesseract,n8n] [--verbose]`
**อ่านอย่างเดียว ไม่เขียนอะไรลง `data/` เลย.** 4 ชั้น:
1. **TRIAGE** — ไฟล์นี้มี text layer *ใช้ได้จริง* ไหม + คิด % ของหน้าที่ถูกปกคลุม
   (แบนเนอร์ "✅ ไฟล์นี้มี text layer" บนหน้าเว็บเป็น **ระดับหน้า** แต่การตัดสินจริงเป็น **ระดับโซน**
   → ยืนยันแล้วบน `AWN202500022003`: มีข้อความ 18 ตัว = แค่รหัสงาน, coverage 0%).
2. **GROUND TRUTH** — recall/precision เทียบ text layer ของ PDF (เกณฑ์ `MIN_RECALL`/
   `MIN_PRECISION` = 0.95).
3. **NO-TEXT PROBE** — ยิงโซนว่าง/กราฟิกล้วน: อะไรที่คืนมา = hallucination
   (`MAX_PHANTOM_CHARS=3`). ผลจริง: **Tesseract ไม่เคยหลอน 0/36**.
4. **SELF-CONSISTENCY** — อ่านโซนเดิมที่ 2 DPI (`DPI_B_FACTOR=0.7`) ต้องตรงกัน ≥ `MIN_SELF_AGREE` (0.80).

- **`--layers truth,probe,consistency`** เลือกรันเฉพาะบางชั้น — จำเป็นกับ `--engines n8n`
  ที่มีโควตา: เดิม `--n8n-limit 5` ถูกใช้หมดตั้งแต่ไฟล์แรก (GT 1 + probe 3 + consistency 1)
  ไฟล์ที่เหลือได้แต่ `ERROR(ครบเพดาน)`. **`--layers probe` = ทุ่มโควตาทั้งหมดไปกับการจับ
  hallucination และกระจายได้ทั่วทุกไฟล์** ซึ่งเป็นคำถามที่มักอยากรู้จริง.
- **⚠️ `render()` ต้องตรงกับ `ocr._render_for_ocr()` เป๊ะ** — เคยพลาดมาแล้ว: หลังเพิ่ม
  `OCR_CROP_MIN_SIDE` ให้ production ตัว verify ยังเรนเดอร์ที่ `--dpi` แบน ๆ ⇒ **ตาข่าย
  นิรภัยวัดเส้นทางเก่า** แล้วรายงานว่าโซนเล็กอ่านไม่ออก (ตัวอักษร 9.1px) ทั้งที่ production
  ขยายเป็น ~32px ไปแล้ว — พร้อมข้อความแนะนำที่ล้าสมัยว่า "ocr.read_zone ไม่มีการเพิ่ม DPI"
  (ซึ่งแก้ไปแล้ว) = ชี้ให้ไปแก้ของที่ไม่พัง. **เครื่องมือวัดที่วัดผิดทางแย่กว่าไม่มีเครื่องมือ**.
  ยืนยันความตรงด้วย md5 เทียบกับ `_render_for_ocr()` โดยตรง. `--no-min-side` = วัดเส้นทางดิบ.

**exit code: `0`=ผ่าน · `1`=ไม่ผ่าน · `2`=รันไม่ได้ · `3`=สรุปไม่ได้ (ไม่มี ground truth)**
- **⚠️ `3` แยกจาก `0` โดยตั้งใจ** — เคยเขียนให้พิมพ์ "ผ่านทุก engine" ทั้งที่ไม่มีเฉลยให้เทียบ
  สักตัว = คำตอบที่ผิดแบบมั่นใจ (กฎเหล็กข้อ 2). เช่นเดียวกับที่เคยพิมพ์ "ผ่าน" ทั้งที่ n8n
  error ทุก call → ตอนนี้มี verdict **แยกรายเครื่องยนต์** + `ok/err/skipped` ต่อ engine.
- ⚠️ **normalizer ต้องเป็นกลางกับทุกภาษา** — เดิมตัด diacritic เฉพาะช่วง Latin-1 ⇒ เช็ก/ฮังการี/
  โปแลนด์ถูกหักคะแนนฟรี รายงาน 94.6% ทั้งที่ของจริง 99.8%. ตอนนี้ใช้ NFKD + ตัด combining mark.

**ค่าตั้ง OCR ทุกตัวอยู่ใน `pipeline._ocr_fingerprint()`** (เข้า cache key ของแท็บแปล) —
เพิ่มค่าใหม่แล้วต้องใส่ที่นี่ด้วย ไม่งั้นแก้ค่าแล้วผลไม่เปลี่ยน (ดูทาง ⑤ ในตาราง N8N).

---

## 🧩 Artwork — "PASS ไม่ได้แปลว่าตรวจครบ" (`coverage`)

**จุดบอด QC ที่ใหญ่ที่สุดของโหมดนี้** — ผู้ใช้ลากหลายโซนบนไฟล์เดียวแล้วกดส่งตรวจ
ได้ ✅ PASS **ทั้งที่ชั้นเทียบข้ามแผงไม่เคยทำงานเลย**.

**กลไก:** `checks.check_group_consistency()` เทียบเฉพาะโซนที่ **`group` ตรงกัน** และ
`_vote_panels` จะรันก็ต่อเมื่อ `len(readable) >= 2` ในกลุ่มนั้น. แต่โซนที่ลากใหม่ได้
`group` **คนละตัว** เสมอ (`nextGroupLetter()` ใน JS ↔ `zones.seq_group()` ฝั่ง Python:
A, B, C, …) ⇒ ทุกกลุ่มมีสมาชิก 1 ตัว ⇒ **`MISMATCH_PANELS`/`MISMATCH_ZOOM` ไม่มีทางฟ้อง**.

วัดจริงด้วย `run_inspection()` (PDF 3 แผง แผงกลางน้ำหนักผิด 185 vs 170):

| การตั้งกลุ่ม | verdict | defect |
|---|---|---|
| A/B/C (ค่าเริ่มต้นที่ผู้ใช้ได้) | **PASS** | **0 — พลาดทั้งที่ของจริงต่างกัน** |
| A/A/A (ตั้งเอง) | FAIL | 1 (`MISMATCH_PANELS`) |

ความต่างที่ **ไม่มีชั้นไหนจับได้เลย** ถ้าไม่มีการเทียบข้ามแผง (วัดจากเคสจำลองตามงานจริง):
น้ำหนักสุทธิ · ประเทศผู้ผลิต · วันหมดอายุ (บาร์โค้ดรอดเพราะ `NUMBER_FAIL` เช็ค check digit).

**⛔ ทางแก้ที่ห้ามทำ: ให้ทุกโซนอยู่กลุ่มเดียวกันโดยอัตโนมัติ** — วัดแล้วเช่นกัน:
โซนคนละเนื้อหา (ส่วนผสม / ที่อยู่ / วันหมดอายุ) ที่ถูกจับรวมกลุ่ม ให้ **defect ปลอม 6 รายการ**
ทันที. การจัดกลุ่มอัตโนมัติแบบ sequential เป็น **การตัดสินใจที่อนุมัติไว้แล้ว 2026-07-20**
(แทน size-cluster heuristic ที่จับคู่ผิดเงียบ ๆ) — มันถูกต้องสำหรับ **การเทียบข้ามไฟล์**
(โซนลำดับเดียวกันของไฟล์ a/b ได้ตัวอักษรตรงกันเอง). ปัญหาอยู่ที่ **ไฟล์เดียว** เท่านั้น.
มีเทสต์ `test_forcing_one_group_creates_false_defects` กันไม่ให้ใครมาแก้ผิดทาง.

**ทางแก้ที่ใช้: บอกความจริงว่าตรวจอะไรไปบ้าง** (advisory 100% — ไม่แตะ defects/verdict/การนับ)

- **`checks.check_coverage(zones, ocr_results)`** → `report.json["coverage"]` — คำนวณ
  **หลัง** ได้ defects แล้ว. คืนต่อชั้น: `ran` + `reason` + `groups`. เหตุผลที่แยกกันชัด
  เพราะ **วิธีแก้คนละอย่าง**: `single_zone` (ไม่ต้องทำอะไร) · `no_shared_group` (ตั้งกลุ่ม) ·
  `group_unreadable` (ลากโซนใหม่) · `spellchecker_missing` (ลง pip).
- **เงื่อนไขต้องสะท้อน `check_group_consistency` เป๊ะ ไม่ใช่ประมาณ** — `header` นับเป็น panel,
  `ignore` ไม่นับ, group ว่างไม่ใช่ "กลุ่มเดียวกัน", zoom ต้องมี panel ที่อ่านออกใน **กลุ่มเดียวกัน**.
  พิสูจน์ด้วย brute force **3,072 ชุด**: ไม่มีเคสที่ coverage บอก "ไม่ได้ทำงาน" แล้วชั้นนั้นฟ้องจริง.
  > ⚠️ ถ้าแก้เงื่อนไขใน `check_group_consistency` **ต้องแก้ `check_coverage` ด้วย** —
  > รายงาน coverage ที่ผิดคือ "คำตอบที่ผิดแบบมั่นใจ" ตรงตัว (กฎเหล็ก 2) แย่กว่าไม่มีเลย.
- **UI 2 จุด** (เหมือนแพตเทิร์นของ `hl_risk`):
  - **แถบใต้ผลสรุป** (`coverageHtml()` ใน `artwork_check.js`) — ต้องอยู่ **ติดใต้ verdict**
    เพราะคนอ่าน PASS แล้วมักเลิกอ่านต่อ. เตือน (พื้นเหลือง) เฉพาะเมื่อชั้นที่ไม่ทำงาน
    "แก้ได้" — `no_zoom_zone`/`single_zone` ถือว่าปกติ ไม่ทำให้แถบเป็นสีเตือน.
  - **`renderGroupHint()`** — เตือน **ก่อน** กดส่งตรวจ ตอนจัดโซน (มี panel ≥2 แต่ไม่มี
    กลุ่มซ้ำเลย). ฝั่ง JS ยังไม่รู้ว่าโซนไหนจะอ่านออก จึงเตือนเฉพาะเคสที่ชัดเจนเท่านั้น.
- **⚠️ CSS ของ `renderReport` มี 2 ที่เสมอ** — `artwork_check.html` และ
  `artwork_check_history.html` (หน้าประวัติเรียก `window.awRenderReport` **ตัวเดียวกัน**
  แต่ CSS อยู่คนละ `{% block extra_css %}`). แก้ข้างเดียว = กล่องนั้นโผล่แบบไม่มีกรอบ
  บนหน้าประวัติ **โดยไม่มี error ให้เห็น**.
  > เกิดจริงมาแล้ว **3 คลาส**: `.aw-cov*` (ตอนเพิ่ม coverage) · `.aw-hl-warn` และ
  > `.aw-img-*` (หลุดมานานก่อนหน้านั้น — **รูป preview บนหน้าประวัติกางเป็น 4475px
  > ไม่มี `max-height` ไม่มีกรอบ ไม่เรียงคู่** จับได้ตอนไล่ตรวจรวบยอด 17 ส.ค.).
  > ตอนนี้มี **`tests/test_artwork_report_css.py` (13 ตัว)** ไล่คลาสทุกตัวที่
  > `renderReport()` พ่นออกมาแล้วยืนยันว่ามีกฎครบทั้งสองหน้า **และกฎตรงกันเป๊ะ**
  > (ยกเว้นที่ตั้งใจให้ต่างใน `ALLOW`) + เช็คว่า guard `if (!$("awFile")) return;`
  > ยังอยู่หลัง `window.aw*` ทุกตัว (ถ้า guard เลื่อนไปก่อน หน้าประวัติจะเรียกไม่ได้).
- **รายงานเก่าที่ไม่มี `coverage`** → `coverageHtml()` คืนสตริงว่าง = ไม่แสดงอะไร (ไม่พัง).
- **ข้อความ verdict เปลี่ยนตาม coverage**: PASS ที่มีชั้นขาด → **"✅ PASS — ไม่พบประเด็น
  ในชั้นที่ตรวจ"** (เดิม "ไม่พบประเด็น" เฉย ๆ = พูดเกินจริง). ทั้งแถบและหัวเรื่องคิดจาก
  `coverageGaps()` **ตัวเดียวกัน** — ห้ามเขียนเงื่อนไขซ้ำสองที่ ไม่งั้นแถบเตือนแต่หัวเรื่อง
  บอกว่าไม่พบประเด็น. **ไม่แตะค่า `verdict`** (สี/การนับ/ประวัติเหมือนเดิม).
- เทสต์: `tests/test_artwork_coverage.py` **80 ตัว** (รวม parametrize brute force).

---

## 🖥️ Entrypoints & HTTPS

- **`app.py`** = entrypoint หลัก (ผู้ใช้รัน `py -3.9 app.py`). `threaded=True`. รองรับ HTTPS.
- **`run_server.py`** = gevent (ทางเลือก, deploy หนัก). **ผู้ใช้ไม่ได้ใช้** (เคย import gevent ผิด
  interpreter). `app.py` เพียงพอแล้วหลัง STREAM เปลี่ยนเป็น request/response.
- **HTTPS** (`USE_HTTPS=True`): จำเป็นสำหรับ STREAM (`getUserMedia`). cert: `python generate_cert.py <ip>`.
  ถ้า port 5000 bind ไม่ได้ (WinError 10013) = port ถูก Windows/Hyper-V สงวน → เปลี่ยน `FLASK_PORT`.

---

## 🔧 กับดัก Windows/Python ที่เจอบ่อย

- **Dual Python**: `pip install` เปล่าอาจลงคนละ interpreter กับ `py -3.9` → ใช้ `py -3.9 -m pip install ...` เสมอ.
- **CONFIG_VERSION footer** = ตัวยืนยันว่ารันโค้ดใหม่จริง. bump ทุกครั้งที่แก้ config ที่ต้องให้ผู้ใช้ verify.
- **`pyspellchecker` = ชั้นตรวจ dictionary/คำขาด (deterministic, เชื่อถือได้)** — จับคำที่ไม่ใช่คำจริง เช่น
  `Sunflow`/`EXPIR`/`Thailan` (คำถูกตัด/สะกดผิด) ในคอลัมน์ "สถานะ". ⚠️ **ถ้า import `spellchecker`
  ไม่ได้ ชั้นนี้ถูกข้ามเงียบๆ** (`_get_spellcheckers()` คืน `[]`) → คำผิดขึ้น ✓ เหมือนไม่มีปัญหา = จุดบอด QC.
  อยู่ใน `requirements.txt` แล้ว. บนสถานีติดตั้งที่ user-site (`%APPDATA%\Python\Python39\site-packages`)
  — **`git pull`/`checkout` ไม่ลบ** (คนละที่กับโฟลเดอร์ repo). เช็ค: `py -3.9 -c "from spellchecker import SpellChecker"`.
  หมายเหตุ: คำขาดที่ "ยังเป็นคำจริง" (เช่น `Sunflower Oil`→`Sunflower`) ไม่มี checker ตัวไหนจับได้ →
  ต้องพึ่งการลากโซนให้ครบ + เทียบ panel. ส่วนคอลัมน์ AI (🤖) เป็น advisory เท่านั้น เชื่อเป็น QC ไม่ได้.
- **`pytesseract` + tesseract binary = ชั้นวาดกรอบแดง (display-only)** — รูปแบบ "หายเงียบ" เดียวกับ
  `pyspellchecker` แต่**ไม่อันตรายเท่า**: ไม่มี = ไม่มีกรอบแดงบนไฟล์ outline/ภาพถ่าย แต่ผลตรวจ QC
  เท่าเดิมทุกอย่าง (ไม่ใช่จุดบอด QC). เช็ค: `py -3.9 -c "import pytesseract; print(pytesseract.get_tesseract_version())"`.
  ต่างจาก pyspellchecker ตรงที่ **ต้องลง binary แยกจาก pip** (ดูหัวข้อ Artwork กรอบแดง).
- **⚠️ Deploy IIS ในอนาคต (ยังไม่ทำ — บันทึกไว้ก่อน):** package ที่ลง user-site ของ dev
  **IIS Application Pool identity เข้าไม่ถึง** → ชั้น dict หายเงียบใน production. วิธีแก้ตอน deploy คือทำ
  **venv ในโฟลเดอร์โปรเจกต์** (`py -3.9 -m venv .venv` + `pip install -r requirements.txt`) แล้วชี้ IIS
  FastCGI/HttpPlatform ไปที่ `.venv\Scripts\python.exe` (package อยู่กับโค้ด ทุก identity เห็นเท่ากัน).
  **ระหว่างพัฒนาบนสถานีไม่ต้องทำ venv** — จะแยกเป็น 2 environment ทำให้สับสน + ดึง accel คนละชุด.
  **⚠️ ก่อนสร้าง venv ต้อง pin accel ก่อน:** `requirements.txt` ปัจจุบัน **ไม่ตรงกับ stack ที่จูนไว้**
  (`onnxruntime==1.19.2` comment ทิ้ง, `openvino` ไม่ pin เป็น 2024.6.0) — venv สดจะได้ accel ต่างจาก
  สถานี = อาจตรวจช้าลงหรือเจอ bug OpenVINO 2025 ตรวจไม่เจอแบบเงียบๆ (ดูหัวข้อ OpenVINO ด้านบน).

---

## 🧰 สภาพแวดล้อม & repo

- HW สถานี: **i7-1165G7** (4C/8T, 15W, AVX-512), 16GB DDR4 (single-channel), Iris Xe, Win10 Pro, Python 3.9.13.
- inference bestX (seg): **iGPU (OpenVINO) ≈ 45-50ms/เฟรม (~20-22 FPS)** = ตัวจริงปัจจุบัน;
  ONNX CPU ≈ 280ms (~2.7-3 FPS) = ชั้น fallback; PyTorch ≈ 315ms = fallback สุดท้าย.
- Repo: `iceamonwat09/digital_vision2026`. Dev branch ปัจจุบัน:
  **`claude/artwork-mode-station-testing-llsrp0`** (merge `claude/artwork-multi-zone-errors-k8linm`
  ซึ่งเป็นชุดที่ผู้ใช้ยอมรับแล้วเข้ามาครบ · ก่อนหน้า: `claude/login-registration-feature-2ndkn9`,
  `claude/artwork-ui-layout-1lnwgt`). **ห้าม push ไป main** — และ **การ merge เข้า main
  ผู้ใช้ระบุว่าห้ามตัดสินใจเองโดยพลการ**.
- SQL Server: 172.32.0.50/VisionIQ. Defect log ผ่าน `sp_log_defect` (เก็บภาพ base64).
- **N8N รันบนเครื่องสถานีเอง** → default ของ `N8N_OCR_WEBHOOK_URL` (`config.py`) และ
  `N8N_TRANSLATE_WEBHOOK_URL` (`artwork_check/config.py`) ชี้ `http://127.0.0.1:5678/...`.
  **มี 2 ที่ ต้องแก้ให้ตรงกันเสมอ** — แก้ที่เดียวอีกตัวจะยิงไปเครื่องเก่าแบบเงียบ ๆ.
  ใช้ `127.0.0.1` ไม่ใช่ `localhost` เพราะ Windows resolve `localhost` เป็น `::1` (IPv6) ก่อน
  ถ้า N8N ผูกเฉพาะ IPv4 จะต่อไม่ติดโดยไม่มี error ที่อ่านออก. ย้ายเครื่องเมื่อไรตั้ง env ทับได้
  ไม่ต้องแก้โค้ด. ⚠️ **IP `172.32.201.106` ที่เหลือใน `generate_cert.py`/README = IP ของสถานีเอง
  สำหรับใบรับรอง HTTPS ห้ามเปลี่ยนเป็น 127.0.0.1** ไม่งั้นเครื่องอื่นเปิดเว็บไม่ได้.
- Tests: `pytest tests/` — **795 ผ่าน / 5 fail (pre-existing)** (artwork/label/barcode/auth —
  **ตอนนี้ครอบคลุมกล้อง Hikrobot แล้วผ่าน SDK ปลอม แต่ยังไม่ครอบคลุม live loop ของ USB/RTSP**).
  เพิ่มล่าสุด: **`tests/test_blur_tolerance.py` 27 ตัว** (เครื่องมือวัดเกณฑ์ความเบลอ —
  เน้นกันเครื่องมือ "โกหก": ภาพที่โมเดลไม่เคยเห็นตำหนิต้องไม่ถูกนับว่าทน),
  **`tests/test_hik_exposure.py` 44 ตัว** (โหมดไล่ exposure — คืนค่ากล้องเสมอ ·
  วัด noise ตามเวลา · ด่าน "ฉากนิ่งไหม" ที่ไม่ขึ้นกับ gain · ไม่สรุปจากด้านเดียว),
  **`tests/test_openvino_ir.py` 24 ตัว** (ด่านตรวจไฟล์ IR ของ OpenVINO +
  การซ่อมตัวเอง — ต้นเหตุของอาการ "ONNX ทำงานเสมอ"),
  **`tests/test_accel_backend.py` 18 ตัว** (ตัวตรวจ export ล้าสมัยด้วย hash +
  การรายงาน backend ที่ตกไปตัวช้ากว่า + **แยก 3 ต้นเหตุของ "iGPU ใช้ไม่ได้"**),
  **`tests/test_hik_burst.py` 112 ตัว** (โหมดถ่ายรัว: ความปลอดภัยของ path ·
  วัดที่วัตถุไม่ใช่ทั้งเฟรม · แยกเบลอเคลื่อนที่จากโฟกัส · "ไม่มั่นใจ = ไม่ตอบ" · ลบ · งานเบื้องหลัง),
  `tests/test_hik_camera.py` **47 ตัว** + `tests/test_hik_routes.py` **64 ตัว** +
  `tests/test_hikrobot_diagnose.py` **16 ตัว** (กล้องอุตสาหกรรม Hikrobot ทั้งชุด — ใช้
  `tests/fake_mvs/` จึงรันได้บนเครื่องที่ไม่มีกล้อง/ไม่มี MVS), `tests/test_artwork_pixdiff.py` 45 ตัว (pixel diff ทั้งหน้า/โซน + ด่านปฏิเสธ),
  `tests/test_artwork_report_css.py` **31 ตัว** (CSS ของ renderReport **และฟังก์ชันช่วย**
  ต้องครบทั้ง 2 หน้า),
  `tests/test_n8n_ocr_response.py` 35 ตัว (แกะคำตอบ N8N + ปฏิเสธ HTML + retry),
  `tests/test_artwork_coverage.py` 80 ตัว (รายงานว่าชั้นไหนได้ตรวจจริง),
  `tests/test_artwork_ocr_quality.py` 15 ตัว (crop ขั้นต่ำ + text layer ที่เสีย),
  `tests/test_artwork_ocr_cache.py` 13 ตัว (cache ของแท็บแปลต้องหลุดเมื่อค่าตั้ง OCR เปลี่ยน),
  `tests/test_auth_registration.py` 39 ตัว (ปุ่มลงทะเบียนหน้า login),
  `tests/test_artwork_ownership.py` 30 ตัว (สิทธิ์เห็นประวัติ + ชื่อผู้ตรวจ).
  ⚠️ `tests/test_inspection_golden.py` **fail 5 ตัวอยู่แล้ว** (pre-existing, `NameError: FieldResult`
  ในโมดูล Label Paper) — ไม่เกี่ยวกับ artwork. ยืนยันด้วย `git stash` ก่อนโทษการแก้ของตัวเอง.
- CONFIG_VERSION ปัจจุบัน: **`2026.08.25-expladder`** (เช็คที่ footer ว่ารันโค้ดใหม่จริง).

---

## ✅ Checklist ก่อน commit

- [ ] flag ใหม่ default = พฤติกรรมเดิม? scope เฉพาะโหมดที่ตั้งใจ?
- [ ] fallback ครบทุกทางที่อาจล้มเหลว?
- [ ] การนับ/DB logging เดิมไม่ถูกแตะ? (ถ้าแตะ inference_loop ให้ไล่ดู)
- [ ] `python -c "import ast; ast.parse(open('app.py').read())"` ผ่าน?
      (แตะ JS ด้วย → `node --check static/js/<ไฟล์>.js`)
- [ ] แตะ JS ที่อ้าง element ใหม่ → **เพิ่ม element ใน `templates/` แล้วหรือยัง**?
      (`$("id")` ที่ไม่มีจริงจะเงียบ ไม่ error — ฟีเจอร์หายไปเฉยๆ)
- [ ] ค่าคงที่ที่ใช้ทั้ง Python และ JS แก้ครบสองฝั่งหรือยัง? (เช่น `zones.HL_*` ↔ `HL_*`,
      `ZOOM_MIN` ↔ `min=` ของ `#awZoomRange`)
- [ ] เพิ่มคลาส CSS ใน `renderReport()` → **ใส่ครบทั้ง `artwork_check.html` และ
      `artwork_check_history.html` แล้วหรือยัง?** (`pytest tests/test_artwork_report_css.py`)
- [ ] เพิ่มค่าตั้งที่เปลี่ยน **ผลการอ่านข้อความ** → ใส่ใน `pipeline._ocr_fingerprint()` แล้วหรือยัง?
      (ไม่ใส่ = cache แท็บ "ข้อความ + คำแปล" ไม่หลุด แก้ค่าแล้วผลไม่เปลี่ยนแบบเงียบ)
- [ ] bump `CONFIG_VERSION` ถ้าผู้ใช้ต้อง verify?
- [ ] ถ้าแตะชั้นที่ "ชี้จุดให้คนดู" — เคสไม่มั่นใจ **ไม่แสดง** แทนที่จะเดา? (กฎเหล็ก 2)
- [ ] dependency ใหม่เป็น optional + auto-fallback? (ไม่มี = ฟีเจอร์หาย ไม่ใช่ระบบพัง)
- [ ] commit message ชัด + push ไป dev branch (ไม่ใช่ main)?
