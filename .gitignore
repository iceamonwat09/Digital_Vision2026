# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
viqenv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# YOLO models (large files)
*.pt
!yolov8n.pt  # Keep small demo model if needed
runs/
weights/

# MongoDB
*.db
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Application specific
uploads/
*.jpg
*.png
!static/

# Environment variables
.env
.env.local

# Label Paper Inspection — runtime data, not source
credentials/
data/label_paper/inspections/
data/label_paper/reports/
# Rendered-master cache: regenerated on first inspect, no need to track
data/label_paper/skus/*/master_*dpi.png

# Artwork Proof Check — runtime data, not source
# (zone templates + brand vocab are kept in git so the team shares them)
data/artwork_check/inspections/

# Auth — JWT signing secret is generated per deployment, never commit it
data/auth/

# TLS certs for HTTPS (self-signed, generated per machine via generate_cert.py)
certs/

# IIS deployment — web.config เก็บ path/secret เฉพาะเครื่อง (ต้นแบบอยู่ที่
# deploy/web.config.example ซึ่ง track ไว้ในกิต)
/web.config
# ไฟล์สำรองชั่วคราวของ deploy/diagnose_iis.ps1 (ปกติถูกลบเองตอนจบ)
/web.config.diagbak
# ตัวเรียกของโหมด standalone — สร้างโดย deploy/run_standalone.ps1 และ
# มีรหัสผ่าน SQL + JWT secret อยู่ข้างใน ห้ามเข้ากิตเด็ดขาด
deploy/start_visioniq.cmd
