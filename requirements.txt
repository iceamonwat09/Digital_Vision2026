# Requirements for YOLO-based Defect Detection System

# Web Framework
Flask==3.0.0
Werkzeug==3.0.1

# Computer Vision
opencv-python==4.8.1.78
ultralytics==8.1.0
numpy>=1.26.0

# Database
pyodbc>=4.0.39           # SQL Server connection driver
# pymongo==4.6.0         # (เดิม — MongoDB, ไม่ใช้แล้ว)

# Utilities
Pillow==10.1.0
pygrabber==0.2
comtypes==1.4.16

# Label Paper Inspection (PDF master + Levenshtein + pixel ΔE2000)
PyMuPDF>=1.24.0           # render master.pdf to numpy RGB + extract text layer
python-Levenshtein>=0.25  # fast text distance (pure-Python fallback included)
scikit-image>=0.22.0      # CIE2000 ΔE per-pixel (skimage.color.deltaE_ciede2000)

# OCR backend — POSTs cropped images to an N8N webhook (Gemini / Vision)
requests>=2.31.0

# Vertex AI — uncomment when OCR_BACKEND=vertex and credentials are wired
# google-cloud-aiplatform>=1.60.0
# google-cloud-documentai>=2.27.0

# Optional: For better performance
# torch==2.1.0
# torchvision==0.16.0
